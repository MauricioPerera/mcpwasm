// deploy-preview-web.mjs — orquestador del navegador: crea la cuenta temporal,
// despliega el app y registra el deploy en la plataforma (SOLO metadatos, sin
// apiToken). Es lo que invoca la tool create_preview de la consola.
// Contrato: knowledge/contracts/deploy-preview-web.md. STUB: implementar.

export async function deployPreviewWeb(fetchImpl, opts = {}) {
  throw new Error("NotImplemented: deployPreviewWeb");
}