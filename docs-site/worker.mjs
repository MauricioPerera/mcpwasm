// AUTOGENERADO por build.mjs. No editar a mano.
const DOC_RFC_SKILLS_IN_LLMS_TXT_MD = "# RFC: Publishing Agent Skills through `llms.txt`\n\n- **Status:** Draft (v0.10)\n- **Date:** 2026-06-02\n- **Author:** automators.work\n- **Depends on:** [llmstxt.org](https://llmstxt.org/) spec, [Agent Skills](https://agentskills.io) (`SKILL.md`)\n- **Reference implementation:** [img.automators.work](https://img.automators.work)\n\n---\n\n## TL;DR\n\nAñade una sección `## Skills` a tu `llms.txt`. Cada entrada es un link a un `SKILL.md` remoto. Los agentes descubren skills en el mismo documento que ya leen para entender tu sitio. Sin servidor, sin proceso persistente, sin autenticación extra.\n\n---\n\n## 1. The Problem\n\n### 1.1 Two ecosystems that do not talk to each other\n\n`llms.txt` tells LLMs what a domain *is*. Agent Skills (`SKILL.md`) tell an agent *how to use* that domain. Both exist. Neither knows about the other.\n\nThe result: a site describes itself in `llms.txt`, and it publishes a `SKILL.md` somewhere, but there is **no standardized way** for the site to say *\"here is the skill you need to work with me\"* — and no standardized way for an agent to discover it.\n\n**Concrete example:** `img.automators.work` serves an SVG placeholder image API. It has a `llms.txt` that documents the API. It also has a `SKILL.md` that teaches agents how to build placeholder URLs. But an agent that reads `llms.txt` has **no signal** that the `SKILL.md` exists. The user must know the URL in advance.\n\n### 1.2 Existing alternatives require infrastructure most sites do not have\n\n| Mechanism | What it requires | Works for a static blog? |\n|---|---|---|\n| **MCP** | A persistent server process, transport layer, endpoint | **No** |\n| **A2A** | A persistent server process, agent-to-agent protocol | **No** |\n| **`/.well-known/skills/`** | Static file serving only | **Yes, but only one skill** |\n\nMCP and A2A are the right tools for complex, stateful integrations. They are overkill for:\n\n- **Static sites** (Cloudflare Pages, GitHub Pages, Netlify, Vercel) — no server process exists.\n- **Existing APIs** that want to teach agents which endpoints to call, not reimplement their surface.\n- **Documentation sites** that want to describe interaction patterns once, not maintain a daemon.\n\nThe reference implementation for this RFC (`img.automators.work`) is a **Cloudflare Pages static site**. It cannot run an MCP server. It *can* serve a text file.\n\n### 1.3 The well-known convention is limited\n\nThe emerging `/.well-known/skills/default/skill.md` convention (proposed by Cloudflare, adopted by Mintlify) works when:\n- The agent knows the domain in advance.\n- The agent is configured to probe `.well-known` paths.\n\nIt fails when:\n- The site wants to declare **multiple** skills for different use cases.\n- The agent encounters the site for the first time via `llms.txt`.\n- The site wants to co-locate skill discovery with the rest of its agent-facing context.\n\n**Co-location matters.** An agent that reads `llms.txt` to understand a site already has the right document open. If skill discovery requires a separate probe, that is extra latency and a second source of truth.\n\n### 1.4 Why structured over prose\n\n`llms.txt` is free-form markdown. A site could write skill discovery as prose:\n\n> \"To use this API from an agent, load the skill at /skills/api-client/SKILL.md.\"\n\nThis works for an LLM reading the document. It does not work for:\n\n- **LLM-free parsing.** A regex parser can extract `## Skills` entries without invoking a model. Prose requires inference to normalize (\"here's a skill:\", \"you can use:\", \"for API access:\") — every site phrases it differently.\n- **Non-LLM tooling.** Validators, CI linters, skill crawlers, and IDE integrations need a fixed schema to function. Prose is unparseable without a model in the loop.\n- **Runtime auto-discovery.** An agent runtime can implement native `## Skills` support with a deterministic code path. With prose, the runtime needs per-site prompt engineering to extract the same signal.\n- **Cross-domain indexing.** A skills directory can index published skills across all domains by crawling `## Skills` sections. This is not possible with ad-hoc prose.\n\nThe structure adds zero friction for publishers (two lines of markdown) and enables the tooling layer above it.\n\n---\n\n## 2. Proposal\n\nAdd an optional `## Skills` section to `llms.txt`. Each entry is a link to a remote `SKILL.md` (or a skill bundle archive) that conforms to the Agent Skills spec.\n\n### 2.1 Syntax\n\n```markdown\n## Skills\n\n- [skill-name](https://example.com/skills/skill-name/SKILL.md): description of when to use this skill.\n- [bundle-name](https://example.com/skills/bundle-name.zip): description. <!-- skill: {\"version\":\"1.0.0\"} -->\n```\n\nRules:\n\n1. The section heading MUST be exactly `## Skills` (case-insensitive).\n2. Each list item MUST follow the standard `llms.txt` link convention: `- [title](URL): description`.\n3. The URL MUST resolve to either:\n   - A raw `SKILL.md` document (`Content-Type: text/markdown`), OR\n   - An archive ending in `.zip` or `.tar.gz` containing `SKILL.md` at the archive root.\n4. The remote `SKILL.md` MUST be a valid Agent Skill (YAML frontmatter + body per the Agent Skills spec).\n5. The URL SHOULD be same-origin as the `llms.txt`. Cross-origin URLs are permitted but carry additional security implications (see §4).\n6. The description SHOULD match or summarize the `description` field in the skill's frontmatter.\n\n### 2.2 Optional inline metadata\n\nA skill entry MAY carry a version hint as a trailing HTML comment:\n\n```markdown\n- [pay-with-x402](/skills/x402/SKILL.md): make x402 payments. <!-- skill: {\"version\":\"1.2.0\"} -->\n```\n\nThe only recognized key is `version`. It is a human-readable hint for quick identification, not a security mechanism. Agents that do not understand the comment MUST ignore it.\n\n**For integrity verification and full metadata** (sha256, license, cost estimates, requirements), agents SHOULD fetch `/.well-known/agent-skills/index.json` if available. That document is the canonical metadata source. The `## Skills` entry is the discovery pointer; `.well-known` is the verification and metadata layer.\n\n### 2.3 Discovery flow\n\n```\n1. Agent encounters a domain (via user instruction, URL in context, or search result)\n2. Agent fetches https://example.com/llms.txt\n3. Agent parses the ## Skills section\n4. Agent surfaces available skills to the user\n5. User opts in to one or more skills\n6. Agent fetches the SKILL.md, verifies sha256 if declared, loads it\n7. Agent caches the skill per HTTP cache semantics of the SKILL.md response\n```\n\nStep 5 is mandatory. Agents MUST NOT auto-install or auto-activate skills without explicit user approval (see §4).\n\n### 2.4 Two primary use cases\n\n**Pattern A — API wrapping.** The site has a public HTTP API. The skill teaches the agent how to authenticate and which endpoints to call. The agent executes calls directly against the API.\n\n*Example:* `img.automators.work` teaches agents to call `/{width}x{height}?bg={hex}` to generate placeholder images.\n\n*Example:* [DemoShop](https://demoshop-88e.pages.dev) publishes three skills (product-search, cart-add, checkout-complete) that teach agents to browse a catalog, add items to a cart, and complete a purchase via its public HTTP API — all without authentication or server-side sessions.\n\n**Pattern B — Interaction instructions.** The site has no dedicated API but wants to describe how an agent should interact with it. The skill contains heuristics, preferred phrasing, or task decomposition patterns.\n\n*Example:* A documentation site teaches agents to quote specific sections when answering questions about its content.\n\n---\n\n## 3. Ecosystem Comparison\n\n| Dimension | MCP | A2A | `/.well-known/skills/` | **`## Skills` in `llms.txt`** |\n|---|---|---|---|---|\n| Requires server process | Yes | Yes | **No** | **No** |\n| Works on static hosts | No | No | **Yes** | **Yes** |\n| Multi-skill per domain | Yes | Yes | **No** (fixed path) | **Yes** |\n| Co-located with `llms.txt` | No | No | No | **Yes** |\n| Zero infrastructure beyond static files | No | No | **Yes** | **Yes** |\n| Complex / stateful integrations | **Yes** | **Yes** | No | No |\n| Simple API wrapping | Overkill | Overkill | Limited | **Designed for this** |\n| Version metadata inline | N/A | N/A | No | **Yes** |\n| User opt-in required | Runtime-dependent | Runtime-dependent | Runtime-dependent | **Mandatory** |\n\n**This RFC does not replace MCP or A2A.** It fills the gap below them: the case where a site wants to publish a skill for a simple API or interaction pattern, without running a server.\n\n### 3.1 Relationship to adjacent protocols\n\nThe core idea here — *a Markdown manifest hosted at your domain that agents read, with no server process* — is being arrived at independently by others, which is a useful signal that the pattern is sound.\n\n**[auth.md](https://github.com/workos/auth.md) (WorkOS).** A protocol where a service hosts an `AUTH.md` describing how an agent can register and authenticate on the user's behalf, discovered via `/.well-known/oauth-authorization-server` metadata. It is **complementary, not competing**, with this RFC:\n\n- **Different layer.** `auth.md` answers *\"how does an agent authenticate to this service?\"*; `## Skills` answers *\"what can an agent do here, and how?\"*. This RFC's example skills are deliberately auth-free; `auth.md` is exactly the missing authentication layer. A fully agent-ready domain can serve **both**: `## Skills` for capability discovery and `AUTH.md` for credential acquisition.\n- **Discovery alignment.** `auth.md` is discovered through a `.well-known` pointer that carries a `skill` field referencing the manifest — the same shape as this RFC's `/.well-known/agent-skills/index.json` metadata layer (§8, Open Question 5). The two `.well-known` documents can coexist.\n- **Terminology overlap.** `auth.md` uses the word \"skill\" for its auth manifest, distinct from an [Agent Skills](https://agentskills.io) `SKILL.md`. Implementers should not conflate the two.\n\n**General principle.** As more agent-facing Markdown conventions appear (`llms.txt`, `## Skills`, `AUTH.md`, `.well-known/*`), the risk is fragmentation. This RFC's position: `llms.txt` is the natural **co-located discovery layer** — an agent already reading it to understand a domain should find capability and pointer information there in one fetch, with `.well-known` and protocol-specific manifests (like `AUTH.md`) as the verification and specialized layers beneath it.\n\n### 3.2 Relationship to `agents.txt` (the action layer)\n\n**[agents.txt](https://agents-txt.com).** A capability-declaration file (`/agents.txt` + optional `/agents.json`) that advertises which agent-facing protocols a site supports — payments (x402, MPP, AP2), authorization (agent-auth, OAuth2), MCP, A2A, UCP, WebMCP, and **Skills**. It positions itself as \"Layer 4 (action)\" above `robots.txt` (access) and `llms.txt` (content). Its `Skills:` directive and its `/.well-known/agent-skills/index.json` both build on the [Agent Skills](https://agentskills.io) `discovery/0.2.0` index schema.\n\nThis RFC and `agents.txt` are **complementary along two axes**:\n\n- **Discovery surface.** `agents.txt` answers *\"which capability families does this site expose?\"* across many protocols at once; `## Skills` in `llms.txt` is the co-located, content-adjacent surface for the Skills family specifically. A site can serve both: a `Skills:` line in `agents.txt` and a `## Skills` section in `llms.txt`, pointing at the **same** `SKILL.md` artifacts.\n- **Trust.** `agents.txt`'s Skills layer carries discovery and integrity (`digest`) but **explicitly leaves authenticity out** (\"prioritize discovery simplicity over trust verification\"). That is exactly the gap this RFC's Tier 2 closes (§4.6): offline-key ed25519 signatures + agent-side key pinning. The two stack cleanly — `agents.txt`/agentskills.io for discovery and integrity, this RFC for authenticity.\n\n**Concrete interoperability (implemented).** The reference generator emits `/.well-known/agent-skills/index.json` as a **superset of the agentskills.io `discovery/0.2.0` schema**: every skill carries the fields that schema expects (`name`, `type: \"skill-md\"`, `description`, `url`, `digest: \"sha256:…\"`) *plus* this RFC's extensions (`version`, `license`, `homepage`, raw `sha256`, ed25519 `signature`, and a top-level `signing_key`). One file therefore satisfies both an `agents.txt`/agentskills.io consumer (discovery + integrity) and a Tier-2 consumer (authenticity) — the publisher maintains no second artifact. The redundant `digest` (prefixed form) and `sha256` (raw hex) fields are kept in parallel so existing consumers that read raw `sha256` are not broken.\n\n### 3.3 Skills as the *recipe* layer over tool discovery\n\nA growing body of work attacks **tool-definition bloat** — the cost of injecting every tool's schema into the model context up front. It comes in two camps:\n\n- **Model/client-side.** Anthropic's [Tool Search Tool](https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-search-tool) (`defer_loading`) loads only the few relevant tools on demand (reported ~85% fewer tool-definition tokens). Tool-retrieval / \"tool RAG\" (LangChain toolkits, Gorilla) does the same via embeddings.\n- **Server-side.** The MCP [progressive-disclosure SEP](https://github.com/modelcontextprotocol/modelcontextprotocol/issues/1888) proposes a server capability that exposes one meta-tool with `searchTools` modes instead of registering hundreds.\n\n**This RFC is complementary, and sits at a third place: publisher-side, and one level up.** Two differences matter:\n\n1. **Search finds; a skill *prescribes*.** Tool search reduces the token cost of *having* many tools, but it does not tell the agent the *order* to use them in, the dependencies, or what to do on error — the agent must still formulate a query (a cold-start problem) and discover the procedure, sometimes by failing. A published skill carries the **recipe**: the procedure at the level of intent. (n8n's own MCP concedes this — it ships an `instructions` blob telling the agent the call order, because the tool list alone is insufficient.)\n2. **Trust + curation.** None of the above says *who* published a capability or verifies it; this RFC adds offline-key signatures + pinning (§4.6) and a human-curated procedure tier.\n\nSo `llms.txt` Skills can be the **server-less catalog** these consumer-side mechanisms need a source for — Tool Search needs a catalog; the SEP needs the server to implement it; a `## Skills` section is that catalog, owned by the domain, with the recipe and the signature attached.\n\n**Evidence (reference POC, [`evals/poc_orchestration/`](../evals/poc_orchestration/README.md)).** Against a live n8n MCP server (which exposes **25 tools** to build a workflow), a small local model was driven through a real agentic loop in five arms spanning *how much capability sits in context as tool schemas vs. as skill prose*:\n\n| arm | tool defs in context | result |\n|---|---:|---|\n| raw MCP (n8n's own setup) | 25 | at a 4k context, the tools **do not even fit** (overflow before turn 1) |\n| skill + declared tool segment | 8 | runs; same task, −68% tool surface |\n| skill + 1 generic MCP passthrough | **1** | runs the full recipe, keeps node introspection |\n| skill + 1 generic HTTP tool (REST, no MCP) | **1** | builds and `POST`s the workflow in a single call |\n\nThe same workflow gets built with **one tool definition in context instead of 25**. The decisive, model-independent result is the 4k overflow: the raw tool list is *unusable* on a small model, while a skill's declared segment is not. (Honest scoping: once the tools *fit*, the orchestration gap among the MCP arms is marginal on an easy task with a capable model; and the leanest REST arm trades the SDK's parameter validation for speed. It is a single-model POC, not a benchmark — see its README.)\n\nThis is the same pattern already live on the reference site **[demoshop](https://demoshop-88e.pages.dev)**, whose skills (`product-search`, `cart-add`) teach an agent to call one REST endpoint directly — zero tools in context — for *simple* capabilities. The n8n POC shows the pattern **scales to a complex, tool-heavy capability**, and maps the cost of each rung.\n\n**Reproducing the server, not just shrinking it.** A companion experiment ([`n8n-skills-sdk`](https://github.com/MauricioPerera/n8n-skills-sdk)) takes the last rung — *no MCP at all*. The n8n MCP's build path turns out to be a thin wrapper over the [`@n8n/workflow-sdk`](https://www.npmjs.com/package/@n8n/workflow-sdk) package (local code→JSON parse + `validateWorkflow`) plus one REST call (`POST /workflows`): `get_sdk_reference`, `validate_workflow`, and `create_workflow_from_code` map onto the package's own exports and the public API. Delivered as a published `SKILL.md` + three local tools (`reference`/`validate`/`create`), a local model built, validated, and created the same workflows the MCP does — in **~3 tool calls vs. the MCP's 6–7**, with **validation preserved** (unlike a naive REST call) and only **3 tool definitions in context**. Two capable ~8–9B models (Qwen, Granite) succeeded cleanly; a 3B model hit a code-writing floor (a property of the model, not the delivery mechanism). The lesson for this RFC: when a capability's \"tools\" are really *a procedure over a library plus an API*, a **published skill can carry the procedure** and the library/API do the work — the server becomes optional.\n\n---\n\n## 4. Security\n\n1. **No auto-installation.** Agents MUST NOT activate a discovered skill without explicit user opt-in.\n2. **Same-origin preference.** Agents SHOULD treat same-origin skills as lower friction than cross-origin skills.\n3. **Content verification.** If `sha256` is declared in the inline metadata, agents MUST verify the hash of the fetched file and refuse to load on mismatch.\n4. **Cross-origin skills require elevated confirmation.** If a skill URL is on a different origin from the `llms.txt` that references it, agents SHOULD require additional user confirmation beyond the base opt-in. This prevents a compromised `llms.txt` from silently delegating trust to a third-party host.\n5. **Least privilege.** Skills loaded from a domain operate within the permission scope of that domain. They cannot request filesystem access, network calls to unrelated origins, or other capabilities not stated in the skill's frontmatter without explicit user re-confirmation.\n\n### 4.6 Signing and authenticity (resolves Open Question 4)\n\n**Threat model.** A `sha256` declared in `## Skills` or in `index.json` provides **integrity** — the content was not corrupted in transit or by a CDN. It does **not** provide **authenticity**, because the same document that points to the skill also asserts its hash. A party that can modify the `llms.txt` or the index can rewrite the pointer and the hash together. Defending against a *compromised or malicious publisher* requires a signature whose trust is not rooted solely in the live document.\n\n**Two-tier model:**\n\n- **Tier 1 — Integrity (`sha256`).** MUST be verified when declared. Cheap, offline, no key management. Already specified in §2.2 and §4.3.\n- **Tier 2 — Authenticity (signature).** OPTIONAL. The publisher signs each `SKILL.md` with a private key kept **offline, off the web server**. The public key is published (e.g., `/.well-known/agent-skills/signing-key.pub`, and/or a `signing_key` field in `index.json`). Agents verify the signature over the skill content.\n\n**What the signature actually buys you.** Publishing the public key on the same origin does not, by itself, defeat a fully compromised origin: an attacker who controls the server can swap key, signatures, and content together. The value is:\n\n1. **Offline key.** Because the private key is not on the server, a server compromise *alone* (without the key) cannot forge valid signatures. The attacker can only serve unsigned or invalid-signature content, which a verifying agent rejects.\n2. **Agent-side key pinning (TOFU).** The agent pins the origin's public key on first use and warns / requires re-confirmation if it later changes. This detects silent key or skill swaps across sessions — the gap that plain `sha256` cannot close.\n\n**Higher assurance.** For identity-bound provenance, publishers SHOULD consider **keyless signing via a transparency log** (e.g., Sigstore): the artifact is signed by an OIDC identity — such as the CI workflow that built it — and recorded in a public log, so verification asserts *who* produced the artifact, not merely that *some* key did. Trade-off: verification needs network access to the transparency log, which sandboxed agents may lack; same-origin ed25519 + pinning works fully offline.\n\n**Reference implementation.** `scripts/generate.py` signs with ed25519 (deterministic, RFC 8032); `scripts/verify_signatures.py` verifies each `SKILL.md` against the published public key.\n\n---\n\n## 5. On Agent-Side Discovery Triggers\n\nThis RFC defines the *publishing* side of the protocol. It does not mandate when or how agents decide to read `llms.txt`.\n\nToday, most agents read `llms.txt` only when the user explicitly directs them to a URL or instructs them to research a site. Proactive, background skill discovery does not yet occur in mainstream runtimes.\n\nThis is not a defect of the proposal — it is the current state of the ecosystem. MCP, A2A, and `/.well-known/skills/` face the same trigger problem: all require the user or agent to know the site exists before discovery begins.\n\n**What this RFC adds, even today:** when an agent does encounter a site — via user instruction, a URL in context, or a tool result — a `## Skills` section gives it an unambiguous, machine-readable signal that purpose-built skills are available. That is more than any site can currently express through `llms.txt` alone.\n\nAs agent runtimes evolve toward more proactive web discovery, the `## Skills` section provides the declaration primitive those systems will need.\n\n### 5.1 Proactive Discovery Mechanisms\n\nTo reduce the friction of skill discovery from \"user must know to ask\" to \"agent finds it automatically\", this RFC proposes four optional mechanisms that sites MAY implement and agents MAY support. None are required for compliance, but each lowers the barrier for the agent side.\n\n#### Mechanism A — HTTP `Link` header\n\nEvery HTTP response from the domain MAY include a `Link` header pointing to `llms.txt`:\n\n```\nLink: </llms.txt>; rel=\"llms.txt\"\n```\n\n**Why it matters:** When the agent makes its *first* request to the domain (e.g., loading the homepage or calling an API endpoint), it receives this header for free. No extra round-trip is needed to probe for `llms.txt`. The agent parses the header, fetches `llms.txt`, and discovers skills before the user even asks.\n\n**Reference implementation:** [DemoShop](https://demoshop-88e.pages.dev) sends this header on every API response.\n\n#### Mechanism B — DNS TXT record\n\nThe domain MAY publish a DNS TXT record:\n\n```\ndemoshop-88e.pages.dev. 300 IN TXT \"llms-txt=https://demoshop-88e.pages.dev/llms.txt\"\n```\n\n**Why it matters:** DNS resolution is often faster than HTTP and can be cached at the resolver. The agent can discover the `llms.txt` URL *before* making any HTTP request, allowing it to preload skills or show them to the user at the exact moment the domain is mentioned.\n\n**Limitation:** DNS TXT records are limited to ~255 bytes per string and require DNS access, which sandboxed agents may not have.\n\n#### Mechanism C — HTML `<meta>` tag\n\nIn addition to the existing `link rel=\"alternate\"`, the site MAY include a visible `<meta>` tag in the HTML `<head>`:\n\n```html\n<meta name=\"llms-txt\" content=\"/llms.txt\">\n```\n\n**Why it matters:** It is more discoverable by HTML parsers than `rel=\"alternate\"` (which some agents ignore). It also signals to developers inspecting the page source that the site exposes agent-facing resources.\n\n#### Mechanism D — The `/llms.txt` convention probe\n\nSimilar to `robots.txt` or `favicon.ico`, agents MAY adopt the convention:\n\n> \"On first encounter with any new domain, attempt `HEAD /llms.txt`. If it returns 200, read it. If 404, assume no skills are published and proceed normally.\"\n\n**Why it matters:** It requires zero cooperation from the site (the file either exists or it doesn't). It costs one cheap HEAD request (~200 bytes). It works for every domain on the web, not just those that implement headers or DNS records.\n\n**Trade-off:** It adds one round-trip per new domain. Agents SHOULD cache negative responses (404) to avoid repeated probes.\n\n### 5.2 Recommended Agent Behavior (Non-Normative)\n\nWhen an agent runtime decides to implement proactive discovery, this is the recommended flow:\n\n```\n1. Agent encounters a domain (user instruction, URL in context, search result)\n2. Check local cache for llms.txt of this domain\n   2a. Hit -> skip to step 5\n   2b. Miss -> continue\n3. Probe for llms.txt using any available mechanism:\n   3a. Check HTTP Link header of the first request already made\n   3b. Check DNS TXT record (if DNS available)\n   3c. HEAD /llms.txt (fallback, cache 404 for 24h)\n4. If llms.txt found, parse and cache it\n5. If ## Skills exists, surface skills to the user\n6. User opts in explicitly to one or more skills\n7. Agent fetches SKILL.md, verifies sha256 if declared, loads it\n8. Agent caches the skill per HTTP cache semantics\n```\n\n**Key principle:** The user opt-in (step 6) remains mandatory. Even with automatic discovery, agents MUST NOT auto-install skills without explicit approval.\n\n**Cache strategy:**\n- `llms.txt`: cache per HTTP `Cache-Control` (typically short TTL, e.g., 5 minutes).\n- Negative 404: cache for 24 hours to avoid repeated probes.\n- `SKILL.md`: cache per HTTP `Cache-Control` (can be long, e.g., immutable).\n\n### 5.3 Observed Execution Gap\n\nDiscovery (Section 5) is only half the problem. Even when an agent successfully finds and reads a `SKILL.md`, there is no guarantee it will *execute* the skill according to its instructions.\n\n**Empirical evidence from DemoShop:** In a live test, an agent:\n1. Discovered the `cart-add` skill via `llms.txt`.\n2. Downloaded and read `SKILL.md`, which specified a Python `urllib` pattern with explicit headers and error handling.\n3. Ignored the prescribed pattern and used its generic PowerShell `Invoke-WebRequest` tool instead.\n\nThe skill was treated as \"interesting documentation\" rather than as a \"behavioral contract\" that overrides the agent's default tools. The API call succeeded, but the agent did not follow the skill's intent: it used different tooling, different error handling, and different conventions than those the site designed.\n\n**Why this happens:**\n- Agent runtimes do not have a \"skill execution mode\" that suspends generic tools and forces compliance with a downloaded `SKILL.md`.\n- Skills are not sandboxed or enforced; they are suggestions that the model may or may not follow.\n- The reward function of the agent (\"complete the user's task\") does not penalize tool substitution if the outcome is similar.\n\n**Implications:**\n- A publisher cannot assume that publishing a skill guarantees the agent will use it as written.\n- The RFC defines *what* to publish and *where* to find it, but not *how* the runtime must enforce execution.\n- This is intentionally out of scope (Section 7), but it is a real, observable gap that future work should address.\n\n### 5.4 Discovery from User Instructions\n\nA related but distinct gap occurs when the user includes a domain in their prompt, but the agent does not associate that URL with the need to discover skills.\n\n**Empirical evidence from img.automators.work:** A user asked:\n\n> \"crea una imagen de 600 x 50 px de color verde https://img.automators.work/\"\n\nThe agent:\n1. Did not fetch `https://img.automators.work/llms.txt`.\n2. Did not discover the `placeholder` skill.\n3. Did not learn that the site generates SVG placeholder images via `/600x50?bg=22c55e`.\n4. Instead, created a local PNG with Python/PIL, ignoring the URL entirely.\n\n**Why this happens:**\n- The agent interpreted the URL as a decorative reference, not as a service endpoint.\n- The runtime has no rule that says: \"when a user mentions a domain, check `/llms.txt` before acting.\"\n- The default behavior is \"do what the user asked with local tools\" rather than \"delegate to the domain's published skills.\"\n\n**Implications:**\n- Skill discovery is not triggered by implicit references in user prompts.\n- Even Mechanism D (the `/llms.txt` convention probe) only works if the agent is already navigating the domain, not when the domain is merely mentioned in text.\n- This suggests that either:\n  - Agents need a pre-action step: \"before answering any task involving a domain, probe its `llms.txt`\", or\n  - User instructions need an explicit signal (e.g., \"use the skill at...\") to trigger discovery.\n\n---\n\n## 6. Why This Is Worth Doing\n\n- **Deployable on any static host.** A Cloudflare Pages site, a GitHub Pages repo, a Netlify deploy — any host that can serve a text file can publish skills through this mechanism. No server process required.\n- **Self-describing at the source.** A site ships its API *and* the skill for consuming it in the same deploy. No third-party marketplace required.\n- **Version-locked to the API.** When the API changes, the skill changes in the same commit. No drift between capability and documentation.\n- **Co-located discovery.** An agent reading `llms.txt` finds skills in the same document, in one fetch, with no additional probing.\n- **Multi-skill support.** A single domain can publish skills for different use cases (e.g., read-only queries, authenticated writes, admin operations) as separate entries.\n- **No gatekeeper.** Publishing a skill is a `git push`. No approval process, no marketplace submission.\n\n---\n\n## 7. Non-Goals\n\n- Replacing local skill filesystems or marketplaces — both remain valid distribution modes.\n- Defining a new skill format — this RFC reuses the Agent Skills `SKILL.md` spec as-is.\n- Mandating skill *execution* behavior — that is the agent runtime's responsibility.\n- Replacing MCP or A2A for complex, stateful integrations — this RFC targets the simpler, static-hosting case.\n\n---\n\n## 8. Open Questions\n\n1. **Should `llms.txt` grow parallel `## MCP` and `## Agents` sections**, making it the single discovery document for a domain's full agent surface? Or should each standard manage its own discovery separately?\n2. **Cross-origin skill trust model.** Should cross-origin skills be disallowed entirely, allowed with elevated confirmation, or allowed freely? Current proposal: allowed with elevated confirmation (§4.4).\n3. **Archive format.** Should `.zip` be the only mandated archive format, or should `.tar.gz` remain in scope? `.zip` is more universally supported; `.tar.gz` is more natural for git-hosted skill bundles.\n4. **Signature scheme beyond `sha256`.** Resolved (§4.6): a two-tier model — `sha256` for integrity (required when declared), plus optional ed25519 signatures over an offline key combined with agent-side key pinning for authenticity. Sigstore is recommended for identity-bound provenance where network-based verification is acceptable.\n5. **Relationship to `/.well-known/skills/`.** Resolved: sites SHOULD serve both. The two mechanisms have distinct, non-overlapping roles:\n\n   | Layer | Mechanism | Role |\n   |---|---|---|\n   | Discovery | `## Skills` in `llms.txt` | Passive, co-located with domain context; zero extra fetch |\n   | Metadata & verification | `.well-known/agent-skills/index.json` | sha256, version, license, cost estimates |\n   | Active install | `.well-known/skills/default/skill.md` | Single-skill convention probe |\n\n   URLs in `## Skills` MAY mirror those already declared in `.well-known/agent-skills/index.json`, reusing the same artifacts without duplication. Agents that support both get redundant coverage at zero extra cost for publishers.\n\n6. **Multi-project origins (namespacing).** Resolved (Executable Skills extension v0.5 SS2.5): an optional per-skill `scope` key -> runtime-side tool prefixing `<scope>__<name>` (published bytes and hashes untouched, preserving content-addressing and the universal-template property), plus one `skills-memory` line per scope binding each snapshot to its scope's skills. Public-name collisions are skipped with a diagnostic (first wins); attestations unaffected. Original problem statement, kept for the record: an origin that aggregates skills for MULTIPLE projects — e.g. a GitHub user/org ROOT site bridging several project pages, since project-page origins are not directly consumable (a URL's origin strips the path) — hits two hard limits, found empirically while aggregating two real publishers on one origin (2026-07-10):\n   - **Skill-name uniqueness.** MCP tool names must be unique per server, and runtimes key skills by name. Two projects publishing the standard knowledge skills (`search_knowledge`, `get_concept`, `list_concepts` — deliberately universal templates) collide on any shared origin.\n   - **One `skills-memory` per origin.** The origin-memory declaration is a single line per `llms.txt`, so a second project's knowledge snapshot has nowhere to go.\n\n   Candidate directions, deliberately unresolved: a `scope`/`project` field in the inline metadata that runtimes use to prefix tool names (`kdd__search_knowledge`); publisher-side name prefixes at generation time; multiple scoped `skills-memory` lines bound to skills by scope; or per-path discovery (contradicts origin-based discovery and the URL-origin model). Whatever the resolution, it must preserve content-addressing (hashes bind to bytes, so re-rooted aggregation today requires byte-identical artifacts).\n\n---\n\n## 9. Reference Implementation\n\n[`img.automators.work`](https://img.automators.work) is a live Cloudflare Pages static site — no server process, no MCP server, no A2A endpoint.\n\n- [`/llms.txt`](https://img.automators.work/llms.txt) — contains a `## Skills` section\n- [`/skills/placeholder/SKILL.md`](https://img.automators.work/skills/placeholder/SKILL.md) — the skill itself\n- [`/docs/rfc-skills-in-llms-txt.md`](https://img.automators.work/docs/rfc-skills-in-llms-txt.md) — this document\n- [`/scripts/parse_llms_txt_skills.py`](https://img.automators.work/scripts/parse_llms_txt_skills.py) — reference parser\n- [`/scripts/validate.py`](https://img.automators.work/scripts/validate.py) — validator\n- [`/schema/llms-txt-skills.schema.json`](https://img.automators.work/schema/llms-txt-skills.schema.json) — JSON schema\n\n---\n\n## 10. Changelog\n\n- **v0.10 (2026-07-10):** Open Question 6 RESOLVED by Executable Skills extension v0.5 SS2.5 (scopes: declarative per-skill `scope` -> runtime-side `<scope>__<name>` prefixing + scoped `skills-memory` lines). The OQ entry records the resolution and keeps the original problem statement for provenance. No core-RFC normative change.\n- **v0.9 (2026-07-10):** Added Open Question 6 (multi-project origins / namespacing), from field experience aggregating two real publishers (llms-txt-skills, KDD) on one GitHub Pages root origin: skill-name uniqueness per origin and the single `skills-memory` line make same-origin multi-project aggregation collide today; candidate directions listed, unresolved. No normative change.\n- **v0.8 (2026-06-02):** Extended §3.3 with the `n8n-skills-sdk` companion experiment — reproducing the n8n MCP's build path entirely with a published skill + `@n8n/workflow-sdk` (local parse/validate) + the REST API (no MCP): a local model builds+validates+creates the same workflows in ~3 tool calls (vs the MCP's 6–7), validation preserved, 3 tool defs in context; cross-model (8–9B succeed, 3B hits a code-writing floor).\n- **v0.7 (2026-06-02):** Added §3.3 \"Skills as the recipe layer over tool discovery\" positioning this RFC against tool-bloat work (Anthropic Tool Search / `defer_loading`, MCP progressive-disclosure SEP #1888, tool RAG) as the publisher-side, recipe-bearing layer those consumer/server-side mechanisms need a catalog for; added a reference POC (`evals/poc_orchestration/`) driving a live n8n MCP (25 tools) through five arms (25→1 tool defs in context), with demoshop as the simple-capability anchor.\n- **v0.6 (2026-06-02):** Added §3.2 \"Relationship to `agents.txt` (the action layer)\" positioning agents.txt as a complementary discovery layer and this RFC as the authenticity layer it omits; the reference generator now emits `/.well-known/agent-skills/index.json` as a superset of the agentskills.io `discovery/0.2.0` schema (`type` + `digest`) so one file serves both agents.txt/agentskills.io consumers and Tier-2 signature verifiers.\n- **v0.5 (2026-06-01):** Added §4.6 \"Signing and authenticity\" with a two-tier trust model (sha256 integrity + optional ed25519 signatures over an offline key, plus agent-side key pinning); resolved Open Question 4; added a reference signing implementation (`scripts/generate.py`, `scripts/verify_signatures.py`); added §3.1 \"Relationship to adjacent protocols\" positioning auth.md (WorkOS) as a complementary authentication layer.\n- **v0.4 (2026-05-19):** Added §1.4 \"Why structured over prose\" addressing the free-form equivalence objection; simplified §2.2 inline metadata to version-only hint, delegating sha256/license/cost to `.well-known/agent-skills/index.json`; resolved Open Question 5 with explicit layer table for `## Skills` vs `.well-known`; fixed duplicate §6 heading.\n- **v0.3 (2026-05-19):** Expanded agent-side discovery triggers with four mechanisms (HTTP Link header, DNS TXT, HTML meta tag, convention probe); added recommended agent behavior flow; added cache strategy; updated examples with DemoShop.\n- **v0.2 (2026-04-21):** Added §3 ecosystem comparison; expanded §4 with cross-origin security rule; added §5 on discovery triggers; added §1.2 infrastructure barrier argument; refined two use-case patterns in §2.4; updated open questions.\n- **v0.1 (2026-04-20):** Initial draft.\n\n";
const DOC_EXT_EXECUTABLE_SKILLS_MD = "# Extension: Executable Skills\n\n**Status:** Draft (v0.5)\n**Date:** 2026-07-10\n**Extends:** [RFC: Publishing Agent Skills through `llms.txt`](./rfc-skills-in-llms-txt.md) (v0.9)\n\n---\n\n## 1. Motivation\n\nThe core RFC publishes skills as instructional documents (`SKILL.md`). Section 5.3 of the core RFC identifies the *execution gap*: an agent may read instructions and still improvise, substitute tools, or drift from the published behavior.\n\nThis extension closes that gap for publishers who want it: a skill MAY additionally ship an **executable artifact** — a small JavaScript file with a declared input schema — that a conforming runtime executes *verbatim* inside a sandbox, instead of asking a model to improvise from prose.\n\nThe result is **Static MCP** — *your tools are files, not servers*: skills published as static, hash-verified content and executed sandboxed on demand. What static site hosting did to web servers (\"do not run Apache, publish HTML\"), this does to MCP servers: the publisher runs no infrastructure at all; the MCP server is materialized per request from the published files and evaporates after responding (ephemeral instance, durable definition).\n\nExecuting third-party code is only acceptable under a strict security model. This extension therefore defines two things, and only two things:\n\n1. The **publication format** for executable skills (publisher side).\n2. The **minimum requirements** for a runtime that chooses to execute them (consumer side).\n\nEverything else — transport, agent UX, approval flows — remains governed by the core RFC. A publisher that adopts this extension remains fully conformant with the core RFC: agents that do not understand executable skills simply fall back to `SKILL.md` prose.\n\n## 2. Publication format\n\n### 2.1 Skill entry\n\nAn executable skill is declared with two additional keys in the skill entry's JSON comment:\n\n```markdown\n- [sum_numbers](/skills/sum_numbers/SKILL.md): Sum two numbers a and b. <!-- skill: {\"version\":\"1.0.0\",\"tool\":\"/skills/sum_numbers/tool.js\",\"tool_sha256\":\"58daf86111bf7278446eb7e0e8c6384713b50cdb6fa97ac039e23846d723dc3e\"} -->\n```\n\n| Key      | Type   | Requirement | Meaning |\n|----------|--------|-------------|---------|\n| `version`| string | inherited from core RFC | Human-readable hint. |\n| `tool`   | string | REQUIRED for executable skills | Path to the executable artifact. MUST be a same-origin path (relative or absolute path, never a full URL to another origin). |\n| `tool_sha256` | string | REQUIRED when `tool` is present | Lowercase hex SHA-256 of the exact bytes served at `tool`. |\n| `scope`  | string | OPTIONAL | Namespace for multi-project origins (§2.5). Pattern `^[a-z][a-z0-9_-]*$`. When present, runtimes MUST expose this skill's tools under `<scope>__<tool-name>`. |\n\nThe key is deliberately **not** `sha256`: the core RFC already uses `sha256` (inline and in `/.well-known/agent-skills/index.json`) for the hash of the fetched `SKILL.md`. The two keys MAY appear on the same skill line and verify different files; runtimes MUST NOT conflate them.\n\nRationale for staying inline (rather than only in `/.well-known/agent-skills/index.json`): the core standard's value proposition is *one static file, no infrastructure*. Two inline keys preserve that. Publishers who already maintain the well-known index MAY mirror the same `tool`/`tool_sha256` fields there; if both are present and disagree, runtimes MUST refuse the skill.\n\n### 2.2 The artifact (`tool.js`)\n\nThe artifact is a single JavaScript file that registers exactly one tool:\n\n```js\nregisterTool({\n  name: \"server_time\",\n  description: \"Return the current server time.\",\n  inputSchema: { type: \"object\", properties: {} },\n  async handler(args) {\n    const r = await host.fetchOrigin(\"/api/time\");\n    return JSON.parse(r.body);\n  }\n});\n```\n\nContract:\n\n- The file MUST call `registerTool(def)` exactly once, where `def.name` matches the skill name in `llms.txt`, `def.inputSchema` is a JSON Schema object, and `def.handler` is a function (sync or async).\n- The handler receives already-parsed `args` and returns a JSON-serializable value. Throwing reports a tool error to the caller; it MUST NOT crash the runtime.\n- The only ambient capability available is `host.fetchOrigin(path, opts?)`: an HTTP fetch **restricted to the publishing origin**, returning `{ status, body }` (body as text, possibly truncated by the runtime). Relative paths are resolved against the publishing origin. `opts` is optional: `{ method?: \"GET\" | \"POST\", body?: string, contentType?: string }`. The `method` defaults to `\"GET\"` if omitted. Only `GET` and `POST` are valid (any other method, or providing a `body` with `GET`, MUST throw); the request body is a string capped by the runtime (reference implementation: 16 KB); `contentType` is the only controllable header (default `application/json` when a body is present). There is no other network, filesystem, timer, or environment access.\n- The artifact MUST NOT rely on any global other than `registerTool`, `host`, and standard ECMAScript built-ins. No `fetch`, no `process`, no dynamic import.\n- **ECMAScript means ECMAScript.** Web/WHATWG APIs that feel universal do NOT exist in the sandbox: no `URLSearchParams`, no `URL`, no `TextEncoder`/`TextDecoder`, no `atob`/`btoa`, no timers. Build query strings with `encodeURIComponent` by hand. (Field-tested: a published skill using `URLSearchParams` passes hash verification and loads, then fails at call time.)\n\n### 2.3 Versioning and updates\n\nThe `tool_sha256` pins the artifact. Publishing a new artifact version means serving new bytes at `tool` and updating both `tool_sha256` and `version` in `llms.txt`. Runtimes cache by hash; a stale hash simply keeps serving the old, verified artifact until the index updates.\n\n### 2.4 Origin memory: search snapshots\n\nA publisher MAY additionally ship a **search snapshot**: a pre-built, hash-pinned full-text index over its own content, so that skills can offer real search over a purely static site — no backend API required. Declared with one origin-level line in `llms.txt` (place it **before** the `## Skills` section — some conformant parsers fold trailing non-list lines into the last skill entry):\n\n```markdown\n<!-- skills-memory: {\"snapshot\":\"/skills-index.snapshot\",\"snapshot_sha256\":\"<hex>\",\"format\":\"minimemory-okf-v1\"} -->\n```\n\n| Key | Requirement | Meaning |\n|---|---|---|\n| `snapshot` | REQUIRED | Same-origin path to the snapshot file. |\n| `snapshot_sha256` | REQUIRED | Lowercase hex SHA-256 of the exact bytes served at `snapshot`. (Named to avoid the core RFC's `sha256`, same rationale as `tool_sha256`.) |\n| `format` | REQUIRED | Snapshot format identifier. Runtimes MUST ignore a `skills-memory` line whose format they do not support (skills load normally; the capability is simply absent). This draft registers one format: `minimemory-okf-v1` (BM25-only index exported by [minimemory](https://github.com/MauricioPerera/minimemory)'s `WasmOkfIndex`; no embeddings, no query-time model needed). |\n\nWhen a supported snapshot is declared, verified, and loaded, the runtime injects one additional capability into that origin's skills:\n\n- `await host.memorySearch(query, k?)` -> `{ hits: [{ text, score, title, concept_id }] }` — BM25 search over the origin's own snapshot. `query` MUST be a string; `k` is capped by the runtime (reference implementation: 10). Like `fetchOrigin`, the capability is strictly origin-scoped: a skill can only search the snapshot its own publisher shipped. Skills SHOULD check for the capability before calling it (`typeof host.memorySearch === \"function\"`) — it is absent when the runtime does not support the declared format or when verification fails.\n\nUpdating content means re-exporting the snapshot and updating `snapshot_sha256` — same lifecycle as `tool_sha256` (§2.3).\n\nAn `llms.txt` MAY declare **multiple** `skills-memory` lines when each carries a distinct `scope` key (§2.5); at most ONE line may omit `scope` (the default/unscoped memory). Without scopes, exactly one line is allowed — the pre-v0.5 behavior, unchanged.\n\n### 2.5 Scopes: multi-project origins\n\nResolves core RFC Open Question 6. An origin that aggregates skills from\nMULTIPLE projects — e.g. a GitHub user/org ROOT site bridging several project\npages, since a URL's origin strips the path — collides on two axes: tool\nnames must be unique per MCP server, and (pre-v0.5) only one `skills-memory`\nline existed per origin. `scope` resolves both **declaratively, at the\naggregation layer, without touching published bytes** — which is mandatory,\nbecause content-addressing binds hashes to bytes: renaming inside a `tool.js`\nwould destroy the universal-template property (one stable `tool_sha256`\necosystem-wide for shared tools like `search_knowledge`).\n\nSemantics:\n\n- `scope` (optional, per skill line, pattern `^[a-z][a-z0-9_-]*$`). When\n  present, the runtime MUST expose every tool registered by that skill's\n  `tool.js` under the public name `<scope>__<registered-name>` (double\n  underscore separator), and route calls back to the unmodified sandbox\n  context. The `tool.js` bytes, its `tool_sha256`, and the names it registers\n  internally are UNCHANGED — prefixing is a host-boundary rename only.\n- Skill recipes exposed as MCP resources follow the public name\n  (`skill://<scope>__<name>`), as does any recipe-fallback tool parameter.\n- A `skills-memory` line with `scope` binds its snapshot to the skills of\n  that scope only: the runtime injects each scope's `host.memorySearch` from\n  its own verified snapshot. Skills without `scope` get the unscoped memory\n  (if any). A skill whose scope has no memory line simply lacks the\n  capability (fails closed in-sandbox, as ever).\n- Two skill lines MUST NOT expose the same public tool name. Runtimes MUST\n  reject (skip, with a diagnostic) any skill whose public names collide with\n  an already-loaded one — first line wins, matching top-to-bottom document\n  order.\n- Attestations (ext-skill-attestations) are UNAFFECTED: they bind\n  `origin + skill + tool_sha256` as published by the ORIGINAL project;\n  `scope` is an aggregation-layer concern and does not enter signed payloads.\n- Backward compatibility: an `llms.txt` with no `scope` keys behaves exactly\n  as under v0.4.\n\nAggregation recipe (informative): to republish another project's skills from\na root origin, copy each skill line verbatim, re-root its `url`/`tool` paths\n(`/project/...`), add `scope`, and add the project's `skills-memory` line\nwith the same `scope` and re-rooted `snapshot` path. All hashes remain valid\nbecause the served bytes are identical. Beware tools with baked root-relative\nfetch paths (e.g. a generated `get_concept`): they only work on the origin\nthey were built for and SHOULD be omitted from aggregation.\n\n## 3. Runtime requirements\n\nA runtime that executes skills published under this extension (a *gateway*, an agent-embedded engine, etc.):\n\n1. **Integrity.** MUST fetch the artifact, compute SHA-256 over the exact received bytes, and compare with the declared `tool_sha256`. On mismatch the skill MUST be excluded (not degraded to prose, not executed) and the rejection SHOULD be observable (log or diagnostic surface). This matches the mandatory-refusal language of core RFC §4.\n2. **Isolation.** MUST execute artifacts in a sandbox where the host environment is not reachable: no ambient network, no filesystem, no host secrets. Host capabilities are injected explicitly; this extension defines only `host.fetchOrigin`, scoped to the publishing origin. A runtime MUST reject any `fetchOrigin` target that resolves outside that origin.\n3. **Resource limits.** SHOULD enforce memory, stack, and execution budgets per invocation, so a hostile or buggy artifact cannot exhaust the runtime. Execution budgets SHOULD be **deterministic** (e.g. counting interrupt-callback invocations) for synchronous execution, not wall-clock: platforms that freeze the clock during synchronous execution (Cloudflare Workers freezes `Date.now()` as a Spectre mitigation) make wall-clock deadlines silently inert against `while(true)` — field-tested: a wall-clock 2 s deadline never fired and the platform killed the request at ~40 s. Deterministic gas does not bound time spent *waiting* on async capabilities, so runtimes SHOULD additionally enforce a wall-clock timeout on each capability call (async waits do advance the clock). (Reference implementation values: 64 MB memory, 1 MB stack, interrupt-count gas budget that cuts an infinite loop in a few seconds, plus a 10 s wall-clock timeout per `fetchOrigin` call.)\n4. **Trust domain.** Artifacts from the *same origin* MAY share an execution context; artifacts from *different origins* MUST NOT. Runtimes SHOULD isolate per skill even within one origin (defense in depth).\n5. **Memory integrity.** A runtime that supports a declared snapshot format MUST verify `snapshot_sha256` over the exact fetched bytes before loading the index. On mismatch the capability MUST NOT be injected (skills that call it get a controlled tool error, not a crash), and the rejection SHOULD be observable — mirroring the artifact rule in requirement 1. Snapshot bytes are covered by the same network timeout as `fetchOrigin` calls.\n6. **Exposure.** How verified skills are exposed to agents is out of scope. The reference implementation exposes them as an MCP server (`tools/list` / `tools/call`), which requires no agent-side changes at all — but any interface satisfying 1–4 conforms. Runtimes exposing skills over MCP MUST return `structuredContent` as an object per the MCP spec: when a handler returns an array or a primitive, wrap it (reference implementation: `{ \"result\": <value> }`). For backwards compatibility with clients that do not support structured outputs, runtimes SHOULD also return the serialized JSON of the *original* (unwrapped) result in a `content` block as a text item (the reference implementation does). Field-tested: unwrapped arrays pass curl inspection but are rejected by conformant client SDKs (`invalid_type`), making the tool unusable in practice.\n\n## 4. Security considerations\n\n- **What the hash buys:** whoever can edit the site cannot silently swap artifact bytes out from under a cached/pinned hash; and a runtime never executes bytes it did not verify. It does **not** authenticate the publisher — for that, compose with the signature scheme of core RFC §4.6 (signing `llms.txt` transitively pins every declared `tool_sha256`, alongside the core RFC's `sha256` for the prose).\n- **What the sandbox buys:** a malicious artifact can, at worst, compute and call its own origin — the same things any visitor's browser can already do to that origin. It cannot reach the runtime's credentials, other tenants, or other origins.\n- **Residual risks:** a compromised publisher origin can still publish a *correctly hashed* malicious artifact (garbage in, verified garbage out); `fetchOrigin` responses are attacker-controlled input to the artifact; and resource limits bound, but do not eliminate, denial-of-service pressure on the runtime. Cross-origin user confirmation rules from core RFC §4 apply unchanged.\n\n## 5. Reference implementation\n\nWorking end-to-end chain (all deployed):\n\n| Piece | URL |\n|---|---|\n| Runtime + gateway source (QuickJS-wasm sandbox on Cloudflare Workers) | https://github.com/MauricioPerera/mcpwasm |\n| Demo publishing site (`llms.txt` with two executable skills) | https://llmstxt-demo-site.rckflr.workers.dev/llms.txt |\n| Realistic publisher (D1-backed bookstore: search/detail/stock/**order** skills, plus permanent robustness fixtures: a deliberately hash-mismatched skill and an infinite-loop skill) | https://llmstxt-bookstore.rckflr.workers.dev/llms.txt |\n| Docs publisher (the standard's own docs, searchable via a hash-pinned BM25 snapshot + `search_spec` skill — the spec searches itself) | https://llmstxt-docs.rckflr.workers.dev/llms.txt |\n| Gateway exposing them as an MCP server | `POST https://llmstxt-gateway.rckflr.workers.dev/mcp?origin=<url-encoded origin>` |\n\nThe gateway demonstrates: discovery from `llms.txt`, per-skill SHA-256 verification with exclusion on mismatch, sandboxed execution (QuickJS-wasm) with async handlers in per-skill contexts, origin-scoped `fetchOrigin` including POST write skills (a real order decrementing D1 stock), deterministic gas interruption of infinite loops, and end-to-end consumption by an unmodified MCP client (headless Claude configured with only the gateway URL).\n\n## 6. Open questions\n\n1. Should `tool`/`tool_sha256` live only in `/.well-known/agent-skills/index.json` instead of inline, keeping the inline comment single-key? (This draft chooses inline for zero-infrastructure parity; feedback welcome.)\n2. Artifact size limit (the reference implementation truncates fetched bodies at 4 KB for `fetchOrigin` but does not yet cap artifact size).\n3. A declared capability list per skill (e.g. `\"capabilities\":[\"fetchOrigin\"]`) so runtimes can surface least-privilege prompts before loading.\n4. WASM artifacts as a second artifact type (`tool.wasm` + WIT-style interface) for non-JS publishers.\n\n## 7. Changelog\n\n- **v0.5 (2026-07-10):** Scopes for multi-project origins (SS2.5, resolves core RFC OQ6): optional `scope` key per skill line -> runtime-side tool prefixing `<scope>__<name>` (published bytes and hashes untouched, preserving the universal-template property); multiple `skills-memory` lines, one per scope, each binding its snapshot to its scope's skills; public-name collision -> skip with diagnostic (first wins); attestations unaffected. Fully backward compatible: no `scope` -> v0.4 behavior.\n- **v0.4 (2026-07-02):** Origin memory (§2.4): hash-pinned search snapshots (`skills-memory` line, `snapshot_sha256`, pluggable `format`) and the origin-scoped `host.memorySearch` capability; runtime requirement for snapshot integrity. Field-tested: the reference gateway serves BM25 search over the spec's own docs published as a static snapshot (`minimemory-okf-v1`).\n- **v0.3 (2026-07-02):** Lessons from a realistic field test (D1-backed bookstore + unmodified MCP client): explicit sandbox-globals note (ECMAScript only, no WHATWG APIs); `fetchOrigin` extended with optional `{method, body, contentType}` (GET/POST only) enabling write skills; resource budgets SHOULD be deterministic gas, not wall-clock (frozen clocks in Workers); MCP exposure MUST wrap non-object results in `structuredContent`.\n- **v0.2 (2026-07-02):** Rename `sha256` -> `tool_sha256` to avoid collision with the core RFC's `sha256` (hash of the fetched `SKILL.md`), per review feedback.\n- **v0.1 (2026-07-02):** Initial draft, extracted from the mcpwasm reference implementation.\n";
const DOC_EXT_SKILL_ATTESTATIONS_MD = "# Extension: Skill Attestations\n\n**Status:** Draft (v0.4)\n**Date:** 2026-07-09\n**Extends:** [Extension: Executable Skills](./ext-executable-skills.md) (v0.4)\n**Design donor:** the three-layer knowledge-governance PoC in [ccdd/examples/okf-integration](https://github.com/MauricioPerera/ccdd/tree/main/examples/okf-integration) (working Ed25519 attestation + freshness tooling), adapted here to executable skills.\n\n---\n\n## 1. Motivation\n\nThe executable-skills extension gives two rings of trust: `tool_sha256` proves **integrity** (these bytes are these bytes) and core RFC §4.6 signatures prove **authenticity** (the publisher signed them). Neither answers the question an agent operator actually cares about before running third-party code: **has a human I trust reviewed this code, and do they still stand behind it?** A correctly hashed, correctly signed malicious artifact passes both rings — the residual risk §4 of the executable-skills extension explicitly admits.\n\nThis extension adds the third ring: **attestations** — signed, expiring statements by named reviewers that a specific artifact, on a specific origin, was reviewed and endorsed. Two design commitments inherited from the donor PoC:\n\n1. **Age is a proxy, not truth.** Whether code is still trustworthy is a human judgment; the only honest way to capture it is a human signature with an explicit validity window. Nothing here pretends to automate that judgment.\n2. **Zero infrastructure, like everything else in this standard.** Attestations are static JSON files. No transparency log, no CA, no server.\n\nThis layer is **OPTIONAL**. Publishers who skip it lose nothing they have today; runtimes decide what unattested skills are worth.\n\n## 2. Attestation format\n\n### 2.1 The attestation object\n\n```json\n{\n  \"origin\": \"https://llmstxt-bookstore.rckflr.workers.dev\",\n  \"skill\": \"create_order\",\n  \"tool_sha256\": \"a3c2…\",\n  \"attester\": \"human:mauricio\",\n  \"signed_on\": \"2026-07-02\",\n  \"valid_until\": \"2027-07-02\",\n  \"signature\": \"<ed25519-signature-base64>\",\n  \"note\": \"reviewed for v0.3 capability contract; no data exfiltration paths\"\n}\n```\n\n| Field | Requirement | Meaning |\n|---|---|---|\n| `origin` | REQUIRED | The publishing origin the review applies to. |\n| `skill` | REQUIRED | Skill name as it appears in `llms.txt`. |\n| `tool_sha256` | REQUIRED | The exact artifact hash reviewed. A new artifact version voids the attestation by construction. |\n| `attester` | REQUIRED | Reviewer identity, `<type>:<name>` (e.g. `human:mauricio`). |\n| `signed_on` / `valid_until` | REQUIRED | Validity window, `YYYY-MM-DD` in UTC. An attestation is EXPIRED when the current UTC date is strictly greater than `valid_until` (the end date is inclusive). |\n| `signature` | REQUIRED | Ed25519 over the signing payload (§2.2), **base64** (aligned with the core RFC tooling, `scripts/verify_signatures.py`). |\n| `note` | OPTIONAL | Human context; not part of the signed payload semantics beyond §2.2. |\n\n### 2.2 Signing payload\n\nThe signature covers the UTF-8 bytes of the canonical string:\n\n```\norigin + \"\\n\" + skill + \"\\n\" + tool_sha256 + \"\\n\" + signed_on + \"\\n\" + valid_until\n```\n\n**`origin` is deliberately inside the payload.** Identical bytes on a different origin are a *different skill in effect*: `fetchOrigin` is scoped to the publishing origin, so the same code talks to a different backend. An attestation MUST NOT be replayable across origins. (This is the one place this extension diverges from the donor PoC, which had no cross-site dimension.)\n\n### 2.1b Alternative: Sigstore (keyless) attestation\n\nThe Ed25519 model above requires pre-registering every reviewer's public key\n(§3) — a coordination step that does not scale past a handful of trusted\nindividuals. As a **second, optional** attestation type, an entry MAY carry a\n`sigstore_bundle` instead of `signature`:\n\n```json\n{\n  \"origin\": \"https://llmstxt-bookstore.rckflr.workers.dev\",\n  \"skill\": \"create_order\",\n  \"tool_sha256\": \"a3c2…\",\n  \"attester\": \"sigstore:https://github.com/OWNER/REPO/.github/workflows/release.yml@refs/heads/main\",\n  \"signed_on\": \"2026-07-02\",\n  \"valid_until\": \"2027-07-02\",\n  \"sigstore_bundle\": { \"mediaType\": \"application/vnd.dev.sigstore.bundle+json;version=0.2\", \"...\": \"...\" }\n}\n```\n\n`attester` is prefixed `sigstore:` followed by the exact OIDC identity URI\n(e.g. a GitHub Actions workflow ref, matching the certificate's Subject\nAlternative Name) — not a pre-registered key name. `sigstore_bundle` is a\nstandard [Sigstore Bundle](https://github.com/sigstore/protobuf-specs) (DSSE\nenvelope + Fulcio certificate chain + Rekor transparency-log inclusion proof).\nThe signed payload is an [in-toto Statement v1](https://in-toto.io/Statement/v1)\nwhose `predicate` carries the *same five fields* as §2.2's canonical string\n(`origin`, `skill`, `tool_sha256`, `signed_on`, `valid_until`) — a verifier\nMUST confirm the DSSE-embedded predicate matches the attestation's own\ntop-level fields; without that cross-check, a validly-signed bundle for skill\nA could be relabeled as skill B's attestation without re-signing anything (the\nsignature only covers the DSSE payload, not whatever JSON a caller wraps\naround it).\n\n**What this buys over pre-registered Ed25519:** *any* OIDC identity can\nproduce a valid Sigstore signature (verifiable against Fulcio's public root +\nRekor's public transparency log) with no prior coordination with the\npublisher or runtime operator. The runtime's trust decision shifts from\n\"which public keys do I whitelist\" to \"which *identities* do I require\" —\ne.g. \"only this specific GitHub Actions release workflow\", still an\nallowlist, but one that scales to any number of contributors without a\nmanual key-registration step per person. This is the core RFC §4.6\n\"higher assurance... keyless signing via a transparency log\" recommendation,\napplied to this extension's third trust ring specifically.\n\n**What it does not change:** the reviewer registry (§3) still decides *which*\nidentities are trusted — Sigstore does not eliminate the allowlist, it changes\nits unit from public keys to OIDC identity strings, and removes the private\nper-reviewer key-management burden (Fulcio issues short-lived certificates\nper signing operation instead).\n\n### 2.3 Publication\n\nAttestations are published by the origin as a static JSON array at:\n\n```\n/.well-known/agent-skills/attestations.json\n```\n\n(consistent with the core RFC's `/.well-known/agent-skills/index.json` metadata layer). Multiple attestations per skill (different reviewers) are allowed and encouraged. Third-party reviewers MAY additionally publish their attestations on their own domain; how runtimes discover those is an open question (§6).\n\n### 2.4 Choosing an attester type (normative guidance)\n\nBoth attester types are normative and fully supported. This section only sets\nthe **default recommendation**, so adopters face one decision instead of two\nparallel models:\n\n- **Sigstore (`sigstore:`) is RECOMMENDED for new deployments.** It scales\n  without per-reviewer key registration (the bottleneck §5 names), binds\n  attestations to auditable OIDC *identities* rather than bearer keys, and\n  removes the private-key management burden entirely (Fulcio issues\n  short-lived certificates per signing operation). This is the core RFC §4.6\n  recommendation applied to this extension.\n- **Pre-registered Ed25519 (`human:`) is the profile for constrained\n  environments.** Choose it when verification must run on a platform without\n  outbound network or filesystem access at verification time (confirmed:\n  Cloudflare Workers, §4), in air-gapped or offline review workflows, or when\n  a deployment deliberately avoids depending on external trust infrastructure\n  (Fulcio/Rekor availability).\n\nDecision rule: **if your runtime can reach Sigstore's public trust\ninfrastructure at verification time, use `sigstore:`; otherwise use\n`human:` Ed25519.** A registry MAY contain both types simultaneously (§3);\nruntimes evaluate each entry per its own type. Publishers do not choose —\nattester type is a property of who reviews and where verification runs, not\nof the published skill.\n\n## 3. Reviewer registry (trust anchor)\n\nVerification requires knowing reviewers' public keys. The registry is **runtime-side configuration**, not publisher data — the publisher hosting the keys that vouch for the publisher would be circular:\n\n```json\n{\n  \"human:mauricio\": { \"public_key\": \"<ed25519-public-key-base64>\", \"registered_at\": \"2026-07-02\" },\n  \"sigstore:https://github.com/OWNER/REPO/.github/workflows/release.yml@refs/heads/main\": {\n    \"issuer\": \"https://token.actions.githubusercontent.com\",\n    \"registered_at\": \"2026-07-02\"\n  }\n}\n```\n\nRuntimes MAY populate it by explicit configuration (recommended) or TOFU-pin keys on first sight (consistent with core RFC §4.6). An attestation from an unknown attester verifies as UNKNOWN-ATTESTER, not as valid. For a `sigstore:` entry, the registry maps the identity to its expected OIDC `issuer` (rather than a public key); verification asks Sigstore's own trust infrastructure (Fulcio, Rekor) whether the bundle's certificate was validly issued to that exact identity by that issuer — the runtime never handles or stores a reviewer's private key material, because there isn't one to store.\n\n## 4. Runtime behavior\n\nA runtime that supports this extension evaluates, per skill, at discovery time:\n\n| Verdict | Condition |\n|---|---|\n| `attested` | ≥1 attestation with valid signature from a registered key, matching `origin`+`skill`+`tool_sha256`, within its validity window |\n| `expired` | matching attestations exist but all are past `valid_until` |\n| `invalid` | a signature fails verification (SHOULD be surfaced loudly — a forged attestation is worse than none) |\n| `unattested` | no matching attestation (including hash mismatch after a skill update) |\n\n**Precedence with multiple attestations:** `invalid` dominates — if any attestation for the skill carries a signature that FAILS verification against a *registered* key, the skill's verdict is `invalid` regardless of other valid attestations (a forged signature is a red flag, not noise; and since `attestations.json` is publisher-controlled, an attacker poisoning it can only block the publisher's own skills). Otherwise: any valid, in-window attestation → `attested`; else any expired one → `expired`; else `unattested`. Attestations from unregistered attesters are ignored for precedence (they are unverifiable, verdict input UNKNOWN-ATTESTER only as diagnostics).\n\nRuntimes SHOULD support at least two modes:\n\n- **Advisory** (default): all verified skills load; the verdict is exposed to the consumer (e.g. in the tool description or a discovery diagnostic) so agents/operators can decide.\n- **Enforcing**: only `attested` skills load; everything else is excluded like a hash mismatch.\n\nVerification MUST be independently computable from static files: fetch `attestations.json`, verify Ed25519 (available in Workers/browsers via WebCrypto) against the registry. No callbacks to any service.\n\n**Sigstore verification is the one exception to \"no callbacks\":** confirming a Fulcio certificate chain and a Rekor inclusion proof needs network access to Sigstore's public trust infrastructure (the core RFC §4.6 already flags this trade-off for identity-bound provenance). A platform without outbound network access at verification time (or without filesystem access to cache Sigstore's TUF-distributed trust root — **confirmed in the mcpwasm reference implementation: Cloudflare Workers cannot run Sigstore verification for exactly this reason**) simply does not support the `sigstore:` attester type; it MUST fall back to treating such an entry as UNKNOWN-ATTESTER, not silently accept or crash.\n\n## 5. Security considerations\n\n- **What this buys:** an agent operator can require \"code reviewed by someone I chose, endorsement not expired\" before executing third-party artifacts. Compromising the publisher's site is no longer enough to ship malicious code to enforcing runtimes — the attacker also needs a registered reviewer's private key.\n- **What this does not buy:** a review is a point-in-time human judgment; it cannot guarantee the absence of vulnerabilities. Key compromise of a reviewer defeats the ring for that reviewer's attestations (mitigation: multiple reviewers, short windows). Revocation before `valid_until` is unsolved here (§6) — short validity windows are the honest stopgap.\n- **Expiry is a feature.** An expired attestation returning to `unattested` is the freshness layer doing its job: endorsements rot unless renewed, exactly like the donor PoC's TTL model.\n\n## 6. Open questions\n\n1. Discovery of third-party attestations not hosted on the publishing origin (reviewer-side feeds? aggregators?).\n2. Revocation before expiry (a static revocation list at the same well-known path is the zero-infra candidate).\n3. Should `enforcing` mode distinguish per-capability risk (e.g. require attestation only for skills using POST)?\n4. Alignment with core RFC §4.6: a signed `llms.txt` could pin `attestations.json` transitively.\n\n## 8. Reference implementation\n\n[mcpwasm](https://github.com/MauricioPerera/mcpwasm)'s local runtime\n(`bin/mcpwasm-local.mjs`, `sigstore-attest.mjs`) implements §2.1b: opt-in via\n`--require-attestation <issuer>|<identity>`, verified end-to-end against a\nreal, live, publicly fetched Sigstore bundle (an npm package's own SLSA\nprovenance attestation) proving the Fulcio/Rekor verification path genuinely\nworks, plus all documented rejection paths (no attestations.json, no matching\nentry, expired, malformed dates, invalid/mismatched bundle). The gateway\n(`worker-gateway.mjs`) still implements only the Ed25519 model — confirmed\nduring development that Sigstore's TUF-based trust-root cache needs\nfilesystem access Cloudflare Workers does not have (§4).\n\n## 7. Changelog\n\n- **v0.4 (2026-07-09):** Added §2.4 (normative guidance): Sigstore is the RECOMMENDED default attester type for new deployments; pre-registered Ed25519 becomes the explicit profile for constrained environments (no network/filesystem at verification time, air-gapped review, no external trust-infrastructure dependency). Converges the two parallel models into one decision rule; no wire-format or verification-semantics change — v0.3 registries and attestations remain valid as-is.\n- **v0.3 (2026-07-09):** Added §2.1b: Sigstore (keyless) attestations as a second, optional attester type alongside Ed25519 — closes the \"only pre-registered reviewers scale\" bottleneck (§5) by trusting OIDC identities instead of pre-shared keys. Updated §3 (reviewer registry entry shape for `sigstore:` attesters) and §4 (network-access exception, platform-support fallback to UNKNOWN-ATTESTER). Added §8 pointing at the reference implementation.\n- **v0.2 (2026-07-02):** Review fixes: base64 encoding for signatures/keys (aligned with core RFC tooling), canonical-origin and date normalization rules for the signing payload, inclusive-end UTC expiry semantics, and explicit verdict precedence (invalid dominates).\n- **v0.1 (2026-07-02):** Initial draft, adapted from the ccdd okf-integration PoC (Ed25519 attestations + freshness windows + key registry), with origin-binding added for the cross-site skill context.\n";
const DOC_MCPWASM_README_MD = "# mcpwasm — Static MCP\n\n[![CI](https://github.com/MauricioPerera/mcpwasm/actions/workflows/ci.yml/badge.svg)](https://github.com/MauricioPerera/mcpwasm/actions/workflows/ci.yml)\n[![npm](https://img.shields.io/npm/v/%40rckflr%2Fmcpwasm)](https://www.npmjs.com/package/@rckflr/mcpwasm)\n\n**Static MCP: your tools are files, not servers.** Tools are published as\nstatic, hash-verified content and executed sandboxed on demand. What static\nsite hosting did to web servers — \"don't run Apache, publish HTML\" — this does\nto MCP servers: don't run an MCP server, publish files. The publisher needs\nzero infrastructure; the MCP server is materialized per request from those\nfiles and evaporates after responding (ephemeral instance, durable definition).\n\nmcpwasm is the reference implementation: a sandboxed runtime for third-party\nMCP tools (untrusted tool code inside QuickJS-wasm on Cloudflare Workers),\nplus a gateway that turns any static site publishing `llms.txt` with\nexecutable skills into an MCP server.\n\nThink \"php-wasm, but for MCP tools\": the platform owner embeds the host, loads\n`tool.js` files, and each tool runs isolated in a QuickJS WebAssembly sandbox.\nThe only bridge from the sandbox to the platform's internals is an explicit\ncapability the host injects. No capability, no access.\n\nThis repo integrates with the [llms-txt-skills](https://github.com/MauricioPerera/llms-txt-skills)\nstandard via two provisional extensions adopted in the spec: **executable\nskills** (v0.4, with *origin memory*) and **skill attestations** (v0.4). See\nthe dedicated sections below.\n\n## Use it now — any static site → an MCP server\n\nPoint the local runtime at any origin that publishes `llms.txt` with executable\nskills. It fetches `/llms.txt`, verifies every `tool_sha256`, sandboxes each\nskill in QuickJS-wasm, and speaks MCP over stdio — **no account, no deploy, no\ninfrastructure on either side**:\n\n```bash\nnpx -y @rckflr/mcpwasm https://usuario.github.io\n```\n\nWire it into an MCP client (Claude Code, Cursor, Cline, …):\n\n```json\n{\n  \"mcpServers\": {\n    \"misitio\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@rckflr/mcpwasm\", \"https://usuario.github.io\"]\n    }\n  }\n}\n```\n\nThat is the whole path from a static site to a running MCP server. The two\nother ways to use mcpwasm are for when you specifically need them: the **hosted\ngateway** (multi-tenant, on Cloudflare Workers — serves many origins behind one\nendpoint, with an onboarding step per origin) and the **embeddable library**\n(`@rckflr/mcpwasm` — for building your own host). Both are documented below;\nthe local runtime above needs none of it. Its honest limits and the\n`index.json`/attestation options are detailed under\n\"[Local MCP runtime](#local-mcp-runtime--no-gateway-at-all)\".\n\n## Use as a library (npm)\n\nThe embeddable host — what the gateway itself builds on — ships as\n[`@rckflr/mcpwasm`](https://www.npmjs.com/package/@rckflr/mcpwasm):\n\n```bash\nnpm install @rckflr/mcpwasm\n```\n\n```js\nimport { AsyncToolHost } from \"@rckflr/mcpwasm\";\n\nconst host = new AsyncToolHost({ allowedOrigin: \"https://example.com\" });\nawait host.init();\nhost.loadToolSource(toolJsSource); // a tool.js that calls registerTool({...})\nconst tools = host.listTools();\nconst result = await host.callTool(\"sum_numbers\", { a: 2, b: 40 });\nhost.dispose();\n```\n\nNotes:\n\n- In Cloudflare Workers, pass a pre-built asyncify module via the `quickjs`\n  option (see `worker-gateway.mjs` for the `CompiledWasm` import pattern and\n  import `@rckflr/mcpwasm/shim` first).\n- Subpath exports: `/host` (sync `ToolHost`), `/host-async`, `/mcp-core`,\n  `/mcp-core-async`, `/llmstxt-parse`, `/shim`.\n- The sync `ToolHost` lazy-imports the optional peer `quickjs-emscripten`\n  unless you pass a pre-built module; the async host's dependencies install\n  with the package.\n- The package contains only the host/core/parser files plus the local runtime\n  binary; the workers, publisher sites and test suites stay in this repo (they\n  are the deployed reference, not the library).\n\n### Local MCP runtime — no gateway at all\n\nThe package also ships a stdio MCP server that runs an origin's skills\n**locally**: it fetches `/llms.txt`, verifies every `tool_sha256`, loads each\nverified skill into its own QuickJS-wasm context, and speaks MCP over\nstdin/stdout — so a static site (e.g. a GitHub Pages *user* site) becomes an\nMCP server on your machine with zero deployed infrastructure on either side:\n\n```bash\nnpx -y @rckflr/mcpwasm https://usuario.github.io\n```\n\nMCP client configuration (Claude Code, Cursor, …):\n\n```json\n{\n  \"mcpServers\": {\n    \"misitio\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@rckflr/mcpwasm\", \"https://usuario.github.io\"]\n    }\n  }\n}\n```\n\nHonest limits (stated in `bin/mcpwasm-local.mjs`): discovery runs once per\nprocess (restart to refresh). Hash verification and the sandbox model\n(per-skill contexts, origin-scoped `fetchOrigin`, resource limits) are the\nsame as the gateway's. **Origin memory is supported**: if the origin declares\n`skills-memory`, the snapshot is fetched, verified against `snapshot_sha256`,\nand `host.memorySearch` is injected — same contract as the gateway; on any\nmismatch (or if the optional `@rckflr/minimemory` engine is missing) the\ncapability is simply absent and skills that call it fail closed in-sandbox.\nEach verified skill's `SKILL.md` recipe is served as an MCP resource and via\nthe `get_skill_guide` tool (see \"Skill recipes as MCP resources\"). Trust is\nyour choice of origin by default (no attestation required); Sigstore\nattestation verification is available opt-in via `--require-attestation`\n(below). Also cross-checks `tool_sha256` against\n`.well-known/agent-skills/index.json` when the origin publishes one (see\n\"Cross-checking against index.json\"). Tested by `npm run local` (hermetic,\nlocalhost-only; part of the CI gate — including verified-snapshot search and\nthe tampered-snapshot fail-closed path).\n\n#### Cross-checking against `index.json`\n\nBoth the local runtime and the gateway now also fetch\n`/.well-known/agent-skills/index.json` — the canonical metadata layer the core\nllms-txt-skills RFC defines (§8 Open Question 5: `llms.txt` is the zero-fetch\ndiscovery pointer, `index.json` is the metadata/verification source of truth).\nWhen a skill's name appears in both `llms.txt` and `index.json`, and the\nlatter declares a `tool_sha256`, it **must** match the one declared in\n`llms.txt`; a mismatch rejects the skill (drift/tampering signal) exactly like\na `tool_sha256` mismatch against the fetched `tool.js` itself. Absence of\n`index.json` (most origins today) changes nothing.\n\n#### Sigstore attestations: `--require-attestation` (local runtime only)\n\n> **Platform limitation, discovered during implementation:** the `sigstore`\n> npm package depends on `@sigstore/tuf` to cache Fulcio/Rekor's trusted root\n> via TUF, and that cache uses `node:fs` with no way to bypass it through the\n> public API. Cloudflare Workers has no filesystem, so Sigstore verification\n> **only runs in the local Node runtime** (`bin/mcpwasm-local.mjs`) — the\n> gateway's attestation model remains the pre-registered-key Ed25519 scheme\n> above, unaffected. This is the one capability asymmetry between the two\n> runtimes; see [Capability support by runtime](#capability-support-by-runtime).\n> The spec (v0.4 §2.4) now names this profile explicitly: Sigstore is the\n> RECOMMENDED default where verification can reach Sigstore's trust\n> infrastructure; pre-registered Ed25519 is the profile for platforms — like\n> Workers — where it cannot.\n\nThe Ed25519 model (above) requires pre-registering every reviewer's public\nkey — a real bottleneck (today, only the maintainer is registered). Sigstore\nverifies **any** OIDC identity (a GitHub Actions workflow, a Google/GitHub\nlogin) without pre-coordination; the runtime's trust decision is which\n*identity* to require, not which *key* to whitelist — closer to what core RFC\n§4.6 recommends for identity-bound provenance.\n\n```bash\nnpx -y @rckflr/mcpwasm https://usuario.github.io \\\n  --require-attestation \"https://token.actions.githubusercontent.com|https://github.com/OWNER/REPO/.github/workflows/release.yml@refs/heads/main\"\n```\n\nWhen set, discovery additionally fetches\n`/.well-known/agent-skills/attestations.json` and, for **every** skill,\nrequires a matching entry (`origin` + `skill` + `tool_sha256`) whose\n`sigstore_bundle` verifies against exactly that `issuer|identity` pair, within\nits `[signed_on, valid_until]` window. A skill without one — absent\nattestations.json, no matching entry, expired, or an invalid/mismatched bundle\n— is excluded, same treatment as a `tool_sha256` mismatch. This flag is\nfail-closed by design: it is opt-in, but once set, absence of a valid\nattestation is *not* tolerated (unlike the gateway's `advisory` mode).\n\nThe attestation object's signed payload is an\n[in-toto Statement v1](https://in-toto.io/Statement/v1) inside a DSSE\nenvelope, whose `predicate` must carry the same 5 fields as the Ed25519 model\n(`origin`, `skill`, `tool_sha256`, `signed_on`, `valid_until`) — verified to\nmatch the attestation's own top-level fields, so a validly-signed bundle for\nskill A cannot be relabeled as skill B's attestation without re-signing.\n`sigstore-attest.mjs` exports `verifySigstoreAttestation` (the verifier) and\n`buildSigstoreStatement` (the canonical payload shape a publisher signs with\n`sigstore attest` or an equivalent SDK call — this repo does not ship a\nsigning tool for it, since producing a real Sigstore signature needs a live\nOIDC flow, out of scope for a script run here).\n\nVerified against a **real, live, publicly fetched** Sigstore bundle (the SLSA\nprovenance attestation for the `sigstore@5.0.0` npm package's own publish,\n`https://registry.npmjs.org/-/npm/v1/attestations/sigstore@5.0.0`) — proving\nthe underlying Fulcio cert-chain + Rekor transparency-log verification\ngenuinely runs and succeeds, and that a schema-mismatched or wrong-identity\nbundle is correctly rejected. All 6 `--require-attestation` rejection paths\n(no attestations.json, empty array, no matching skill, malformed date,\nexpired, invalid/empty bundle) verified end-to-end against\n`bin/mcpwasm-local.mjs`. **Honest gap:** producing a *positive* fixture (a\nreal Sigstore signature over this repo's own canonical payload, verifying as\n`attested`) needs a live OIDC signing flow this environment cannot complete\nheadlessly — untested is the happy path specifically, not the security-critical\nrejection paths.\n\n#### Developing your own skills: `--serve <dir>`\n\nPointing the runtime at a raw GitHub URL does **not** work: `new URL(...).origin`\nkeeps only scheme+host+port, so `https://raw.githubusercontent.com/you/repo/main/`\ncollapses to `https://raw.githubusercontent.com` — the `you/repo/main` part (and\ntherefore `/llms.txt`) is gone. `--serve` is the practical alternative: it starts\nan internal static file server (bound to `127.0.0.1` only, never exposed to the\nnetwork) over a local directory — e.g. your own `git clone` of a skills repo —\nand uses that as the origin, combining \"serve this directory\" and \"connect to\nit\" into one command:\n\n```bash\nnpx -y @rckflr/mcpwasm --serve ./my-skills-repo\n# npx -y @rckflr/mcpwasm --serve ./my-skills-repo --port 4000   (fixed port, optional)\n```\n\nThis is meant for developing and testing your own skills locally before\npublishing them (to GitHub Pages or any other static host) — not for browsing\nsomeone else's GitHub repo directly. Path-traversal requests against the\ninternal file server are rejected (resolved and checked against the served\ndirectory's root); covered by `npm run local`.\n\n## Why\n\nMCP clients (Claude, Cursor, others) can call arbitrary tools. Running a\nthird-party tool's code directly in your backend means that code can read your\nsecrets, hit your DB, phone home, or loop forever. You either trust the author\nfully or you don't run the tool.\n\nmcpwasm removes the trust requirement for the *code*:\n\n- The tool runs in a separate QuickJS-wasm context with no host globals beyond\n  what the host predefines (`registerTool`, `host`). No `fetch`, no `process`,\n  no disk, no secrets.\n- The platform secret (e.g. a Stripe key) stays on the host side. The tool can\n  only ask the host to perform a *named* internal action via `host.callInternal`\n  (sync host) or a scoped `host.fetchOrigin` (async host). The host decides what\n  is allowed.\n- The tool's `tool.js` is content-addressed by SHA-256; the gateway refuses to\n  load it if the hash declared in `llms.txt` does not match the bytes it\n  fetched.\n- Resource limits bound what a malicious/buggy tool can do: memory cap, stack\n  cap, a deterministic gas budget (interrupt-handler invocation count), and a\n  wall-clock fetch deadline per call.\n\n## Static MCP vs. a traditional MCP server\n\nIn mcpwasm, *publishing* (static files + a hash) and *execution* (a runtime\nthat discovers, verifies, and sandboxes on demand) are two separate things. In\na traditional MCP server they are the same thing — the server you deploy *is*\nthe execution, with no isolation layer in between.\n\n| | Static MCP — local (`npx @rckflr/mcpwasm`) | Static MCP — gateway | Traditional MCP server |\n|---|---|---|---|\n| What the publisher ships | Static files: `llms.txt` + `tool.js` (+ `SKILL.md`) | Same static files | A running server process (any language) |\n| Infrastructure the publisher operates | None — GitHub Pages, R2, any static host | None — same | The whole server: uptime, scaling, patching, secrets |\n| Where the tool code runs | Your own machine, in a QuickJS-wasm sandbox | The gateway Worker, in the same sandbox | The publisher's own process, natively, no isolation |\n| Integrity guarantee | SHA-256 verified before any byte executes | Same | None built in — you trust the deployed binary/image as-is |\n| Third trust ring (human review) | Not enforced (v1 limit — trust is your choice of origin) | Ed25519 attestations, `enforcing` mode in production | No standard mechanism |\n| Transport | stdio (JSON-RPC over stdin/stdout) | HTTP POST (JSON-RPC); needs the gateway URL + a token | Either — but fixed per implementation |\n| Network hops for the MCP call itself | Zero (local process); the tool can still call out via `fetchOrigin` | Two: client → gateway → publisher origin | Zero for local stdio, one for remote HTTP |\n| Measured overhead (this repo's own benchmarks) | Not separately benchmarked — same sandbox cost, no gateway hop | ~2 ms sandbox warm, ~6 ms for the full gateway vs. a direct API call ([BENCHMARK.md](./BENCHMARK.md)) | N/A — no sandbox tax, but no isolation either |\n| Discovery freshness | Once per process start (restart to refresh) | Cached 60 s (two layers + cron preheat) | Whatever the server implements |\n| Multi-client / shared access | No — one local process per user | Yes — one gateway serves any number of MCP clients | Yes, if built as a shared server |\n| Auth | None (you chose to run it) | Optional: shared token or per-client tokens + rate limiting | Whatever the publisher builds |\n| Best fit | Developing/testing your own skills locally; zero-trust execution of someone else's skills without running a server | Teams wanting one shared endpoint serving many static publishers, with signed review as policy | Stateful logic, database connections, capabilities that genuinely need no sandbox constraint |\n\nThe takeaway that doesn't fit in a table: a traditional MCP server answers \"how\ndo I expose this logic as a tool?\" Static MCP additionally answers \"how do I\nrun code from an origin I don't fully trust?\" If you write and control the\nserver yourself, traditional is simpler and none of this is necessary. Static\nMCP matters when the tool code comes from *someone else*, and you want a\nverifiable guarantee (hash + sandbox, optionally review) before running it —\nthat is the problem this repo exists to solve, not a general-purpose\nalternative to MCP.\n\n### If you already have an API\n\nYou do not need to build or maintain an MCP protocol server — that is the\nwhole point. The runtime (local or gateway) already handles JSON-RPC,\n`tools/list`, `tools/call`, and the transport; none of that is your code.\n\nWhat you still have to write is **not** prose. A `tool.js` per action is real,\nsmall glue code: it validates `args` against the schema you declared, calls\nyour existing API through `host.fetchOrigin`, and shapes the response — see\n`bookstore/content/create_order.tool.js` in this repo for a concrete example\n(validates `qty` and `book_id`, handles a 409 for insufficient stock as a\ndistinct case, never lets a malformed call reach your backend). This is a\ndifferent, stronger mechanism than a `SKILL.md` with no `tool.js`: prose-only\nskills are the core RFC's basic mode — an agent reads them and *improvises*\nthe HTTP call with whatever generic request tool it has, with no schema\nvalidation, no sandbox, and no hash pinning. That is the \"execution gap\" that\nexecutable skills (this repo's reference feature) close. Handing an agent a\nraw \"make any HTTP request\" capability against your API reintroduces the\nproblem this project exists to avoid — your backend ends up validating\nagainst an arbitrary caller either way; a `tool.js` does that validation\nbefore your API is ever hit, and the agent only ever gets the specific,\nparameterized actions you defined.\n\n\"Zero infrastructure\" is literal for internal use — your own team pointing\n`npx @rckflr/mcpwasm` (or `--serve`) at your published skills needs no server\non either side. For external clients to reach you without installing\nanything, you need one endpoint answering MCP over HTTP; that means either\nyour origin gets added to an existing deployed gateway's `ALLOWED_ORIGINS`,\nor you `wrangler deploy` your own instance of the same generic gateway code in\nthis repo, configured for your origin. Either way it is a one-time,\ntool-agnostic deploy — not a bespoke MCP server built per API.\n\n## Architecture\n\n```\n                         (1) publish llms.txt + tool.js + SKILL.md\n   Publisher site  ───────────────────────────────────────────┐\n   (static: R2/Pages/                                          │\n    any host serving /llms.txt)                                │\n                                                               ▼\n                                                        ┌───────────────┐\n   MCP client  ──POST /mcp?origin=<pub>──►  Gateway Worker │ discovers     │\n   (Claude,                                            │ llms.txt,     │\n    Cursor, ...)                                        │ verifies     │\n        ▲                                               │ sha256 per    │\n        │  (5) JSON-RPC response                         │ skill,        │\n        └──────────────────────────────────────────────  │ loads tools   │\n                                                        │ in sandbox    │\n                                                        └──────┬────────┘\n                                                               │ (2) per request:\n                                                               │     new QuickJS\n                                                               │     context,\n                                                               │     origin-scoped\n                                                               ▼\n                                                        ┌───────────────┐\n                                                        │ AsyncToolHost │  (3) tool code\n                                                        │ (QuickJS-wasm │      calls\n                                                        │  asyncify)    │      host.fetchOrigin\n                                                        │               │────► (4) host fetches\n                                                        │  mem/stack/   │      ONLY the allowed\n                                                        │  interrupt    │      origin, returns\n                                                        │  limits set   │      {status,body}\n                                                        └───────────────┘\n```\n\nFlow:\n\n1. A publisher site ships `/llms.txt` plus per-skill `tool.js` and `SKILL.md`\n   files. `llms.txt` lists each executable skill with a SHA-256 of its\n   `tool.js`, and may declare an *origin memory* snapshot (see below).\n2. On each request the gateway downloads `llms.txt`, parses the executable\n   skills, downloads each `tool.js`, verifies SHA-256, and loads the verified\n   ones into a fresh `AsyncToolHost` scoped to that origin.\n3. Tool code runs inside QuickJS-wasm. It can only call\n   `host.fetchOrigin(path, opts?)` (`opts`: `{method: \"GET\"|\"POST\",\n   body?: string ≤16 KB, contentType?}` — write skills go through POST),\n   which is async from the host side but synchronous-looking inside the sandbox\n   (QuickJS asyncify suspends/resumes the wasm stack). An origin that declares\n   a verified memory snapshot additionally gets `host.memorySearch(query, k?)`.\n4. The host fetches only the allowed origin; any other origin throws inside the\n   sandbox.\n5. The gateway maps MCP `tools/list` and `tools/call` over JSON-RPC 2.0 and\n   returns the result to the client.\n\nPieces live in:\n\n- `host.mjs` — synchronous `ToolHost` (sync tools, `host.callInternal` capability).\n- `host-async.mjs` — `AsyncToolHost` (async handlers, `host.fetchOrigin` + the\n  `extraCapabilities` mechanism that backs `host.memorySearch`, resource hardening).\n- `mcp-core.mjs` / `mcp-core-async.mjs` — JSON-RPC 2.0 MCP core (transport-agnostic).\n- `worker.mjs` — PoC MCP server (sync host, inline tools).\n- `worker-spike.mjs` — async spike (fetchHome/fetchEvil).\n- `worker-gateway.mjs` + `llmstxt-parse.mjs` — the gateway.\n- `worker-memspike.mjs` — memory spike: the docs-site origin published and\n  served through the gateway end-to-end (`host.memorySearch` over a BM25\n  snapshot), exercised by `mf-memspike.mjs`.\n\n## The executable-skill line in `llms.txt`\n\n> **Status: Draft v0.5, adopted.** This format is specified by the\n> [Executable Skills extension](https://github.com/MauricioPerera/llms-txt-skills/blob/master/docs/ext-executable-skills.md)\n> of the [llms-txt-skills](https://github.com/MauricioPerera/llms-txt-skills)\n> standard. This repo is its reference implementation; the spec and this code\n> are kept aligned (every MUST in the spec is field-tested here).\n\nUnder a `## Skills` section, an executable skill is a normal markdown list item\nfollowed by an HTML comment carrying a JSON object with `version`, `tool` (path\nto the `tool.js`), and `tool_sha256` (hex SHA-256 of the `tool.js` bytes):\n\n```\n- [sum_numbers](/skills/sum_numbers/SKILL.md): Sum two numbers a and b. <!-- skill: {\"version\":\"1.0.0\",\"tool\":\"/skills/sum_numbers/tool.js\",\"tool_sha256\":\"58daf86111bf7278446eb7e0e8c6384713b50cdb6fa97ac039e23846d723dc3e\"} -->\n```\n\nParsed by `llmstxt-parse.mjs`:\n\n- The `<!-- skill: {...} -->` comment marks the line as an *executable* skill.\n  List items without it are treated as descriptive-only and ignored by the\n  gateway.\n- `tool` is resolved relative to the origin.\n- `tool_sha256` is verified against the fetched `tool.js` bytes before the tool is\n  loaded. Mismatch → the skill is rejected (logged) and not registered.\n- If the JSON is invalid the line is silently skipped (no throw).\n\nA `tool.js` registers itself:\n\n```js\nregisterTool({\n  name: \"sum_numbers\",\n  description: \"Sum two numbers a and b.\",\n  inputSchema: { type: \"object\", properties: { a: { type: \"number\" }, b: { type: \"number\" } }, required: [\"a\", \"b\"] },\n  handler(args) { return Number(args.a) + Number(args.b); }\n});\n```\n\n## Origin memory (search over static content)\n\n> Spec: *origin memory* in\n> [Executable Skills v0.5](https://github.com/MauricioPerera/llms-txt-skills/blob/master/docs/ext-executable-skills.md).\n\n> **Both runtimes.** Origin memory runs in the **gateway**\n> (`worker-gateway.mjs`) and in the **local runtime** (`bin/mcpwasm-local.mjs`),\n> with the same contract: snapshot fetched, verified against `snapshot_sha256`,\n> and only then is `host.memorySearch` injected. In the local runtime the BM25\n> engine (`@rckflr/minimemory`) is an **optionalDependency** — installed by\n> default with `npx`/`npm install`; if it is missing, the runtime says so on\n> stderr and degrades to \"no memory\" (capability absent, skills that call it\n> fail closed in-sandbox — spec-conformant, not a crash).\n\nA publisher that wants its skills to search over its own static content (docs,\ncatalog text, any corpus) declares a memory snapshot with a single HTML comment\n**before** the `## Skills` section (this ordering is required by the reference\nparser):\n\n```\n<!-- skills-memory: {\"snapshot\":\"/skills-index.snapshot\",\"snapshot_sha256\":\"a0235f071aa7e28f2096312f22f1ad035901595f3fa91d2cc92b5879bbb7f6d5\",\"format\":\"minimemory-okf-v1\"} -->\n\n## Skills\n...\n```\n\n- `snapshot` is a path (relative to the origin) to a BM25 snapshot in the\n  `minimemory-okf-v1` format (built by the [`@rckflr/minimemory`](https://www.npmjs.com/package/@rckflr/minimemory)\n  engine; the wasm binary ships as `minimemory_bg.wasm`).\n- `snapshot_sha256` is the hex SHA-256 of the snapshot bytes. Both runtimes\n  download the snapshot and **verify it against this hash before injecting\n  the capability** — same content-addressing rule as `tool.js`. On mismatch,\n  fetch failure, non-200, or an unsupported `format`, the snapshot is discarded\n  and **the capability is not injected**.\n- When the snapshot verifies, the runtime injects\n  `host.memorySearch(query, k?)` into every skill of that origin (via the\n  `extraCapabilities` bridge in `AsyncToolHost`, same raw-JSON asyncify pattern\n  as `host.fetchOrigin`). `k` defaults to 5 and is clamped to `[1, 10]`. It\n  returns `{ hits: [{ text, score, title, concept_id }] }` (or `{ error }`).\n- Without a verified snapshot the capability is absent: a skill that calls\n  `host.memorySearch` sees `undefined` and throws **inside the sandbox**,\n  surfacing as `isError: true` (controlled failure, not a runtime crash). The\n  skills still list — only the memory capability is missing.\n\nThe reference publisher is the docs-site (see \"Repository layout\"): it serves\nthe spec snapshot and a `search_spec` skill that runs\n`host.memorySearch(args.q, k)` to do BM25 search over the four llms-txt-skills\ndocuments, plus `get_doc` and `list_docs`.\n\n### Capability support by runtime\n\nDiscovery, `tool_sha256` content-addressing, sandboxed `tool.js` execution,\norigin memory (`host.memorySearch`), skill recipes as MCP resources, and\nEd25519 attestations work on **both** runtimes. One capability remains asymmetric, for a declared platform reason,\nnot a bug:\n\n| Capability | Gateway (`worker-gateway.mjs`, Workers) | Local runtime (`bin/mcpwasm-local.mjs`, Node) |\n| :--- | :---: | :---: |\n| Origin memory — `host.memorySearch` | ✅ full | ✅ full (engine is an optionalDependency; if missing, degrades to capability-absent) |\n| Sigstore (keyless) attestations | ❌ Workers has no `node:fs` for `@sigstore/tuf`'s trust-root cache | ✅ `--require-attestation` |\n\nConsequence: the **local runtime is the full-featured reference** — memory and\nSigstore both work there. The gateway matches it except for Sigstore\nverification, where its attestation model remains Ed25519-only (platform\nlimitation of Workers, documented in\n[Sigstore](#sigstore-attestations---require-attestation-local-runtime-only)\nabove).\n\n## Scopes — multiple projects on one origin\n\n> Spec: §2.5 of\n> [Executable Skills v0.5](https://github.com/MauricioPerera/llms-txt-skills/blob/master/docs/ext-executable-skills.md)\n> (resolves RFC v0.10 Open Question 6). Both runtimes, since 0.6.0.\n\nOne origin (e.g. a GitHub Pages root site) can aggregate skills from several\nprojects. Without namespacing, two projects that both publish a\n`search_knowledge` tool collide, and only one `skills-memory` line can exist\nper origin. Scopes fix both:\n\n```\n<!-- skills-memory: {\"snapshot\":\"/KDD/skills-index.snapshot\",\"snapshot_sha256\":\"…\",\"format\":\"minimemory-okf-v1\",\"scope\":\"kdd\"} -->\n\n## Skills\n\n- [search_knowledge](/KDD/skills/search_knowledge/SKILL.md): … <!-- skill: {\"version\":\"1.0.0\",\"tool\":\"/KDD/skills/search_knowledge/tool.js\",\"tool_sha256\":\"…\",\"scope\":\"kdd\"} -->\n```\n\n- `scope` (optional, pattern `^[a-z][a-z0-9_-]*$`) declares the project\n  namespace of a skill line. The runtime exposes the tool under the public\n  name **`<scope>__<toolName>`** (`kdd__search_knowledge`); the rename happens\n  at the **host boundary only** — the published `tool.js` bytes, its\n  `tool_sha256`, and any attestations are untouched. This preserves the\n  universal-template property: the same `tool.js` can be served by every\n  publisher under different scopes with one ecosystem-wide hash.\n- One `skills-memory` line **per scope** (at most one without `scope`). Each\n  skill gets `host.memorySearch` bound to **its own scope's** verified\n  snapshot — memories are isolated per project.\n- An invalid `scope` value makes the line non-executable (reported, not\n  loaded). A public-name collision (two lines mapping to the same public\n  name) keeps the first and skips the rest with a diagnostic.\n- Skill recipes follow the public name: `skill://kdd__search_knowledge`.\n- Fully backward compatible: no `scope` ⇒ exactly the pre-0.6.0 behavior.\n\n## Skill recipes (SKILL.md) as MCP resources\n\n> Both runtimes. An executable skill has two halves: the **recipe**\n> (`SKILL.md` — when/how to use it, sequencing, constraints; the \"recipe\n> layer\" the core RFC §3.3 defines) and the **capability** (`tool.js`).\n> Serving only the tools loses the recipe — the agent gets the hammer without\n> the manual.\n\nDiscovery also fetches each verified skill's `SKILL.md` and verifies it\nagainst the `sha256` declared in the same `llms.txt` line (core RFC field).\nVerified recipes are exposed two ways:\n\n- **MCP resources** — `resources/list` / `resources/read`, uri\n  `skill://<name>`, `text/markdown`; the capability is advertised in\n  `initialize`. The semantically correct path for clients that support it.\n- **`get_skill_guide` tool** — a synthetic, runtime-provided (not sandboxed)\n  tool returning the recipe by skill name. Universal fallback: every MCP\n  client supports tools.\n\nFailure semantics mirror everything else here, but the halves are\n**independent**: a missing/unfetchable/hash-mismatched `SKILL.md` omits the\n*recipe* (warned on stderr / console) while the *tool* — verified by its own\n`tool_sha256` — loads unaffected. Under `enforcing` attestation mode, an\nexcluded skill's recipe is excluded with it. Size cap: `MAX_SKILLMD_BYTES`\n(default 256 KB). Covered by `npm run local` (verified recipe served, tampered\nrecipe excluded, missing recipe tolerated).\n\n## Skill attestations (advisory)\n\n> Spec: [Skill Attestations v0.4](https://github.com/MauricioPerera/llms-txt-skills/blob/master/docs/ext-skill-attestations.md).\n\nThis section describes the **gateway's** model: Ed25519 signatures from a\nruntime-side pre-registered `REVIEWERS` key registry. The **local runtime**\nadditionally supports **Sigstore (keyless)** attestations via\n`--require-attestation` — no pre-registered key, any OIDC identity the runtime\nexplicitly trusts — see \"Sigstore attestations\" above; that section closes\nthe \"only one registered reviewer scales\" bottleneck this one has.\n\nA publisher may serve a third trust ring — signed reviewer attestations — at\n`/.well-known/agent-skills/attestations.json`. Each entry is an Ed25519\nsignature over the canonical payload and has this shape:\n\n```json\n{\n  \"origin\": \"https://llmstxt-docs.rckflr.workers.dev\",\n  \"skill\": \"search_spec\",\n  \"tool_sha256\": \"95301993...\",\n  \"attester\": \"human:mauricio\",\n  \"signed_on\": \"2026-07-02\",\n  \"valid_until\": \"2027-07-02\",\n  \"signature\": \"<base64 Ed25519 signature>\"\n}\n```\n\nThe signed payload is the UTF-8 bytes of\n`origin + \"\\n\" + skill + \"\\n\" + tool_sha256 + \"\\n\" + signed_on + \"\\n\" + valid_until`\nwith `origin` canonical (lowercase, no trailing slash, no default port) and\n`tool_sha256` lowercase hex. The gateway:\n\n- Fetches `attestations.json` during discovery (only when attestations are\n  not `off`). A 404 or malformed array means \"no attestations\" (every skill is\n  `unattested`), not a discovery error.\n- Verifies each signature with WebCrypto (`Ed25519` via `crypto.subtle`, public\n  key imported raw) against the runtime-side reviewer registry `REVIEWERS`\n  (a `REVIEWERS` var in `wrangler-gateway.toml` mapping\n  `attester → { public_key: <base64 raw 32 bytes>, registered_at }`). An\n  attester not in the registry is ignored; a registered attester whose\n  signature fails marks the skill `invalid`.\n- Computes a per-skill verdict with precedence **invalid > attested > expired >\n  unattested** (`invalid` dominates): a matching attestation from a registered\n  reviewer with a valid signature inside its `[signed_on, valid_until]` window\n  is `attested`; valid signature outside the window is `expired`; no matching\n  attestation is `unattested`.\n- Exposes the verdicts two ways: a tag appended to each tool's `description`\n  in `tools/list`, and a summary header `X-Gw-Attestations`\n  (`attested=N,expired=N,invalid=N,unattested=N`) on every response.\n\nThree modes via `ATTESTATION_MODE`:\n\n- `off` (default when `ATTESTATION_MODE` is unset) — attestations are not\n  fetched; behavior is the pre-T25 gateway.\n- `advisory` — everything loads; verdicts are visible but do not exclude.\n- `enforcing` (deployed since T45) — only `attested` skills load;\n  non-`attested` skills are excluded exactly like a `tool_sha256` mismatch\n  (logged, not registered).\n\n`scripts/attest.mjs` is the signing tool (Node `node:crypto` Ed25519, no deps):\n\n- `node scripts/attest.mjs keygen` — generates an Ed25519 pair, writes the\n  private key to `.attester-key.json` and prints **only the public key** (base64\n  raw 32 bytes) for the `REVIEWERS` registry.\n- `node scripts/attest.mjs sign <origin> <skill> <valid_until>` — fetches the\n  origin's live `llms.txt`, reads the real `tool_sha256` for the skill, signs,\n  and prints the attestation object JSON.\n\nThe private key lives in `.attester-key.json` and is **local and gitignored** —\nnever commit it, and it is never printed by the tool. No key material belongs\nin this repo or in `REVIEWERS` (only public keys).\n\nThird-party publishers (sites you do not control): see [`ONBOARDING.md`](./ONBOARDING.md)\nfor the eligibility, review, attestation, activation, and revocation process.\n\n## Quick start\n\nRequirements: Node 18+ and `npm install` (already done in this checkout).\n\n```bash\nnpm install\nnpm test      # build + e2e Miniflare for the sync PoC (worker.mjs)\nnpm run spike # build + e2e Miniflare for the async spike (worker-spike.mjs)\nnpm run gateway # build + e2e Miniflare for the gateway (worker-gateway.mjs) — hits the live demo site\nnpm run memspike # build the memory snapshot + memspike worker, then e2e Miniflare against the docs-site origin (host.memorySearch / BM25)\n```\n\n`npm run gateway` is documented as-is from `package.json`; it builds the gateway\nworker and runs `mf-gateway.mjs` against the real deployed demo site. `npm run\nmemspike` does the same for the memory capability: `build-memsnapshot.mjs` →\n`build-memspike.mjs` → `mf-memspike.mjs`.\n\n### Try the deployed gateway (curl)\n\nThe gateway is live at `https://llmstxt-gateway.rckflr.workers.dev`. It is\nrestricted to origins in its allowlist; the demo site\n`https://llmstxt-demo-site.rckflr.workers.dev`, the bookstore\n`https://llmstxt-bookstore.rckflr.workers.dev` (D1-backed, includes a write\nskill `create_order`), and the docs-site\n`https://llmstxt-docs.rckflr.workers.dev` (origin memory / BM25) are allowed.\n`origin` is URL-encoded as a query param. **The deployed gateway has auth\nenabled:** every request below needs `-H \"Authorization: Bearer <AUTH_TOKEN>\"`\n(the `AUTH_TOKEN` secret; 401 otherwise). The token is a secret — it is not in\nthis repo. The deployed gateway can also run in **per-client mode** (the\n`CLIENTS` secret), in which case each client sends its own\n`Authorization: Bearer <client_token>` with the same curl syntax; the response\nthen carries `X-Gw-Client: <client_id>`.\n\nList the skills the demo site publishes:\n\n```bash\ncurl -s -X POST \\\n  \"https://llmstxt-gateway.rckflr.workers.dev/mcp?origin=https%3A%2F%2Fllmstxt-demo-site.rckflr.workers.dev\" \\\n  -H \"Authorization: Bearer <AUTH_TOKEN>\" \\\n  -H \"content-type: application/json\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\"}'\n```\n\nCall `sum_numbers` (pure sync tool, runs in the sandbox):\n\n```bash\ncurl -s -X POST \\\n  \"https://llmstxt-gateway.rckflr.workers.dev/mcp?origin=https%3A%2F%2Fllmstxt-demo-site.rckflr.workers.dev\" \\\n  -H \"Authorization: Bearer <AUTH_TOKEN>\" \\\n  -H \"content-type: application/json\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":2,\"method\":\"tools/call\",\"params\":{\"name\":\"sum_numbers\",\"arguments\":{\"a\":2,\"b\":40}}}'\n```\n\nCall `server_time` (async tool that calls `host.fetchOrigin(\"/api/time\")` on the\nallowed origin):\n\n```bash\ncurl -s -X POST \\\n  \"https://llmstxt-gateway.rckflr.workers.dev/mcp?origin=https%3A%2F%2Fllmstxt-demo-site.rckflr.workers.dev\" \\\n  -H \"Authorization: Bearer <AUTH_TOKEN>\" \\\n  -H \"content-type: application/json\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":3,\"method\":\"tools/call\",\"params\":{\"name\":\"server_time\",\"arguments\":{}}}'\n```\n\nThe other deployed workers (their root path returns 404 by design — only\nspecific routes like `/llms.txt` are served):\n\n- PoC sync host: `https://toolhost-mcp.rckflr.workers.dev` (POST `/mcp`).\n- Demo publisher: `https://llmstxt-demo-site.rckflr.workers.dev/llms.txt`.\n- Bookstore publisher: `https://llmstxt-bookstore.rckflr.workers.dev/llms.txt`.\n- Docs publisher (origin memory): `https://llmstxt-docs.rckflr.workers.dev/llms.txt`.\n\n## Security model (honest)\n\nWhat it guarantees:\n\n- **Tool-host isolation.** Tool code runs in a QuickJS-wasm context separate\n  from the Worker's JS. It sees only `registerTool`, `host`, and what the host\n  prelude defines. No `fetch`, no `process`, no globals leak by default.\n- **Secrets stay outside the sandbox.** In the sync PoC, the platform secret is\n  read from `env.STRIPE_SECRET` on the host side and is never exposed to tool\n  code — the tool can only call named internal methods. In the gateway, there\n  is no platform secret; the only capabilities are `host.fetchOrigin` and (when\n  declared and verified) `host.memorySearch`.\n- **SHA-256 content addressing.** The gateway downloads `tool.js` and verifies\n  it against the `tool_sha256` declared in `llms.txt` before loading. Mismatched or\n  corrupt content is rejected and not cached. The same rule applies to the\n  origin-memory snapshot (`snapshot_sha256`): unverified → capability not injected.\n- **Skill attestations (third trust ring, spec `ext-skill-attestations` v0.4).**\n  See the dedicated section. Publishers may serve signed reviewer attestations;\n  the gateway verifies them via WebCrypto against the runtime-side `REVIEWERS`\n  registry and exposes per-skill verdicts (`attested`/`expired`/`invalid`/\n  `unattested`, `invalid` dominates) in each tool description and the\n  `X-Gw-Attestations` header. Modes: `off` (default when unset) / `advisory`\n  (everything loads, verdicts visible) / `enforcing` (only `attested` skills\n  load; deployed mode since T45). `scripts/attest.mjs` is the signing tool\n  (keygen + sign).\n- **Origin-scoped fetch.** `host.fetchOrigin` only fetches the single allowed\n  origin for the request. Any other origin throws *inside the sandbox* and is\n  surfaced as `isError: true`, not a JSON-RPC error.\n- **Resource limits (defaults in `host-async.mjs`, applied per request):**\n  - memory: 64 MB (`setMemoryLimit`)\n  - stack: 1 MB (`setMaxStackSize`)\n  - deterministic gas: 20 000 interrupt-handler invocations per\n    `callTool` / `loadToolSource` (`setInterruptHandler` with an invocation\n    counter). This is the primary cutoff, because `Date.now()` **freezes** in\n    Cloudflare Workers during synchronous execution (Spectre mitigation), so a\n    pure `while(true){}` never advances the clock. The gas counter does not\n    depend on the clock — it counts how many times QuickJS invoked the handler\n    (calibrated ~100× over the heaviest legitimate skill; see TAREA12B).\n  - wall-clock interrupt deadline: 2000 ms per `callTool` / `loadToolSource`\n    (a cheap backstop where the clock does advance — Node/tests).\n  - outbound fetch deadline: 10 s per `host.fetchOrigin`\n    (`AbortSignal.timeout` + a `Promise.race` backstop that fires even if the\n    fetch impl ignores the signal; on firing it throws \"fetchOrigin timeout\"\n    inside the sandbox → `isError: true`, not a gateway crash).\n- **Fresh context per request.** A new QuickJS context (and runtime) is built\n  per request and disposed at the end; no state survives between requests.\n- **Per-skill contexts in the gateway.** Each skill is loaded into its own\n  QuickJS context; a skill cannot see or overwrite another skill's\n  registration or globals, even within the same origin.\n- **Concurrency safety.** The gateway keeps a pool of up to N independent\n  instances of the asyncify wasm module per isolate (`WASM_POOL_SIZE`, default\n  4, clamped to [1, 8]; the compiled `WebAssembly.Module` is shared, each\n  instance has its own memory). Each request acquires one instance exclusively,\n  so up to N requests run truly in parallel per isolate and the (N+1)-th waits\n  by polling in its own request context (workerd cancels continuations of\n  promises resolved from another request context, so a FIFO handoff is not\n  viable) — with N=1 this degenerates to the previous per-module mutex (TAREA19).\n  Discovery is single-flighted per origin so concurrent cold requests share one\n  discovery pass.\n\nWhat it does **not** guarantee:\n\n- **Auth has three modes, selected by config.** Precedence is per-client\n  → legacy shared token → dev open.\n  - *Per-client (`CLIENTS` secret, opt-in).* `CLIENTS` is a JSON secret mapping\n    `sha256_hex_of_token → { client_id, rpm? }`. Tokens never appear in\n    cleartext in config — the key is the lowercase hex SHA-256 of the token's\n    UTF-8 bytes. On `POST /mcp` the gateway hashes the `Authorization: Bearer\n    <token>` value and does an exact lookup on that hash; the lookup *is* the\n    timing-safe mechanism (a fixed digest is compared, never the cleartext\n    token against a secret). A known token passes and the response carries\n    `X-Gw-Client: <client_id>`; an unknown token, missing header, or malformed\n    header yields `401`. `AUTH_TOKEN` is ignored in this mode. If `CLIENTS` is\n    set but its JSON is invalid, the gateway **fail-closes** — every `POST /mcp`\n    returns `401` rather than opening by config error (signalled on `GET /`).\n  - *Legacy shared token (`AUTH_TOKEN` secret).* If `CLIENTS` is unset, the\n    `AUTH_TOKEN` secret enables a single shared bearer token (constant-time\n    comparison); the deployed gateway has it enabled. Without it the gateway\n    runs open (dev mode). The PoC worker remains open.\n  - *Per-client rate limiting (opt-in, requires per-client mode).* When\n    per-client mode is active, the client's `rpm` is a non-null number, and the\n    `RATE_LIMITER` Durable Object binding is present, each `POST /mcp` is\n    counted against a **fixed window** of 60 s persisted in the DO's\n    SQLite-backed storage (one DO instance per `client_id`, keyed by name).\n    Within quota, responses carry `X-Gw-RateLimit-Limit` / `-Remaining` /\n    `-Reset`; `Remaining` counts **including the current request** (the\n    admitted sequence shows `rpm, rpm-1, … 1`). Over quota the gateway returns\n    `429` with `Retry-After` and `Remaining: 0`. Honest edges: a fixed window\n    allows a burst of up to `2 × rpm` straddling a window boundary (a client\n    can spend a full window's quota at its tail and the next window's at its\n    head), and the limiter is per-request, not per-cost — it caps call count,\n    not payload size, CPU, or complexity. If the DO itself fails while the\n    limiter is active, the gateway **fail-closes** with an observable\n    `500 rate_limiter_unavailable` rather than letting the request through\n    uncounted. Without the binding (or with no `rpm`), the limiter stays\n    inactive and the request path is byte-identical to the prior behavior.\n- **Per-skill isolation is context-level, not process-level.** Skills get\n  separate QuickJS contexts but share the same wasm module instance and the\n  same Worker request; the boundary is the QuickJS API surface, not an OS\n  process.\n- **One asyncify suspension at a time — per module instance.** QuickJS asyncify\n  suspends/resumes a single stack per wasm instance; within one request all\n  execution is sequential on its instance. Cross-request parallelism comes from\n  the instance pool (up to `WASM_POOL_SIZE` concurrent requests per isolate),\n  not from overlapping suspensions on one instance.\n- **State is in-memory and per-request.** No persistence, no warm state between\n  requests. A tool that accumulates state loses it when the request ends.\n- **DoS is bounded, not impossible.** The limits above cap a single call's\n  cost; a determined caller can still spend the limits' worth of CPU/memory\n  per request. Discovery is cached in two layers: layer 1 caches the parsed\n  result per isolate for 60 s, and layer 2 caches the full post-verification\n  result in the Cache API per colo for 60 s (observable via the\n  `X-Gw-Discovery: hit|l2|miss` response header — `hit` served from layer 1,\n  `l2` hydrated cross-isolate from layer 2, `miss` fetched from the origin).\n  The layer 2 key carries a config fingerprint (attestation mode + reviewer\n  registry + UTC date), so changing the config never serves stale verdicts.\n  What is cached is post-verification (the `tool.js` bytes were already\n  hash-checked when layer 2 was populated), inside the account's own trust\n  domain; the cold path is amortized more, but still not zero. A scheduled\n  preheat (cron every minute, `[triggers]` in `wrangler-gateway.toml`) runs\n  discovery for every allowlisted origin and instantiates a wasm module, so\n  the cron's isolate/colo rarely serves a cold miss — honest caveat: the\n  Cache API is per-colo and the cron runs in one location, so other colos\n  still pay their first miss.\n- **The publisher is trusted for the skill list.** The gateway trusts the\n  origin's `/llms.txt` to name skills; it verifies the `tool.js` bytes match\n  the declared SHA-256, but it does not vet what the tool does.\n\n## Repository layout\n\n| File / dir | Purpose |\n|---|---|\n| `CHANGELOG.md` | Per-release changes of the published npm package (verified against the actual tarballs). |\n| `host.mjs` | Synchronous `ToolHost`: loads `tool.js` into QuickJS-wasm, injects the `host.callInternal` capability. |\n| `host-async.mjs` | `AsyncToolHost`: asyncify variant, async handlers, `host.fetchOrigin` capability, the `extraCapabilities` bridge (`host.memorySearch`), mem/stack/gas hardening. |\n| `mcp-core.mjs` | Sync MCP JSON-RPC 2.0 core (`initialize`, `tools/list`, `tools/call`, `ping`). Transport-agnostic. |\n| `mcp-core-async.mjs` | Async MCP core; awaits `AsyncToolHost.callTool`. |\n| `worker.mjs` | PoC MCP server (sync host, inline tools) deployed at `toolhost-mcp.rckflr.workers.dev`. |\n| `worker-spike.mjs` | Async spike (fetch_home/fetch_evil) proving origin-scoped fetch. |\n| `worker-gateway.mjs` | The gateway: discover → verify → load → serve MCP, + origin-memory injection and attestations. Deployed at `llmstxt-gateway.rckflr.workers.dev`. |\n| `llmstxt-parse.mjs` | Pure parser for the executable-skill lines (and the `skills-memory` line) of `llms.txt`. Also reports prose-only (`nonExecutable`) skills found in `## Skills`. |\n| `sigstore-attest.mjs` | `verifySigstoreAttestation` / `buildSigstoreStatement` — Sigstore (keyless) attestation verification. Node-only (see \"Sigstore attestations\" above); used by `bin/mcpwasm-local.mjs`'s `--require-attestation`, not the gateway. |\n| `worker-memspike.mjs` | Memory spike: docs-site origin served through the gateway with `host.memorySearch` over a BM25 snapshot. |\n| `internal-logic.mjs` | Demo platform logic for the sync PoC (holds the secret, exposes `createPayment`/`refundPayment`). |\n| `tools-inline.mjs` | Inline `tool.js` sources for the sync PoC. |\n| `shim.mjs` | `location`/`self` shim needed by the quickjs-emscripten wasm loader in Workers. |\n| `build.mjs` / `build-spike.mjs` / `build-gateway.mjs` | esbuild bundlers (conditions `workerd`, external `*.wasm`) for the PoC, spike, and gateway workers. |\n| `build-memspike.mjs` / `build-memsnapshot.mjs` | esbuild bundler for the memspike worker, and the snapshot builder for the docs-site BM25 snapshot. |\n| `mf-test.mjs` / `mf-spike.mjs` / `mf-gateway.mjs` / `mf-memspike.mjs` | e2e tests with Miniflare v4 against the built workers (PoC, spike, gateway, memspike). |\n| `wrangler.toml` | Wrangler config for the PoC (sync) worker. |\n| `wrangler-gateway.toml` | Wrangler config for the gateway. Vars: `ALLOWED_ORIGINS` (origin allowlist), `REVIEWERS` (attestation reviewer registry, JSON), `ATTESTATION_MODE` (`off`/`advisory`/`enforcing`). Service bindings `DEMO`, `BOOKSTORE`, `DOCS` (same-account worker-to-worker fetch, bypassing Cloudflare error 1042). `AUTH_TOKEN` and `CLIENTS` are set as secrets, not in this file. Durable Object binding `RATE_LIMITER` (class `RateLimiter`, migration `v1` with `new_sqlite_classes`) deploys with the worker; the limiter stays inactive until a `CLIENTS` registry with `rpm` values exists. |\n| `scripts/attest.mjs` | Attestation tool: `keygen` (writes local `.attester-key.json`, prints public key) and `sign <origin> <skill> <valid_until>` (Ed25519 attestation JSON). |\n| `bench/` + `BENCHMARK.md` | `bench/run.mjs` (single-client latency harness against the deployed workers) and its raw results; `BENCHMARK.md` is the write-up. |\n| `quickjs.wasm` / `quickjs-asyncify.wasm` | Pre-compiled QuickJS binaries imported as static `CompiledWasm` modules. |\n| `minimemory_bg.wasm` | Pre-compiled minimemory (BM25) wasm, the engine behind `host.memorySearch`. Imported as a static `CompiledWasm` module by the gateway. |\n| `demo-site/` | Demo publisher site (`llms.txt` + `sum_numbers` / `server_time` skills). Deployed at `llmstxt-demo-site.rckflr.workers.dev`. |\n| `bookstore/` | Realistic publisher: D1-backed catalog (52 books), read skills + `create_order` write skill, plus permanent robustness fixtures (`corrupt_skill` hash-mismatch, `busy_loop` infinite loop). Deployed at `llmstxt-bookstore.rckflr.workers.dev`. |\n| `docs-site/` | Docs publisher: serves the llms-txt-skills spec documents + a `skills-index.snapshot` (BM25, `minimemory-okf-v1`), with `search_spec` (BM25 via `host.memorySearch`), `get_doc`, and `list_docs` skills. Deployed at `llmstxt-docs.rckflr.workers.dev`. |\n| `reports/` | Development reports, one `TAREA*-REPORT.md` per milestone (see below), plus the raw MCP-client outputs of T13-T15. |\n| `.github/workflows/ci.yml` | GitHub Actions CI: two jobs (`hermetic` gate + `prod-integration` non-blocking) on push and pull_request to `main`. |\n\n## CI\n\nThe workflow in `.github/workflows/ci.yml` runs two jobs on every push and\npull_request to `main`, both on `ubuntu-latest` with Node 22 and `npm ci`\n(with cache), timing out after 15 minutes.\n\nThe `hermetic` job is the gate. It runs five local suites — `npm test`,\n`npm run spike`, `npm run memspike`, `npm run gateway:offline`, `npm run\nlocal` — each preceded by its own build. None of these touch the network\nbeyond `npm` itself: `test`, `spike`, `memspike`, and `local` are fully local\n(the last spawns the stdio runtime against an in-process fake publisher on\n`127.0.0.1`), and `gateway:offline` is the hermetic mode of the gateway suite\n(T35), where the production workers are replaced by in-process fakes served\nthrough the same URL-to-binding map the gateway uses. Hermeticity is enforced\nby an outbound fetch interceptor: if anything in the suite tries to leave the\nprocess for the network, the run\nfails. This job blocks the merge.\n\nThe `prod-integration` job runs `npm run gateway`, the online gateway suite\nagainst the deployed production workers (`*.rckflr.workers.dev`) over the\npublic internet. This is the only command in CI that reaches production, and\nits purpose is to detect drift between the fakes and the real workers. It is\nnon-blocking (`continue-on-error`): an outage on their side surfaces as a\nwarning, not a red gate, so a foreign incident cannot block work in this repo.\n\n## Development notes\n\nEach milestone is documented in its `reports/TAREA*-REPORT.md` (TAREA1 through TAREA45;\n`TAREA2` and `TAREA30` were skipped in numbering and `TAREA12B` is a\ncontinuation of TAREA12).\nThe non-obvious bits live there:\n\n- `reports/TAREA4-REPORT.md` — deploying to Cloudflare Workers: the `CompiledWasm` rule\n  and why importing the `.wasm` as a static module avoids\n  \"Wasm code generation disallowed by embedder\".\n- `reports/TAREA5-REPORT.md` — the asyncify spike: why asyncify is needed for an\n  `await`-shaped capability, and the promise-pumping loop in\n  `AsyncToolHost.callTool`.\n- `reports/TAREA7-REPORT.md` — the gateway: sha256 verification, the Cache API use, and\n  the Cloudflare error 1042 (same-account worker-to-worker fetch via\n  `workers.dev`) workaround via a service binding.\n- `reports/TAREA12-REPORT.md` / `reports/TAREA12B-REPORT.md` — `Date.now()` is frozen in\n  Cloudflare Workers during synchronous execution, so a wall-clock deadline\n  never cuts a `while(true){}`. Fix: a deterministic gas budget — the interrupt\n  handler counts its own invocations and interrupts at 20 000, independent of\n  the clock. Calibrated against the heaviest legitimate skill.\n- `reports/TAREA14-REPORT.md` — `structuredContent` in an MCP result must be a JSON\n  object (MCP-shaped), not a bare scalar/array; the gateway normalizes tool\n  output accordingly.\n- `reports/TAREA19-REPORT.md` — concurrency: a per-wasm-module mutex on instantiation\n  plus single-flight discovery per origin, so parallel cold requests share one\n  discovery pass and one module build.\n- `reports/TAREA22-REPORT.md` — origin memory: the `skills-memory` line, sha256-verified\n  BM25 snapshot, and the `host.memorySearch` capability injected via\n  `extraCapabilities`.\n- `reports/TAREA25-REPORT.md` — skill attestations (Ed25519, WebCrypto, `REVIEWERS`\n  registry, verdicts, advisory/enforcing modes, `scripts/attest.mjs`).\n- `reports/TAREA26-REPORT.md` — code-review fixes: `extraCapabilities` now forwards all\n  positional args (so `host.memorySearch(q, k)` keeps `k`), and the\n  `fetchOrigin` timeout backstop timer is cleared on resolve (no leaked\n  timers).\n\nBenchmark headline numbers (full matrix and methodology in\n[`BENCHMARK.md`](./BENCHMARK.md), single-client from México to the Workers\nedge, not a load test; latest figures from the post-pool+preheat run):\nthe sandbox itself costs **~2 ms warm** (gateway pure-sandbox `sum_numbers`\np50 ≈ 55 ms vs. the same worker's raw ping p50 ≈ 53 ms), and the full gateway\nadds **~6 ms** over calling the publisher's API directly for the same read\n(`stock_report` through the gateway p50 = 96 ms vs. direct API p50 = 90 ms).\nA cold discovery miss costs ~210–400 ms (compile + sha256 + fetch); the\nscheduled preheat (see \"Security model\" above) keeps the cron's own\nisolate/colo mostly out of this cold path.\n\nRun the e2e tests with `npm test` (sync) / `npm run spike` (async) /\n`npm run gateway` (gateway against the live demo site) / `npm run memspike`\n(memory capability against the docs-site origin).";
const SEARCH_SPEC_TOOL_JS = "registerTool({\r\n  name: \"search_spec\",\r\n  description: \"BM25 search over the llms-txt-skills spec snapshot (4 documents). Returns hits as {text, score, title, concept_id}.\",\r\n  inputSchema: {\r\n    type: \"object\",\r\n    properties: {\r\n      q: { type: \"string\", description: \"Free-text BM25 query over the spec documents.\" },\r\n      k: { type: \"number\", description: \"Max number of hits to return (integer 1..10, default 5).\" }\r\n    },\r\n    required: [\"q\"]\r\n  },\r\n  handler: async function (args) {\r\n    args = args || {};\r\n    // Validacion de tipos (sandbox ECMAScript puro).\r\n    if (typeof args.q !== \"string\" || args.q.trim().length === 0) {\r\n      throw new Error(\"q must be a non-empty string\");\r\n    }\r\n    let k = 5;\r\n    if (args.k !== undefined && args.k !== null) {\r\n      if (typeof args.k !== \"number\" || !Number.isFinite(args.k) || Math.floor(args.k) !== args.k) {\r\n        throw new Error(\"k must be an integer number\");\r\n      }\r\n      if (args.k < 1 || args.k > 10) {\r\n        throw new Error(\"k must be between 1 and 10 inclusive\");\r\n      }\r\n      k = args.k;\r\n    }\r\n    // host.memorySearch(query, k) es la capability que implementa el gateway\r\n    // (provisional). Devuelve {hits:[{text,score,title,concept_id}]} o {error}.\r\n    // Check defensivo: si la capability no se inyecto, devolver un error de tool\r\n    // claro en vez de lanzar TypeError (host es undefined / no tiene el metodo).\r\n    if (typeof host === \"undefined\" || host === null ||\r\n        typeof host.memorySearch !== \"function\") {\r\n      return { ok: false, error: \"memory capability unavailable\" };\r\n    }\r\n    const r = await host.memorySearch(args.q, k);\r\n    if (r && r.error) throw new Error(r.error);\r\n    return r;\r\n  }\r\n});";
const SEARCH_SPEC_SKILL_MD = "---\r\nname: search_spec\r\nversion: 1.0.0\r\nlicense: MIT\r\n---\r\n\r\n# search_spec\r\n\r\nBM25 search over the llms-txt-skills spec snapshot (4 documents: the RFC, the\r\nexecutable-skills extension, the skill-attestations extension, and the mcpwasm\r\nreference README). Uses the gateway-provided `host.memorySearch` capability over\r\na hash-pinned snapshot declared in `/llms.txt` via the `skills-memory` line.\r\n\r\n## Arguments\r\n\r\n- `q` (string, required): free-text BM25 query.\r\n- `k` (number, optional): max hits to return, integer 1..10, default 5.\r\n\r\n## Returns\r\n\r\n`{ hits: [{ text, score, title, concept_id }] }` — `text` is the matched chunk\r\nsnippet, `score` is the BM25 score (higher magnitude = better match), `title`\r\nis `<doc>: <section>`, `concept_id` identifies the chunk.\r\n\r\n## Example\r\n\r\n```json\r\n{ \"q\": \"tool_sha256 integrity verification\", \"k\": 5 }\r\n```";
const GET_DOC_TOOL_JS = "registerTool({\r\n  name: \"get_doc\",\r\n  description: \"Fetch one of the 4 published llms-txt-skills documents by name. Returns {name, length, content} with content truncated to 4000 chars.\",\r\n  inputSchema: {\r\n    type: \"object\",\r\n    properties: {\r\n      name: {\r\n        type: \"string\",\r\n        description: \"Document name.\",\r\n        enum: [\"rfc-skills-in-llms-txt\", \"ext-executable-skills\", \"ext-skill-attestations\", \"mcpwasm-readme\"]\r\n      }\r\n    },\r\n    required: [\"name\"]\r\n  },\r\n  handler: async function (args) {\r\n    args = args || {};\r\n    var allowed = [\"rfc-skills-in-llms-txt\", \"ext-executable-skills\", \"ext-skill-attestations\", \"mcpwasm-readme\"];\r\n    if (typeof args.name !== \"string\" || allowed.indexOf(args.name) === -1) {\r\n      throw new Error(\"name must be one of: \" + allowed.join(\", \"));\r\n    }\r\n    // query string a mano: no URLSearchParams en el sandbox QuickJS.\r\n    var path = \"/docs/\" + encodeURIComponent(args.name) + \".md\";\r\n    var r = await host.fetchOrigin(path);\r\n    if (r.status >= 400) {\r\n      return { name: args.name, length: 0, content: \"\", error: \"fetch failed with status \" + r.status };\r\n    }\r\n    var body = typeof r.body === \"string\" ? r.body : String(r.body);\r\n    var content = body.length > 4000 ? body.slice(0, 4000) : body;\r\n    return { name: args.name, length: body.length, content: content };\r\n  }\r\n});";
const GET_DOC_SKILL_MD = "---\r\nname: get_doc\r\nversion: 1.0.0\r\nlicense: MIT\r\n---\r\n\r\n# get_doc\r\n\r\nFetch the full markdown of one of the 4 published documents via\r\n`host.fetchOrigin(\"/docs/<name>.md\")`.\r\n\r\n## Arguments\r\n\r\n- `name` (string, required): one of `rfc-skills-in-llms-txt`,\r\n  `ext-executable-skills`, `ext-skill-attestations`, `mcpwasm-readme`.\r\n\r\n## Returns\r\n\r\n`{ name, length, content }` — `content` is the markdown body truncated to 4000\r\nchars; `length` is the full body length in characters.\r\n\r\n## Example\r\n\r\n```json\r\n{ \"name\": \"ext-executable-skills\" }\r\n```";
const LIST_DOCS_TOOL_JS = "registerTool({\r\n  name: \"list_docs\",\r\n  description: \"List the 4 published llms-txt-skills documents with title and path. Static, no network fetch.\",\r\n  inputSchema: {\r\n    type: \"object\",\r\n    properties: {}\r\n  },\r\n  handler: async function (args) {\r\n    return {\r\n      docs: [\r\n        { name: \"rfc-skills-in-llms-txt\", title: \"RFC: Publishing Agent Skills through llms.txt\", path: \"/docs/rfc-skills-in-llms-txt.md\" },\r\n        { name: \"ext-executable-skills\", title: \"Extension: Executable Skills\", path: \"/docs/ext-executable-skills.md\" },\r\n        { name: \"ext-skill-attestations\", title: \"Extension: Skill Attestations\", path: \"/docs/ext-skill-attestations.md\" },\r\n        { name: \"mcpwasm-readme\", title: \"mcpwasm — Static MCP (reference implementation README)\", path: \"/docs/mcpwasm-readme.md\" }\r\n      ]\r\n    };\r\n  }\r\n});";
const LIST_DOCS_SKILL_MD = "---\r\nname: list_docs\r\nversion: 1.0.0\r\nlicense: MIT\r\n---\r\n\r\n# list_docs\r\n\r\nList the 4 published llms-txt-skills documents with their title and path.\r\nStatic — no network fetch.\r\n\r\n## Arguments\r\n\r\nNone.\r\n\r\n## Returns\r\n\r\n`{ docs: [{ name, title, path }] }` for the 4 documents.";
const SNAPSHOT = "[{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-18#0\",\"metadata\":{\"content\":\"The attestation object's signed payload is an [in-toto Statement v1](https://in-toto.io/Statement/v1) inside a DSSE envelope, whose `predicate` must carry the same 5 fields as the Ed25519 model (`origin`, `skill`, `tool_sha256`, `signed_on`, `valid_until`) — verified to match the attestation's own top-level fields, so a validly-signed bundle for skill A cannot be relabeled as skill B's attestation without re-signing. `sigstore-attest.mjs` exports `verifySigstoreAttestation` (the verifier) and `buildSigstoreStatement` (the canonical payload shape a publisher signs with `sigstore attest` or an equivalent SDK call — this repo does not ship a signing tool for it, since producing a real Sigstore signature needs a live OIDC flow, out of scope for a script run here).\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-18\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-47#1\",\"metadata\":{\"content\":\"workflows the MCP does — in **~3 tool calls vs. the MCP's 6–7**, with **validation preserved** (unlike a naive RES\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-47\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-72#0\",\"metadata\":{\"content\":\"**Trade-off:** It adds one round-trip per new domain. Agents SHOULD cache negative responses (404) to avoid repeated probes.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-72\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"ext-executable-skills-2-1-skill-entry-9#0\",\"metadata\":{\"content\":\"The key is deliberately **not** `sha256`: the core RFC already uses `sha256` (inline and in `/.well-known/agent-skills/index.json`) for the hash of the fetched `SKILL.md`. The two keys MAY appear on the same skill line and verify different files; runtimes MUST NOT conflate them.\",\"okf_concept\":\"ext-executable-skills-2-1-skill-entry-9\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.1 Skill entry\"}},{\"id\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-10#0\",\"metadata\":{\"content\":\"**What this buys over pre-registered Ed25519:** *any* OIDC identity can produce a valid Sigstore signature (verifiable against Fulcio's public root + Rekor's public transparency log) with no prior coordination with the publisher or runtime operator. The runtime's trust decision shifts from \\\"which public keys do I whitelist\\\" to \\\"which *identities* do I require\\\" — e.g. \\\"only this specific GitHub Actions release workflow\\\", still an allowlist, but one that scales to any number of contributors without a manual key-registration step per person. This is the core RFC §4.6 \\\"higher assurance... keyless signing via a transparency log\\\" recommendation, applied to this extension's third trust ring specifically.\",\"okf_concept\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-10\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.1b Alternative: Sigstore (keyless) attestation\"}},{\"id\":\"mcpwasm-readme-repository-layout-70#0\",\"metadata\":{\"content\":\"| File / dir | Purpose | |---|---| | `CHANGELOG.md` | Per-release changes of the published npm package (verified against the actual tarballs). | | `host.mjs` | Synchronous `ToolHost`: loads `tool.js` into QuickJS-wasm, injects the `host.callInternal` capability. | | `host-async.mjs` | `AsyncToolHost`: asyncify variant, async handlers, `host.fetchOrigin` capability, the `extraCapabilities` bridge (`host.memorySearch`), mem/stack/gas hardening. | | `mcp-core.mjs` | Sync MCP JSON-RPC 2.0 core (`initialize`, `tools/list`, `tools/call`, `ping`). Transport-agnostic. | | `mcp-core-async.mjs` | Async MCP core; awaits `AsyncToolHost.callTool`. | | `worker.mjs` | PoC MCP server (sync host, inline tools) deployed at `toolhost-mcp.rckflr.workers.dev`. | | `worker-spike.mjs` | Async spike (fetch_home/f\",\"okf_concept\":\"mcpwasm-readme-repository-layout-70\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Repository layout\"}},{\"id\":\"ext-skill-attestations-2-3-publication-12#0\",\"metadata\":{\"content\":\"Attestations are published by the origin as a static JSON array at:\",\"okf_concept\":\"ext-skill-attestations-2-3-publication-12\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.3 Publication\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-20#0\",\"metadata\":{\"content\":\"#### Developing your own skills: `--serve <dir>`\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-20\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"rfc-skills-in-llms-txt-3-2-relationship-to-agents-txt-the-actio-37#0\",\"metadata\":{\"content\":\"**Concrete interoperability (implemented).** The reference generator emits `/.well-known/agent-skills/index.json` as a **superset of the agentskills.io `discovery/0.2.0` schema**: every skill carries the fields that schema expects (`name`, `type: \\\"skill-md\\\"`, `description`, `url`, `digest: \\\"sha256:…\\\"`) *plus* this RFC's extensions (`version`, `license`, `homepage`, raw `sha256`, ed25519 `signature`, and a top-level `signing_key`). One file therefore satisfies both an `agents.txt`/agentskills.io consumer (discovery + integrity) and a Tier-2 consumer (authenticity) — the publisher maintains no second artifact. The redundant `digest` (prefixed form) and `sha256` (raw hex) fields are kept in parallel so existing consumers that read raw `sha256` are not broken.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-2-relationship-to-agents-txt-the-actio-37\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.2 Relationship to `agents.txt` (the action layer)\"}},{\"id\":\"rfc-skills-in-llms-txt-1-4-why-structured-over-prose-12#0\",\"metadata\":{\"content\":\"`llms.txt` is free-form markdown. A site could write skill discovery as prose:\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-4-why-structured-over-prose-12\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.4 Why structured over prose\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-12#0\",\"metadata\":{\"content\":\"Honest limits (stated in `bin/mcpwasm-local.mjs`): discovery runs once per process (restart to refresh). Hash verification and the sandbox model (per-skill contexts, origin-scoped `fetchOrigin`, resource limits) are the same as the gateway's. **Origin memory is supported**: if the origin declares `skills-memory`, the snapshot is fetched, verified against `snapshot_sha256`, and `host.memorySearch` is injected — same contract as the gateway; on any mismatch (or if the optional `@rckflr/minimemory` engine is missing) the capability is simply absent and skills that call it fail closed in-sandbox. Each verified skill's `SKILL.md` recipe is served as an MCP resource and via the `get_skill_guide` tool (see \\\"Skill recipes as MCP resources\\\").\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-12\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"rfc-skills-in-llms-txt-8-open-questions-88#1\",\"metadata\":{\"content\":\"is more natural for git-hosted skill bundles. 4. **Signature scheme beyond `sha256`.** Resolved (§4.6): a two-tier model — `sha256` for integrity (required when declared), plus optional ed25519 signatures over an offline key\",\"okf_concept\":\"rfc-skills-in-llms-txt-8-open-questions-88\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 8. Open Questions\"}},{\"id\":\"rfc-skills-in-llms-txt-8-open-questions-91#0\",\"metadata\":{\"content\":\"6. **Multi-project origins (namespacing).** Resolved (Executable Skills extension v0.5 SS2.5): an optional per-skill `scope` key -> runtime-side tool prefixing `<scope>__<name>` (published bytes and hashes untouched, preserving content-addressing and the universal-template property), plus one `skills-memory` line per scope binding each snapshot to its scope's skills. Public-name collisions are skipped with a diagnostic (first wins); attestations unaffected. Original problem statement, kept for the record: an origin that aggregates skills for MULTIPLE projects — e.g.\",\"okf_concept\":\"rfc-skills-in-llms-txt-8-open-questions-91\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 8. Open Questions\"}},{\"id\":\"mcpwasm-readme-try-the-deployed-gateway-curl-64#0\",\"metadata\":{\"content\":\"Call `sum_numbers` (pure sync tool, runs in the sandbox):\",\"okf_concept\":\"mcpwasm-readme-try-the-deployed-gateway-curl-64\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Try the deployed gateway (curl)\"}},{\"id\":\"rfc-skills-in-llms-txt-2-2-optional-inline-metadata-20#0\",\"metadata\":{\"content\":\"The only recognized key is `version`. It is a human-readable hint for quick identification, not a security mechanism. Agents that do not understand the comment MUST ignore it.\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-2-optional-inline-metadata-20\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2.2 Optional inline metadata\"}},{\"id\":\"mcpwasm-readme-security-model-honest-69#1\",\"metadata\":{\"content\":\"elds `401`. `AUTH_TOKEN` is ignored in this mode. If `CLIENTS` is set but its JSON is invalid, the\",\"okf_concept\":\"mcpwasm-readme-security-model-honest-69\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Security model (honest)\"}},{\"id\":\"rfc-skills-in-llms-txt-9-reference-implementation-94#0\",\"metadata\":{\"content\":\"- [`/llms.txt`](https://img.automators.work/llms.txt) — contains a `## Skills` section - [`/skills/placeholder/SKILL.md`](https://img.automators.work/skills/placeholder/SKILL.md) — the skill itself - [`/docs/rfc-skills-in-llms-txt.md`](https://img.automators.work/docs/rfc-skills-in-llms-txt.md) — this document - [`/scripts/parse_llms_txt_skills.py`](https://img.automators.work/scripts/parse_llms_txt_skills.py) — reference parser - [`/scripts/validate.py`](https://img.automators.work/scripts/validate.py) — validator - [`/schema/llms-txt-skills.schema.json`](https://img.automators.work/schema/llms-txt-skills.schema.json) — JSON schema\",\"okf_concept\":\"rfc-skills-in-llms-txt-9-reference-implementation-94\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 9. Reference Implementation\"}},{\"id\":\"rfc-skills-in-llms-txt-1-1-two-ecosystems-that-do-not-talk-to-e-3#0\",\"metadata\":{\"content\":\"The result: a site describes itself in `llms.txt`, and it publishes a `SKILL.md` somewhere, but there is **no standardized way** for the site to say *\\\"here is the skill you need to work with me\\\"* — and no standardized way for an agent to discover it.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-1-two-ecosystems-that-do-not-talk-to-e-3\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.1 Two ecosystems that do not talk to each other\"}},{\"id\":\"ext-executable-skills-1-motivation-5#0\",\"metadata\":{\"content\":\"1. The **publication format** for executable skills (publisher side). 2. The **minimum requirements** for a runtime that chooses to execute them (consumer side).\",\"okf_concept\":\"ext-executable-skills-1-motivation-5\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 1. Motivation\"}},{\"id\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-8#0\",\"metadata\":{\"content\":\"The Ed25519 model above requires pre-registering every reviewer's public key (§3) — a coordination step that does not scale past a handful of trusted individuals. As a **second, optional** attestation type, an entry MAY carry a `sigstore_bundle` instead of `signature`:\",\"okf_concept\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-8\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.1b Alternative: Sigstore (keyless) attestation\"}},{\"id\":\"ext-executable-skills-7-changelog-30#1\",\"metadata\":{\"content\":\"lity; runtime requirement for snapshot integrity. Field-tested: the reference gateway serves BM25 search over the spec\",\"okf_concept\":\"ext-executable-skills-7-changelog-30\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 7. Changelog\"}},{\"id\":\"ext-executable-skills-2-5-scopes-multi-project-origins-21#0\",\"metadata\":{\"content\":\"- `scope` (optional, per skill line, pattern `^[a-z][a-z0-9_-]*$`). When present, the runtime MUST expose every tool registered by that skill's `tool.js` under the public name `<scope>__<registered-name>` (double underscore separator), and route calls back to the unmodified sandbox context. The `tool.js` bytes, its `tool_sha256`, and the names it registers internally are UNCHANGED — prefixing is a host-boundary rename only. - Skill recipes exposed as MCP resources follow the public name (`skill://<scope>__<name>`), as does any recipe-fallback tool parameter. - A `skills-memory` line with `scope` binds its snapshot to the skills of that scope only: the runtime injects each scope's `host.memorySearch` from its own verified snapshot. Skills without `scope` get the unscoped memory (if any).\",\"okf_concept\":\"ext-executable-skills-2-5-scopes-multi-project-origins-21\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.5 Scopes: multi-project origins\"}},{\"id\":\"ext-executable-skills-2-2-the-artifact-tool-js-11#0\",\"metadata\":{\"content\":\"The artifact is a single JavaScript file that registers exactly one tool:\",\"okf_concept\":\"ext-executable-skills-2-2-the-artifact-tool-js-11\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.2 The artifact (`tool.js`)\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-51#0\",\"metadata\":{\"content\":\"This section describes the **gateway's** model: Ed25519 signatures from a runtime-side pre-registered `REVIEWERS` key registry. The **local runtime** additionally supports **Sigstore (keyless)** attestations via `--require-attestation` — no pre-registered key, any OIDC identity the runtime explicitly trusts — see \\\"Sigstore attestations\\\" above; that section closes the \\\"only one registered reviewer scales\\\" bottleneck this one has.\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-51\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"mcpwasm-readme-try-the-deployed-gateway-curl-67#0\",\"metadata\":{\"content\":\"- PoC sync host: `https://toolhost-mcp.rckflr.workers.dev` (POST `/mcp`). - Demo publisher: `https://llmstxt-demo-site.rckflr.workers.dev/llms.txt`. - Bookstore publisher: `https://llmstxt-bookstore.rckflr.workers.dev/llms.txt`. - Docs publisher (origin memory): `https://llmstxt-docs.rckflr.workers.dev/llms.txt`.\",\"okf_concept\":\"mcpwasm-readme-try-the-deployed-gateway-curl-67\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Try the deployed gateway (curl)\"}},{\"id\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-80#0\",\"metadata\":{\"content\":\"**Implications:** - A publisher cannot assume that publishing a skill guarantees the agent will use it as written. - The RFC defines *what* to publish and *where* to find it, but not *how* the runtime must enforce execution. - This is intentionally out of scope (Section 7), but it is a real, observable gap that future work should address.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-80\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.3 Observed Execution Gap\"}},{\"id\":\"mcpwasm-readme-if-you-already-have-an-api-30#0\",\"metadata\":{\"content\":\"You do not need to build or maintain an MCP protocol server — that is the whole point. The runtime (local or gateway) already handles JSON-RPC, `tools/list`, `tools/call`, and the transport; none of that is your code.\",\"okf_concept\":\"mcpwasm-readme-if-you-already-have-an-api-30\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: If you already have an API\"}},{\"id\":\"ext-executable-skills-5-reference-implementation-27#1\",\"metadata\":{\"content\":\"s://llmstxt-gateway.rckflr.workers.dev/mcp?origin=<u\",\"okf_concept\":\"ext-executable-skills-5-reference-implementation-27\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 5. Reference implementation\"}},{\"id\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-58#0\",\"metadata\":{\"content\":\"**What this RFC adds, even today:** when an agent does encounter a site — via user instruction, a URL in context, or a tool result — a `## Skills` section gives it an unambiguous, machine-readable signal that purpose-built skills are available. That is more than any site can currently express through `llms.txt` alone.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-58\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5. On Agent-Side Discovery Triggers\"}},{\"id\":\"ext-skill-attestations-3-reviewer-registry-trust-anchor-18#0\",\"metadata\":{\"content\":\"Runtimes MAY populate it by explicit configuration (recommended) or TOFU-pin keys on first sight (consistent with core RFC §4.6). An attestation from an unknown attester verifies as UNKNOWN-ATTESTER, not as valid. For a `sigstore:` entry, the registry maps the identity to its expected OIDC `issuer` (rather than a public key); verification asks Sigstore's own trust infrastructure (Fulcio, Rekor) whether the bundle's certificate was validly issued to that exact identity by that issuer — the runtime never handles or stores a reviewer's private key material, because there isn't one to store.\",\"okf_concept\":\"ext-skill-attestations-3-reviewer-registry-trust-anchor-18\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 3. Reviewer registry (trust anchor)\"}},{\"id\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-9#0\",\"metadata\":{\"content\":\"`attester` is prefixed `sigstore:` followed by the exact OIDC identity URI (e.g. a GitHub Actions workflow ref, matching the certificate's Subject Alternative Name) — not a pre-registered key name. `sigstore_bundle` is a standard [Sigstore Bundle](https://github.com/sigstore/protobuf-specs) (DSSE envelope + Fulcio certificate chain + Rekor transparency-log inclusion proof).\",\"okf_concept\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-9\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.1b Alternative: Sigstore (keyless) attestation\"}},{\"id\":\"mcpwasm-readme-scopes-multiple-projects-on-one-origin-45#0\",\"metadata\":{\"content\":\"``` <!-- skills-memory: {\\\"snapshot\\\":\\\"/KDD/skills-index.snapshot\\\",\\\"snapshot_sha256\\\":\\\"…\\\",\\\"format\\\":\\\"minimemory-okf-v1\\\",\\\"scope\\\":\\\"kdd\\\"} -->\",\"okf_concept\":\"mcpwasm-readme-scopes-multiple-projects-on-one-origin-45\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Scopes — multiple projects on one origin\"}},{\"id\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-15#0\",\"metadata\":{\"content\":\"| Key | Requirement | Meaning | |---|---|---| | `snapshot` | REQUIRED | Same-origin path to the snapshot file. | | `snapshot_sha256` | REQUIRED | Lowercase hex SHA-256 of the exact bytes served at `snapshot`. (Named to avoid the core RFC's `sha256`, same rationale as `tool_sha256`.) | | `format` | REQUIRED | Snapshot format identifier. Runtimes MUST ignore a `skills-memory` line whose format they do not support (skills load normally; the capability is simply absent). This draft registers one format: `minimemory-okf-v1` (BM25-only index exported by [minimemory](https://github.com/MauricioPerera/minimemory)'s `WasmOkfIndex`; no embeddings, no query-time model needed). |\",\"okf_concept\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-15\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.4 Origin memory: search snapshots\"}},{\"id\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-19#0\",\"metadata\":{\"content\":\"An `llms.txt` MAY declare **multiple** `skills-memory` lines when each carries a distinct `scope` key (§2.5); at most ONE line may omit `scope` (the default/unscoped memory). Without scopes, exactly one line is allowed — the pre-v0.5 behavior, unchanged.\",\"okf_concept\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-19\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.4 Origin memory: search snapshots\"}},{\"id\":\"mcpwasm-readme-mcpwasm-static-mcp-2#0\",\"metadata\":{\"content\":\"mcpwasm is the reference implementation: a sandboxed runtime for third-party MCP tools (untrusted tool code inside QuickJS-wasm on Cloudflare Workers), plus a gateway that turns any static site publishing `llms.txt` with executable skills into an MCP server.\",\"okf_concept\":\"mcpwasm-readme-mcpwasm-static-mcp-2\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: mcpwasm — Static MCP\"}},{\"id\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-27#0\",\"metadata\":{\"content\":\"*Example:* A documentation site teaches agents to quote specific sections when answering questions about its content.\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-27\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2.4 Two primary use cases\"}},{\"id\":\"mcpwasm-readme-capability-support-by-runtime-42#0\",\"metadata\":{\"content\":\"| Capability | Gateway (`worker-gateway.mjs`, Workers) | Local runtime (`bin/mcpwasm-local.mjs`, Node) | | :--- | :---: | :---: | | Origin memory — `host.memorySearch` | ✅ full | ✅ full (engine is an optionalDependency; if missing, degrades to capability-absent) | | Sigstore (keyless) attestations | ❌ Workers has no `node:fs` for `@sigstore/tuf`'s trust-root cache | ✅ `--require-attestation` |\",\"okf_concept\":\"mcpwasm-readme-capability-support-by-runtime-42\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Capability support by runtime\"}},{\"id\":\"rfc-skills-in-llms-txt-1-1-two-ecosystems-that-do-not-talk-to-e-2#0\",\"metadata\":{\"content\":\"`llms.txt` tells LLMs what a domain *is*. Agent Skills (`SKILL.md`) tell an agent *how to use* that domain. Both exist. Neither knows about the other.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-1-two-ecosystems-that-do-not-talk-to-e-2\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.1 Two ecosystems that do not talk to each other\"}},{\"id\":\"rfc-skills-in-llms-txt-2-2-optional-inline-metadata-21#0\",\"metadata\":{\"content\":\"**For integrity verification and full metadata** (sha256, license, cost estimates, requirements), agents SHOULD fetch `/.well-known/agent-skills/index.json` if available. That document is the canonical metadata source. The `## Skills` entry is the discovery pointer; `.well-known` is the verification and metadata layer.\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-2-optional-inline-metadata-21\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2.2 Optional inline metadata\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-15#0\",\"metadata\":{\"content\":\"#### Sigstore attestations: `--require-attestation` (local runtime only)\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-15\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"mcpwasm-readme-skill-recipes-skill-md-as-mcp-resources-48#0\",\"metadata\":{\"content\":\"Discovery also fetches each verified skill's `SKILL.md` and verifies it against the `sha256` declared in the same `llms.txt` line (core RFC field). Verified recipes are exposed two ways:\",\"okf_concept\":\"mcpwasm-readme-skill-recipes-skill-md-as-mcp-resources-48\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill recipes (SKILL.md) as MCP resources\"}},{\"id\":\"mcpwasm-readme-security-model-honest-69#0\",\"metadata\":{\"content\":\"- **Auth has three modes, selected by config.** Precedence is per-client → legacy shared token → dev open. - *Per-client (`CLIENTS` secret, opt-in).* `CLIENTS` is a JSON secret mapping `sha256_hex_of_token → { client_id, rpm? }`. Tokens never appear in cleartext in config — the key is the lowercase hex SHA-256 of the token's UTF-8 bytes. On `POST /mcp` the gateway hashes the `Authorization: Bearer <token>` value and does an exact lookup on that hash; the lookup *is* the timing-safe mechanism (a fixed digest is compared, never the cleartext token against a secret). A known token passes and the response carries `X-Gw-Client: <client_id>`; an unknown token, missing header, or malformed header yields `401`. `AUTH_TOKEN` is ignored in this mode.\",\"okf_concept\":\"mcpwasm-readme-security-model-honest-69\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Security model (honest)\"}},{\"id\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-55#0\",\"metadata\":{\"content\":\"This RFC defines the *publishing* side of the protocol. It does not mandate when or how agents decide to read `llms.txt`.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-55\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5. On Agent-Side Discovery Triggers\"}},{\"id\":\"rfc-skills-in-llms-txt-1-4-why-structured-over-prose-13#0\",\"metadata\":{\"content\":\"This works for an LLM reading the document. It does not work for:\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-4-why-structured-over-prose-13\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.4 Why structured over prose\"}},{\"id\":\"ext-executable-skills-2-5-scopes-multi-project-origins-20#0\",\"metadata\":{\"content\":\"Resolves core RFC Open Question 6. An origin that aggregates skills from MULTIPLE projects — e.g. a GitHub user/org ROOT site bridging several project pages, since a URL's origin strips the path — collides on two axes: tool names must be unique per MCP server, and (pre-v0.5) only one `skills-memory` line existed per origin. `scope` resolves both **declaratively, at the aggregation layer, without touching published bytes** — which is mandatory, because content-addressing binds hashes to bytes: renaming inside a `tool.js` would destroy the universal-template property (one stable `tool_sha256` ecosystem-wide for shared tools like `search_knowledge`).\",\"okf_concept\":\"ext-executable-skills-2-5-scopes-multi-project-origins-20\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.5 Scopes: multi-project origins\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-10#0\",\"metadata\":{\"content\":\"The package also ships a stdio MCP server that runs an origin's skills **locally**: it fetches `/llms.txt`, verifies every `tool_sha256`, loads each verified skill into its own QuickJS-wasm context, and speaks MCP over stdin/stdout — so a static site (e.g. a GitHub Pages *user* site) becomes an MCP server on your machine with zero deployed infrastructure on either side:\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-10\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"mcpwasm-readme-npx-y-rckflr-mcpwasm-serve-my-skills-rep-23#0\",\"metadata\":{\"content\":\"This is meant for developing and testing your own skills locally before publishing them (to GitHub Pages or any other static host) — not for browsing someone else's GitHub repo directly. Path-traversal requests against the internal file server are rejected (resolved and checked against the served directory's root); covered by `npm run local`.\",\"okf_concept\":\"mcpwasm-readme-npx-y-rckflr-mcpwasm-serve-my-skills-rep-23\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: npx -y @rckflr/mcpwasm --serve ./my-skills-repo --port 4000   (fixed port, optional)\"}},{\"id\":\"rfc-skills-in-llms-txt-1-3-the-well-known-convention-is-limited-9#0\",\"metadata\":{\"content\":\"The emerging `/.well-known/skills/default/skill.md` convention (proposed by Cloudflare, adopted by Mintlify) works when: - The agent knows the domain in advance. - The agent is configured to probe `.well-known` paths.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-3-the-well-known-convention-is-limited-9\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.3 The well-known convention is limited\"}},{\"id\":\"ext-executable-skills-1-motivation-3#0\",\"metadata\":{\"content\":\"The result is **Static MCP** — *your tools are files, not servers*: skills published as static, hash-verified content and executed sandboxed on demand. What static site hosting did to web servers (\\\"do not run Apache, publish HTML\\\"), this does to MCP servers: the publisher runs no infrastructure at all; the MCP server is materialized per request from the published files and evaporates after responding (ephemeral instance, durable definition).\",\"okf_concept\":\"ext-executable-skills-1-motivation-3\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 1. Motivation\"}},{\"id\":\"ext-skill-attestations-1-motivation-3#0\",\"metadata\":{\"content\":\"1. **Age is a proxy, not truth.** Whether code is still trustworthy is a human judgment; the only honest way to capture it is a human signature with an explicit validity window. Nothing here pretends to automate that judgment. 2. **Zero infrastructure, like everything else in this standard.** Attestations are static JSON files. No transparency log, no CA, no server.\",\"okf_concept\":\"ext-skill-attestations-1-motivation-3\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 1. Motivation\"}},{\"id\":\"mcpwasm-readme-mcpwasm-static-mcp-4#0\",\"metadata\":{\"content\":\"This repo integrates with the [llms-txt-skills](https://github.com/MauricioPerera/llms-txt-skills) standard via two provisional extensions adopted in the spec: **executable skills** (v0.4, with *origin memory*) and **skill attestations** (v0.4). See the dedicated sections below.\",\"okf_concept\":\"mcpwasm-readme-mcpwasm-static-mcp-4\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: mcpwasm — Static MCP\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-12#1\",\"metadata\":{\"content\":\"ide` tool (see \\\"Skill recipes as MCP resources\\\"). Trust is your choice of origin by default (no attestatio\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-12\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"ext-skill-attestations-7-changelog-29#0\",\"metadata\":{\"content\":\"- **v0.4 (2026-07-09):** Added §2.4 (normative guidance): Sigstore is the RECOMMENDED default attester type for new deployments; pre-registered Ed25519 becomes the explicit profile for constrained environments (no network/filesystem at verification time, air-gapped review, no external trust-infrastructure dependency). Converges the two parallel models into one decision rule; no wire-format or verification-semantics change — v0.3 registries and attestations remain valid as-is. - **v0.3 (2026-07-09):** Added §2.1b: Sigstore (keyless) attestations as a second, optional attester type alongside Ed25519 — closes the \\\"only pre-registered reviewers scale\\\" bottleneck (§5) by trusting OIDC identities instead of pre-shared keys.\",\"okf_concept\":\"ext-skill-attestations-7-changelog-29\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 7. Changelog\"}},{\"id\":\"mcpwasm-readme-static-mcp-vs-a-traditional-mcp-server-29#0\",\"metadata\":{\"content\":\"The takeaway that doesn't fit in a table: a traditional MCP server answers \\\"how do I expose this logic as a tool?\\\" Static MCP additionally answers \\\"how do I run code from an origin I don't fully trust?\\\" If you write and control the server yourself, traditional is simpler and none of this is necessary. Static MCP matters when the tool code comes from *someone else*, and you want a verifiable guarantee (hash + sandbox, optionally review) before running it — that is the problem this repo exists to solve, not a general-purpose alternative to MCP.\",\"okf_concept\":\"mcpwasm-readme-static-mcp-vs-a-traditional-mcp-server-29\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Static MCP vs. a traditional MCP server\"}},{\"id\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-24#0\",\"metadata\":{\"content\":\"*Example:* `img.automators.work` teaches agents to call `/{width}x{height}?bg={hex}` to generate placeholder images.\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-24\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2.4 Two primary use cases\"}},{\"id\":\"ext-skill-attestations-2-2-signing-payload-6#0\",\"metadata\":{\"content\":\"The signature covers the UTF-8 bytes of the canonical string:\",\"okf_concept\":\"ext-skill-attestations-2-2-signing-payload-6\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.2 Signing payload\"}},{\"id\":\"mcpwasm-readme-use-it-now-any-static-site-an-mcp-server-6#0\",\"metadata\":{\"content\":\"Wire it into an MCP client (Claude Code, Cursor, Cline, …):\",\"okf_concept\":\"mcpwasm-readme-use-it-now-any-static-site-an-mcp-server-6\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Use it now — any static site → an MCP server\"}},{\"id\":\"rfc-skills-in-llms-txt-8-open-questions-88#0\",\"metadata\":{\"content\":\"1. **Should `llms.txt` grow parallel `## MCP` and `## Agents` sections**, making it the single discovery document for a domain's full agent surface? Or should each standard manage its own discovery separately? 2. **Cross-origin skill trust model.** Should cross-origin skills be disallowed entirely, allowed with elevated confirmation, or allowed freely? Current proposal: allowed with elevated confirmation (§4.4). 3. **Archive format.** Should `.zip` be the only mandated archive format, or should `.tar.gz` remain in scope? `.zip` is more universally supported; `.tar.gz` is more natural for git-hosted skill bundles. 4.\",\"okf_concept\":\"rfc-skills-in-llms-txt-8-open-questions-88\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 8. Open Questions\"}},{\"id\":\"rfc-skills-in-llms-txt-3-2-relationship-to-agents-txt-the-actio-34#0\",\"metadata\":{\"content\":\"**[agents.txt](https://agents-txt.com).** A capability-declaration file (`/agents.txt` + optional `/agents.json`) that advertises which agent-facing protocols a site supports — payments (x402, MPP, AP2), authorization (agent-auth, OAuth2), MCP, A2A, UCP, WebMCP, and **Skills**. It positions itself as \\\"Layer 4 (action)\\\" above `robots.txt` (access) and `llms.txt` (content). Its `Skills:` directive and its `/.well-known/agent-skills/index.json` both build on the [Agent Skills](https://agentskills.io) `discovery/0.2.0` index schema.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-2-relationship-to-agents-txt-the-actio-34\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.2 Relationship to `agents.txt` (the action layer)\"}},{\"id\":\"ext-executable-skills-2-3-versioning-and-updates-13#0\",\"metadata\":{\"content\":\"The `tool_sha256` pins the artifact. Publishing a new artifact version means serving new bytes at `tool` and updating both `tool_sha256` and `version` in `llms.txt`. Runtimes cache by hash; a stale hash simply keeps serving the old, verified artifact until the index updates.\",\"okf_concept\":\"ext-executable-skills-2-3-versioning-and-updates-13\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.3 Versioning and updates\"}},{\"id\":\"ext-skill-attestations-4-runtime-behavior-22#0\",\"metadata\":{\"content\":\"Runtimes SHOULD support at least two modes:\",\"okf_concept\":\"ext-skill-attestations-4-runtime-behavior-22\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 4. Runtime behavior\"}},{\"id\":\"ext-executable-skills-4-security-considerations-25#0\",\"metadata\":{\"content\":\"- **What the hash buys:** whoever can edit the site cannot silently swap artifact bytes out from under a cached/pinned hash; and a runtime never executes bytes it did not verify. It does **not** authenticate the publisher — for that, compose with the signature scheme of core RFC §4.6 (signing `llms.txt` transitively pins every declared `tool_sha256`, alongside the core RFC's `sha256` for the prose). - **What the sandbox buys:** a malicious artifact can, at worst, compute and call its own origin — the same things any visitor's browser can already do to that origin. It cannot reach the runtime's credentials, other tenants, or other origins.\",\"okf_concept\":\"ext-executable-skills-4-security-considerations-25\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 4. Security considerations\"}},{\"id\":\"mcpwasm-readme-capability-support-by-runtime-41#0\",\"metadata\":{\"content\":\"Discovery, `tool_sha256` content-addressing, sandboxed `tool.js` execution, origin memory (`host.memorySearch`), skill recipes as MCP resources, and Ed25519 attestations work on **both** runtimes. One capability remains asymmetric, for a declared platform reason, not a bug:\",\"okf_concept\":\"mcpwasm-readme-capability-support-by-runtime-41\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Capability support by runtime\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-59#0\",\"metadata\":{\"content\":\"Third-party publishers (sites you do not control): see [`ONBOARDING.md`](./ONBOARDING.md) for the eligibility, review, attestation, activation, and revocation process.\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-59\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-38#0\",\"metadata\":{\"content\":\"A growing body of work attacks **tool-definition bloat** — the cost of injecting every tool's schema into the model context up front. It comes in two camps:\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-38\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-16#0\",\"metadata\":{\"content\":\"The Ed25519 model (above) requires pre-registering every reviewer's public key — a real bottleneck (today, only the maintainer is registered). Sigstore verifies **any** OIDC identity (a GitHub Actions workflow, a Google/GitHub login) without pre-coordination; the runtime's trust decision is which *identity* to require, not which *key* to whitelist — closer to what core RFC §4.6 recommends for identity-bound provenance.\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-16\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"ext-skill-attestations-4-runtime-behavior-23#0\",\"metadata\":{\"content\":\"- **Advisory** (default): all verified skills load; the verdict is exposed to the consumer (e.g. in the tool description or a discovery diagnostic) so agents/operators can decide. - **Enforcing**: only `attested` skills load; everything else is excluded like a hash mismatch.\",\"okf_concept\":\"ext-skill-attestations-4-runtime-behavior-23\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 4. Runtime behavior\"}},{\"id\":\"rfc-skills-in-llms-txt-2-2-optional-inline-metadata-19#0\",\"metadata\":{\"content\":\"A skill entry MAY carry a version hint as a trailing HTML comment:\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-2-optional-inline-metadata-19\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2.2 Optional inline metadata\"}},{\"id\":\"mcpwasm-readme-if-you-already-have-an-api-32#0\",\"metadata\":{\"content\":\"\\\"Zero infrastructure\\\" is literal for internal use — your own team pointing `npx @rckflr/mcpwasm` (or `--serve`) at your published skills needs no server on either side. For external clients to reach you without installing anything, you need one endpoint answering MCP over HTTP; that means either your origin gets added to an existing deployed gateway's `ALLOWED_ORIGINS`, or you `wrangler deploy` your own instance of the same generic gateway code in this repo, configured for your origin. Either way it is a one-time, tool-agnostic deploy — not a bespoke MCP server built per API.\",\"okf_concept\":\"mcpwasm-readme-if-you-already-have-an-api-32\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: If you already have an API\"}},{\"id\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-59#0\",\"metadata\":{\"content\":\"As agent runtimes evolve toward more proactive web discovery, the `## Skills` section provides the declaration primitive those systems will need.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-59\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5. On Agent-Side Discovery Triggers\"}},{\"id\":\"mcpwasm-readme-the-executable-skill-line-in-llms-txt-35#0\",\"metadata\":{\"content\":\"Under a `## Skills` section, an executable skill is a normal markdown list item followed by an HTML comment carrying a JSON object with `version`, `tool` (path to the `tool.js`), and `tool_sha256` (hex SHA-256 of the `tool.js` bytes):\",\"okf_concept\":\"mcpwasm-readme-the-executable-skill-line-in-llms-txt-35\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: The executable-skill line in `llms.txt`\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-71#0\",\"metadata\":{\"content\":\"**Why it matters:** It requires zero cooperation from the site (the file either exists or it doesn't). It costs one cheap HEAD request (~200 bytes). It works for every domain on the web, not just those that implement headers or DNS records.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-71\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"mcpwasm-readme-if-you-already-have-an-api-31#0\",\"metadata\":{\"content\":\"What you still have to write is **not** prose. A `tool.js` per action is real, small glue code: it validates `args` against the schema you declared, calls your existing API through `host.fetchOrigin`, and shapes the response — see `bookstore/content/create_order.tool.js` in this repo for a concrete example (validates `qty` and `book_id`, handles a 409 for insufficient stock as a distinct case, never lets a malformed call reach your backend). This is a different, stronger mechanism than a `SKILL.md` with no `tool.js`: prose-only skills are the core RFC's basic mode — an agent reads them and *improvises* the HTTP call with whatever generic request tool it has, with no schema validation, no sandbox, and no hash pinning.\",\"okf_concept\":\"mcpwasm-readme-if-you-already-have-an-api-31\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: If you already have an API\"}},{\"id\":\"mcpwasm-readme-skill-recipes-skill-md-as-mcp-resources-49#0\",\"metadata\":{\"content\":\"- **MCP resources** — `resources/list` / `resources/read`, uri `skill://<name>`, `text/markdown`; the capability is advertised in `initialize`. The semantically correct path for clients that support it. - **`get_skill_guide` tool** — a synthetic, runtime-provided (not sandboxed) tool returning the recipe by skill name. Universal fallback: every MCP client supports tools.\",\"okf_concept\":\"mcpwasm-readme-skill-recipes-skill-md-as-mcp-resources-49\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill recipes (SKILL.md) as MCP resources\"}},{\"id\":\"mcpwasm-readme-try-the-deployed-gateway-curl-63#0\",\"metadata\":{\"content\":\"List the skills the demo site publishes:\",\"okf_concept\":\"mcpwasm-readme-try-the-deployed-gateway-curl-63\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Try the deployed gateway (curl)\"}},{\"id\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-31#0\",\"metadata\":{\"content\":\"**[auth.md](https://github.com/workos/auth.md) (WorkOS).** A protocol where a service hosts an `AUTH.md` describing how an agent can register and authenticate on the user's behalf, discovered via `/.well-known/oauth-authorization-server` metadata. It is **complementary, not competing**, with this RFC:\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-31\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.1 Relationship to adjacent protocols\"}},{\"id\":\"ext-skill-attestations-4-runtime-behavior-24#0\",\"metadata\":{\"content\":\"Verification MUST be independently computable from static files: fetch `attestations.json`, verify Ed25519 (available in Workers/browsers via WebCrypto) against the registry. No callbacks to any service.\",\"okf_concept\":\"ext-skill-attestations-4-runtime-behavior-24\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 4. Runtime behavior\"}},{\"id\":\"mcpwasm-readme-skill-recipes-skill-md-as-mcp-resources-50#0\",\"metadata\":{\"content\":\"Failure semantics mirror everything else here, but the halves are **independent**: a missing/unfetchable/hash-mismatched `SKILL.md` omits the *recipe* (warned on stderr / console) while the *tool* — verified by its own `tool_sha256` — loads unaffected. Under `enforcing` attestation mode, an excluded skill's recipe is excluded with it. Size cap: `MAX_SKILLMD_BYTES` (default 256 KB). Covered by `npm run local` (verified recipe served, tampered recipe excluded, missing recipe tolerated).\",\"okf_concept\":\"mcpwasm-readme-skill-recipes-skill-md-as-mcp-resources-50\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill recipes (SKILL.md) as MCP resources\"}},{\"id\":\"rfc-skills-in-llms-txt-2-proposal-16#0\",\"metadata\":{\"content\":\"Add an optional `## Skills` section to `llms.txt`. Each entry is a link to a remote `SKILL.md` (or a skill bundle archive) that conforms to the Agent Skills spec.\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-proposal-16\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2. Proposal\"}},{\"id\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-32#1\",\"metadata\":{\"content\":\"5). The two `.well-known` documents can coexist. - **Terminology overlap.** `auth.md` uses the word \\\"skill\\\" for its auth manifest, distinct from an [Agent Skills](https:\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-32\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.1 Relationship to adjacent protocols\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-46#0\",\"metadata\":{\"content\":\"This is the same pattern already live on the reference site **[demoshop](https://demoshop-88e.pages.dev)**, whose skills (`product-search`, `cart-add`) teach an agent to call one REST endpoint directly — zero tools in context — for *simple* capabilities. The n8n POC shows the pattern **scales to a complex, tool-heavy capability**, and maps the cost of each rung.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-46\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"ext-skill-attestations-2-3-publication-13#0\",\"metadata\":{\"content\":\"(consistent with the core RFC's `/.well-known/agent-skills/index.json` metadata layer). Multiple attestations per skill (different reviewers) are allowed and encouraged. Third-party reviewers MAY additionally publish their attestations on their own domain; how runtimes discover those is an open question (§6).\",\"okf_concept\":\"ext-skill-attestations-2-3-publication-13\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.3 Publication\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-41#0\",\"metadata\":{\"content\":\"1. **Search finds; a skill *prescribes*.** Tool search reduces the token cost of *having* many tools, but it does not tell the agent the *order* to use them in, the dependencies, or what to do on error — the agent must still formulate a query (a cold-start problem) and discover the procedure, sometimes by failing. A published skill carries the **recipe**: the procedure at the level of intent. (n8n's own MCP concedes this — it ships an `instructions` blob telling the agent the call order, because the tool list alone is insufficient.) 2. **Trust + curation.** None of the above says *who* published a capability or verifies it; this RFC adds offline-key signatures + pinning (§4.6) and a human-curated procedure tier.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-41\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"rfc-skills-in-llms-txt-8-open-questions-90#0\",\"metadata\":{\"content\":\"URLs in `## Skills` MAY mirror those already declared in `.well-known/agent-skills/index.json`, reusing the same artifacts without duplication. Agents that support both get redundant coverage at zero extra cost for publishers.\",\"okf_concept\":\"rfc-skills-in-llms-txt-8-open-questions-90\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 8. Open Questions\"}},{\"id\":\"rfc-skills-in-llms-txt-1-3-the-well-known-convention-is-limited-10#0\",\"metadata\":{\"content\":\"It fails when: - The site wants to declare **multiple** skills for different use cases. - The agent encounters the site for the first time via `llms.txt`. - The site wants to co-locate skill discovery with the rest of its agent-facing context.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-3-the-well-known-convention-is-limited-10\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.3 The well-known convention is limited\"}},{\"id\":\"ext-executable-skills-1-motivation-4#0\",\"metadata\":{\"content\":\"Executing third-party code is only acceptable under a strict security model. This extension therefore defines two things, and only two things:\",\"okf_concept\":\"ext-executable-skills-1-motivation-4\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 1. Motivation\"}},{\"id\":\"rfc-skills-in-llms-txt-3-2-relationship-to-agents-txt-the-actio-36#0\",\"metadata\":{\"content\":\"- **Discovery surface.** `agents.txt` answers *\\\"which capability families does this site expose?\\\"* across many protocols at once; `## Skills` in `llms.txt` is the co-located, content-adjacent surface for the Skills family specifically. A site can serve both: a `Skills:` line in `agents.txt` and a `## Skills` section in `llms.txt`, pointing at the **same** `SKILL.md` artifacts. - **Trust.** `agents.txt`'s Skills layer carries discovery and integrity (`digest`) but **explicitly leaves authenticity out** (\\\"prioritize discovery simplicity over trust verification\\\"). That is exactly the gap this RFC's Tier 2 closes (§4.6): offline-key ed25519 signatures + agent-side key pinning. The two stack cleanly — `agents.txt`/agentskills.io for discovery and integrity, this RFC for authenticity.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-2-relationship-to-agents-txt-the-actio-36\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.2 Relationship to `agents.txt` (the action layer)\"}},{\"id\":\"mcpwasm-readme-mcpwasm-static-mcp-3#0\",\"metadata\":{\"content\":\"Think \\\"php-wasm, but for MCP tools\\\": the platform owner embeds the host, loads `tool.js` files, and each tool runs isolated in a QuickJS WebAssembly sandbox. The only bridge from the sandbox to the platform's internals is an explicit capability the host injects. No capability, no access.\",\"okf_concept\":\"mcpwasm-readme-mcpwasm-static-mcp-3\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: mcpwasm — Static MCP\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-52#0\",\"metadata\":{\"content\":\"A publisher may serve a third trust ring — signed reviewer attestations — at `/.well-known/agent-skills/attestations.json`. Each entry is an Ed25519 signature over the canonical payload and has this shape:\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-52\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-60#0\",\"metadata\":{\"content\":\"To reduce the friction of skill discovery from \\\"user must know to ask\\\" to \\\"agent finds it automatically\\\", this RFC proposes four optional mechanisms that sites MAY implement and agents MAY support. None are required for compliance, but each lowers the barrier for the agent side.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-60\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-83#0\",\"metadata\":{\"content\":\"The agent: 1. Did not fetch `https://img.automators.work/llms.txt`. 2. Did not discover the `placeholder` skill. 3. Did not learn that the site generates SVG placeholder images via `/600x50?bg=22c55e`. 4. Instead, created a local PNG with Python/PIL, ignoring the URL entirely.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-83\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.4 Discovery from User Instructions\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-44#0\",\"metadata\":{\"content\":\"| arm | tool defs in context | result | |---|---:|---| | raw MCP (n8n's own setup) | 25 | at a 4k context, the tools **do not even fit** (overflow before turn 1) | | skill + declared tool segment | 8 | runs; same task, −68% tool surface | | skill + 1 generic MCP passthrough | **1** | runs the full recipe, keeps node introspection | | skill + 1 generic HTTP tool (REST, no MCP) | **1** | builds and `POST`s the workflow in a single call |\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-44\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"rfc-skills-in-llms-txt-1-2-existing-alternatives-require-infras-7#0\",\"metadata\":{\"content\":\"- **Static sites** (Cloudflare Pages, GitHub Pages, Netlify, Vercel) — no server process exists. - **Existing APIs** that want to teach agents which endpoints to call, not reimplement their surface. - **Documentation sites** that want to describe interaction patterns once, not maintain a daemon.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-2-existing-alternatives-require-infras-7\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.2 Existing alternatives require infrastructure most sites do not have\"}},{\"id\":\"mcpwasm-readme-architecture-34#0\",\"metadata\":{\"content\":\"- `host.mjs` — synchronous `ToolHost` (sync tools, `host.callInternal` capability). - `host-async.mjs` — `AsyncToolHost` (async handlers, `host.fetchOrigin` + the `extraCapabilities` mechanism that backs `host.memorySearch`, resource hardening). - `mcp-core.mjs` / `mcp-core-async.mjs` — JSON-RPC 2.0 MCP core (transport-agnostic). - `worker.mjs` — PoC MCP server (sync host, inline tools). - `worker-spike.mjs` — async spike (fetchHome/fetchEvil). - `worker-gateway.mjs` + `llmstxt-parse.mjs` — the gateway. - `worker-memspike.mjs` — memory spike: the docs-site origin published and served through the gateway end-to-end (`host.memorySearch` over a BM25 snapshot), exercised by `mf-memspike.mjs`.\",\"okf_concept\":\"mcpwasm-readme-architecture-34\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Architecture\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-39#0\",\"metadata\":{\"content\":\"- **Model/client-side.** Anthropic's [Tool Search Tool](https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-search-tool) (`defer_loading`) loads only the few relevant tools on demand (reported ~85% fewer tool-definition tokens). Tool-retrieval / \\\"tool RAG\\\" (LangChain toolkits, Gorilla) does the same via embeddings. - **Server-side.** The MCP [progressive-disclosure SEP](https://github.com/modelcontextprotocol/modelcontextprotocol/issues/1888) proposes a server capability that exposes one meta-tool with `searchTools` modes instead of registering hundreds.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-39\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-42#0\",\"metadata\":{\"content\":\"So `llms.txt` Skills can be the **server-less catalog** these consumer-side mechanisms need a source for — Tool Search needs a catalog; the SEP needs the server to implement it; a `## Skills` section is that catalog, owned by the domain, with the recipe and the signature attached.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-42\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"ext-executable-skills-extension-executable-skills-0#0\",\"metadata\":{\"content\":\"**Status:** Draft (v0.5) **Date:** 2026-07-10 **Extends:** [RFC: Publishing Agent Skills through `llms.txt`](./rfc-skills-in-llms-txt.md) (v0.9)\",\"okf_concept\":\"ext-executable-skills-extension-executable-skills-0\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: Extension: Executable Skills\"}},{\"id\":\"ext-skill-attestations-4-runtime-behavior-21#0\",\"metadata\":{\"content\":\"**Precedence with multiple attestations:** `invalid` dominates — if any attestation for the skill carries a signature that FAILS verification against a *registered* key, the skill's verdict is `invalid` regardless of other valid attestations (a forged signature is a red flag, not noise; and since `attestations.json` is publisher-controlled, an attacker poisoning it can only block the publisher's own skills). Otherwise: any valid, in-window attestation → `attested`; else any expired one → `expired`; else `unattested`. Attestations from unregistered attesters are ignored for precedence (they are unverifiable, verdict input UNKNOWN-ATTESTER only as diagnostics).\",\"okf_concept\":\"ext-skill-attestations-4-runtime-behavior-21\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 4. Runtime behavior\"}},{\"id\":\"ext-executable-skills-1-motivation-1#0\",\"metadata\":{\"content\":\"The core RFC publishes skills as instructional documents (`SKILL.md`). Section 5.3 of the core RFC identifies the *execution gap*: an agent may read instructions and still improvise, substitute tools, or drift from the published behavior.\",\"okf_concept\":\"ext-executable-skills-1-motivation-1\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 1. Motivation\"}},{\"id\":\"rfc-skills-in-llms-txt-4-security-48#0\",\"metadata\":{\"content\":\"1. **No auto-installation.** Agents MUST NOT activate a discovered skill without explicit user opt-in. 2. **Same-origin preference.** Agents SHOULD treat same-origin skills as lower friction than cross-origin skills. 3. **Content verification.** If `sha256` is declared in the inline metadata, agents MUST verify the hash of the fetched file and refuse to load on mismatch. 4. **Cross-origin skills require elevated confirmation.** If a skill URL is on a different origin from the `llms.txt` that references it, agents SHOULD require additional user confirmation beyond the base opt-in. This prevents a compromised `llms.txt` from silently delegating trust to a third-party host. 5. **Least privilege.** Skills loaded from a domain operate within the permission scope of that domain. They cannot requ\",\"okf_concept\":\"rfc-skills-in-llms-txt-4-security-48\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 4. Security\"}},{\"id\":\"mcpwasm-readme-origin-memory-search-over-static-content-37#0\",\"metadata\":{\"content\":\"A publisher that wants its skills to search over its own static content (docs, catalog text, any corpus) declares a memory snapshot with a single HTML comment **before** the `## Skills` section (this ordering is required by the reference parser):\",\"okf_concept\":\"mcpwasm-readme-origin-memory-search-over-static-content-37\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Origin memory (search over static content)\"}},{\"id\":\"mcpwasm-readme-security-model-honest-68#0\",\"metadata\":{\"content\":\"- **Tool-host isolation.** Tool code runs in a QuickJS-wasm context separate from the Worker's JS. It sees only `registerTool`, `host`, and what the host prelude defines. No `fetch`, no `process`, no globals leak by default. - **Secrets stay outside the sandbox.** In the sync PoC, the platform secret is read from `env.STRIPE_SECRET` on the host side and is never exposed to tool code — the tool can only call named internal methods. In the gateway, there is no platform secret; the only capabilities are `host.fetchOrigin` and (when declared and verified) `host.memorySearch`. - **SHA-256 content addressing.** The gateway downloads `tool.js` and verifies it against the `tool_sha256` declared in `llms.txt` before loading. Mismatched or corrupt content is rejected and not cached.\",\"okf_concept\":\"mcpwasm-readme-security-model-honest-68\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Security model (honest)\"}},{\"id\":\"rfc-skills-in-llms-txt-1-4-why-structured-over-prose-15#0\",\"metadata\":{\"content\":\"The structure adds zero friction for publishers (two lines of markdown) and enables the tooling layer above it.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-4-why-structured-over-prose-15\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.4 Why structured over prose\"}},{\"id\":\"rfc-skills-in-llms-txt-8-open-questions-89#0\",\"metadata\":{\"content\":\"| Layer | Mechanism | Role | |---|---|---| | Discovery | `## Skills` in `llms.txt` | Passive, co-located with domain context; zero extra fetch | | Metadata & verification | `.well-known/agent-skills/index.json` | sha256, version, license, cost estimates | | Active install | `.well-known/skills/default/skill.md` | Single-skill convention probe |\",\"okf_concept\":\"rfc-skills-in-llms-txt-8-open-questions-89\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 8. Open Questions\"}},{\"id\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-17#0\",\"metadata\":{\"content\":\"- `await host.memorySearch(query, k?)` -> `{ hits: [{ text, score, title, concept_id }] }` — BM25 search over the origin's own snapshot. `query` MUST be a string; `k` is capped by the runtime (reference implementation: 10). Like `fetchOrigin`, the capability is strictly origin-scoped: a skill can only search the snapshot its own publisher shipped. Skills SHOULD check for the capability before calling it (`typeof host.memorySearch === \\\"function\\\"`) — it is absent when the runtime does not support the declared format or when verification fails.\",\"okf_concept\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-17\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.4 Origin memory: search snapshots\"}},{\"id\":\"ext-skill-attestations-2-4-choosing-an-attester-type-normative--16#0\",\"metadata\":{\"content\":\"Decision rule: **if your runtime can reach Sigstore's public trust infrastructure at verification time, use `sigstore:`; otherwise use `human:` Ed25519.** A registry MAY contain both types simultaneously (§3); runtimes evaluate each entry per its own type. Publishers do not choose — attester type is a property of who reviews and where verification runs, not of the published skill.\",\"okf_concept\":\"ext-skill-attestations-2-4-choosing-an-attester-type-normative--16\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.4 Choosing an attester type (normative guidance)\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-19#1\",\"metadata\":{\"content\":\"ified end-to-end against `bin/mcpwasm-local.mjs`. **Honest gap:** producing a *positive* fixture (a real Sigstore signature over this repo's own canonical payload, verifying as `attested`) needs a live OIDC signing flow this environment canno\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-19\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-45#0\",\"metadata\":{\"content\":\"The same workflow gets built with **one tool definition in context instead of 25**. The decisive, model-independent result is the 4k overflow: the raw tool list is *unusable* on a small model, while a skill's declared segment is not. (Honest scoping: once the tools *fit*, the orchestration gap among the MCP arms is marginal on an easy task with a capable model; and the leanest REST arm trades the SDK's parameter validation for speed. It is a single-model POC, not a benchmark — see its README.)\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-45\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-64#0\",\"metadata\":{\"content\":\"The domain MAY publish a DNS TXT record:\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-64\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-70#0\",\"metadata\":{\"content\":\"Similar to `robots.txt` or `favicon.ico`, agents MAY adopt the convention:\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-70\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-11#0\",\"metadata\":{\"content\":\"**What it does not change:** the reviewer registry (§3) still decides *which* identities are trusted — Sigstore does not eliminate the allowlist, it changes its unit from public keys to OIDC identity strings, and removes the private per-reviewer key-management burden (Fulcio issues short-lived certificates per signing operation instead).\",\"okf_concept\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-11\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.1b Alternative: Sigstore (keyless) attestation\"}},{\"id\":\"mcpwasm-readme-ci-71#0\",\"metadata\":{\"content\":\"The workflow in `.github/workflows/ci.yml` runs two jobs on every push and pull_request to `main`, both on `ubuntu-latest` with Node 22 and `npm ci` (with cache), timing out after 15 minutes.\",\"okf_concept\":\"mcpwasm-readme-ci-71\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: CI\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-21#0\",\"metadata\":{\"content\":\"Pointing the runtime at a raw GitHub URL does **not** work: `new URL(...).origin` keeps only scheme+host+port, so `https://raw.githubusercontent.com/you/repo/main/` collapses to `https://raw.githubusercontent.com` — the `you/repo/main` part (and therefore `/llms.txt`) is gone. `--serve` is the practical alternative: it starts an internal static file server (bound to `127.0.0.1` only, never exposed to the network) over a local directory — e.g. your own `git clone` of a skills repo — and uses that as the origin, combining \\\"serve this directory\\\" and \\\"connect to it\\\" into one command:\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-21\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"rfc-skills-in-llms-txt-8-open-questions-92#0\",\"metadata\":{\"content\":\"Candidate directions, deliberately unresolved: a `scope`/`project` field in the inline metadata that runtimes use to prefix tool names (`kdd__search_knowledge`); publisher-side name prefixes at generation time; multiple scoped `skills-memory` lines bound to skills by scope; or per-path discovery (contradicts origin-based discovery and the URL-origin model). Whatever the resolution, it must preserve content-addressing (hashes bind to bytes, so re-rooted aggregation today requires byte-identical artifacts).\",\"okf_concept\":\"rfc-skills-in-llms-txt-8-open-questions-92\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 8. Open Questions\"}},{\"id\":\"ext-skill-attestations-2-1-the-attestation-object-5#1\",\"metadata\":{\"content\":\"ore RFC tooling, `scripts/verify_signatures.py`). | | `note` | OPTIONAL | Human context; not part of\",\"okf_concept\":\"ext-skill-attestations-2-1-the-attestation-object-5\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.1 The attestation object\"}},{\"id\":\"rfc-skills-in-llms-txt-1-2-existing-alternatives-require-infras-6#0\",\"metadata\":{\"content\":\"MCP and A2A are the right tools for complex, stateful integrations. They are overkill for:\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-2-existing-alternatives-require-infras-6\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.2 Existing alternatives require infrastructure most sites do not have\"}},{\"id\":\"rfc-skills-in-llms-txt-3-ecosystem-comparison-29#0\",\"metadata\":{\"content\":\"**This RFC does not replace MCP or A2A.** It fills the gap below them: the case where a site wants to publish a skill for a simple API or interaction pattern, without running a server.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-ecosystem-comparison-29\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3. Ecosystem Comparison\"}},{\"id\":\"mcpwasm-readme-use-it-now-any-static-site-an-mcp-server-7#0\",\"metadata\":{\"content\":\"That is the whole path from a static site to a running MCP server. The two other ways to use mcpwasm are for when you specifically need them: the **hosted gateway** (multi-tenant, on Cloudflare Workers — serves many origins behind one endpoint, with an onboarding step per origin) and the **embeddable library** (`@rckflr/mcpwasm` — for building your own host). Both are documented below; the local runtime above needs none of it. Its honest limits and the `index.json`/attestation options are detailed under \\\"[Local MCP runtime](#local-mcp-runtime--no-gateway-at-all)\\\".\",\"okf_concept\":\"mcpwasm-readme-use-it-now-any-static-site-an-mcp-server-7\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Use it now — any static site → an MCP server\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-54#1\",\"metadata\":{\"content\":\"whose signature fails marks the skill `invalid`. - Computes a per-skill verdict with precedence **invalid > attested > expired > unattested** (`invalid` dominates): a matching attestation from a registered reviewer with a valid signature inside its `[signed_on, valid_u\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-54\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"ext-executable-skills-2-2-the-artifact-tool-js-12#0\",\"metadata\":{\"content\":\"- The file MUST call `registerTool(def)` exactly once, where `def.name` matches the skill name in `llms.txt`, `def.inputSchema` is a JSON Schema object, and `def.handler` is a function (sync or async). - The handler receives already-parsed `args` and returns a JSON-serializable value. Throwing reports a tool error to the caller; it MUST NOT crash the runtime. - The only ambient capability available is `host.fetchOrigin(path, opts?)`: an HTTP fetch **restricted to the publishing origin**, returning `{ status, body }` (body as text, possibly truncated by the runtime). Relative paths are resolved against the publishing origin. `opts` is optional: `{ method?: \\\"GET\\\" | \\\"POST\\\", body?: string, contentType?: string }`. The `method` defaults to `\\\"GET\\\"` if omitted. Only `GET` and `POST` are valid (an\",\"okf_concept\":\"ext-executable-skills-2-2-the-artifact-tool-js-12\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.2 The artifact (`tool.js`)\"}},{\"id\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-82#0\",\"metadata\":{\"content\":\"**Empirical evidence from img.automators.work:** A user asked:\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-82\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.4 Discovery from User Instructions\"}},{\"id\":\"rfc-skills-in-llms-txt-skills-17#0\",\"metadata\":{\"content\":\"- [skill-name](https://example.com/skills/skill-name/SKILL.md): description of when to use this skill. - [bundle-name](https://example.com/skills/bundle-name.zip): description. <!-- skill: {\\\"version\\\":\\\"1.0.0\\\"} --> ```\",\"okf_concept\":\"rfc-skills-in-llms-txt-skills-17\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: Skills\"}},{\"id\":\"ext-executable-skills-1-motivation-6#0\",\"metadata\":{\"content\":\"Everything else — transport, agent UX, approval flows — remains governed by the core RFC. A publisher that adopts this extension remains fully conformant with the core RFC: agents that do not understand executable skills simply fall back to `SKILL.md` prose.\",\"okf_concept\":\"ext-executable-skills-1-motivation-6\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 1. Motivation\"}},{\"id\":\"mcpwasm-readme-security-model-honest-68#1\",\"metadata\":{\"content\":\"ed or corrupt content is rejected and not cached. The same rule ap\",\"okf_concept\":\"mcpwasm-readme-security-model-honest-68\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Security model (honest)\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-19#0\",\"metadata\":{\"content\":\"Verified against a **real, live, publicly fetched** Sigstore bundle (the SLSA provenance attestation for the `sigstore@5.0.0` npm package's own publish, `https://registry.npmjs.org/-/npm/v1/attestations/sigstore@5.0.0`) — proving the underlying Fulcio cert-chain + Rekor transparency-log verification genuinely runs and succeeds, and that a schema-mismatched or wrong-identity bundle is correctly rejected. All 6 `--require-attestation` rejection paths (no attestations.json, empty array, no matching skill, malformed date, expired, invalid/empty bundle) verified end-to-end against `bin/mcpwasm-local.mjs`.\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-19\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"mcpwasm-readme-mcpwasm-static-mcp-1#0\",\"metadata\":{\"content\":\"**Static MCP: your tools are files, not servers.** Tools are published as static, hash-verified content and executed sandboxed on demand. What static site hosting did to web servers — \\\"don't run Apache, publish HTML\\\" — this does to MCP servers: don't run an MCP server, publish files. The publisher needs zero infrastructure; the MCP server is materialized per request from those files and evaporates after responding (ephemeral instance, durable definition).\",\"okf_concept\":\"mcpwasm-readme-mcpwasm-static-mcp-1\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: mcpwasm — Static MCP\"}},{\"id\":\"mcpwasm-readme-skills-39#1\",\"metadata\":{\"content\":\"discarded and **the capability is not injected**. - When the snapshot verifies, the runtime injects `host.memorySearch(query, k?)` into every skill of that origin (via the `extraCapabilities` bridge in `AsyncToolHost`, same raw-JSON asyncify pattern as `host.fetch\",\"okf_concept\":\"mcpwasm-readme-skills-39\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skills\"}},{\"id\":\"rfc-skills-in-llms-txt-8-open-questions-91#1\",\"metadata\":{\"content\":\"aggregates skills for MULTIPLE projects — e.g. a GitHub user/org ROOT site bridging several project pages, since project-page origins are not directly consumable (a URL's origin strips the path) — hits two hard limits, found empirically while aggregating two real publishers\",\"okf_concept\":\"rfc-skills-in-llms-txt-8-open-questions-91\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 8. Open Questions\"}},{\"id\":\"rfc-skills-in-llms-txt-tl-dr-1#0\",\"metadata\":{\"content\":\"Añade una sección `## Skills` a tu `llms.txt`. Cada entrada es un link a un `SKILL.md` remoto. Los agentes descubren skills en el mismo documento que ya leen para entender tu sitio. Sin servidor, sin proceso persistente, sin autenticación extra.\",\"okf_concept\":\"rfc-skills-in-llms-txt-tl-dr-1\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: TL;DR\"}},{\"id\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-54#0\",\"metadata\":{\"content\":\"**Reference implementation.** `scripts/generate.py` signs with ed25519 (deterministic, RFC 8032); `scripts/verify_signatures.py` verifies each `SKILL.md` against the published public key.\",\"okf_concept\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-54\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 4.6 Signing and authenticity (resolves Open Question 4)\"}},{\"id\":\"rfc-skills-in-llms-txt-1-2-existing-alternatives-require-infras-8#0\",\"metadata\":{\"content\":\"The reference implementation for this RFC (`img.automators.work`) is a **Cloudflare Pages static site**. It cannot run an MCP server. It *can* serve a text file.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-2-existing-alternatives-require-infras-8\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.2 Existing alternatives require infrastructure most sites do not have\"}},{\"id\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-14#0\",\"metadata\":{\"content\":\"A publisher MAY additionally ship a **search snapshot**: a pre-built, hash-pinned full-text index over its own content, so that skills can offer real search over a purely static site — no backend API required. Declared with one origin-level line in `llms.txt` (place it **before** the `## Skills` section — some conformant parsers fold trailing non-list lines into the last skill entry):\",\"okf_concept\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-14\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.4 Origin memory: search snapshots\"}},{\"id\":\"ext-skill-attestations-1-motivation-2#0\",\"metadata\":{\"content\":\"This extension adds the third ring: **attestations** — signed, expiring statements by named reviewers that a specific artifact, on a specific origin, was reviewed and endorsed. Two design commitments inherited from the donor PoC:\",\"okf_concept\":\"ext-skill-attestations-1-motivation-2\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 1. Motivation\"}},{\"id\":\"mcpwasm-readme-capability-support-by-runtime-43#0\",\"metadata\":{\"content\":\"Consequence: the **local runtime is the full-featured reference** — memory and Sigstore both work there. The gateway matches it except for Sigstore verification, where its attestation model remains Ed25519-only (platform limitation of Workers, documented in [Sigstore](#sigstore-attestations---require-attestation-local-runtime-only) above).\",\"okf_concept\":\"mcpwasm-readme-capability-support-by-runtime-43\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Capability support by runtime\"}},{\"id\":\"rfc-skills-in-llms-txt-1-2-existing-alternatives-require-infras-5#0\",\"metadata\":{\"content\":\"| Mechanism | What it requires | Works for a static blog? | |---|---|---| | **MCP** | A persistent server process, transport layer, endpoint | **No** | | **A2A** | A persistent server process, agent-to-agent protocol | **No** | | **`/.well-known/skills/`** | Static file serving only | **Yes, but only one skill** |\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-2-existing-alternatives-require-infras-5\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.2 Existing alternatives require infrastructure most sites do not have\"}},{\"id\":\"rfc-skills-in-llms-txt-10-changelog-95#1\",\"metadata\":{\"content\":\"rections listed, unresolved. No normative change. - **v0.8 (2026-06-02):** Extended §3.3 with the `n8n-skills-sdk` companion experime\",\"okf_concept\":\"rfc-skills-in-llms-txt-10-changelog-95\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 10. Changelog\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-53#0\",\"metadata\":{\"content\":\"The signed payload is the UTF-8 bytes of `origin + \\\"\\\\n\\\" + skill + \\\"\\\\n\\\" + tool_sha256 + \\\"\\\\n\\\" + signed_on + \\\"\\\\n\\\" + valid_until` with `origin` canonical (lowercase, no trailing slash, no default port) and `tool_sha256` lowercase hex. The gateway:\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-53\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"ext-executable-skills-3-runtime-requirements-24#1\",\"metadata\":{\"content\":\"gin` target that resolves outside that origin. 3. **Resource limits.** SHOULD enforce memory, stack, and execution budgets per invocation, so a hostil\",\"okf_concept\":\"ext-executable-skills-3-runtime-requirements-24\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 3. Runtime requirements\"}},{\"id\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-50#0\",\"metadata\":{\"content\":\"- **Tier 1 — Integrity (`sha256`).** MUST be verified when declared. Cheap, offline, no key management. Already specified in §2.2 and §4.3. - **Tier 2 — Authenticity (signature).** OPTIONAL. The publisher signs each `SKILL.md` with a private key kept **offline, off the web server**. The public key is published (e.g., `/.well-known/agent-skills/signing-key.pub`, and/or a `signing_key` field in `index.json`). Agents verify the signature over the skill content.\",\"okf_concept\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-50\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 4.6 Signing and authenticity (resolves Open Question 4)\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-65#0\",\"metadata\":{\"content\":\"**Why it matters:** DNS resolution is often faster than HTTP and can be cached at the resolver. The agent can discover the `llms.txt` URL *before* making any HTTP request, allowing it to preload skills or show them to the user at the exact moment the domain is mentioned.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-65\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"rfc-skills-in-llms-txt-5-2-recommended-agent-behavior-non-norma-75#0\",\"metadata\":{\"content\":\"**Cache strategy:** - `llms.txt`: cache per HTTP `Cache-Control` (typically short TTL, e.g., 5 minutes). - Negative 404: cache for 24 hours to avoid repeated probes. - `SKILL.md`: cache per HTTP `Cache-Control` (can be long, e.g., immutable).\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-2-recommended-agent-behavior-non-norma-75\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.2 Recommended Agent Behavior (Non-Normative)\"}},{\"id\":\"ext-executable-skills-4-security-considerations-25#1\",\"metadata\":{\"content\":\"e's credentials, other tenants, or other origins. - **Residual risks:** a compromised publisher origin can still publish a *correctly hashed* malicious artifact (garbage in, verified garbage out); `fetch\",\"okf_concept\":\"ext-executable-skills-4-security-considerations-25\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 4. Security considerations\"}},{\"id\":\"mcpwasm-readme-use-as-a-library-npm-8#0\",\"metadata\":{\"content\":\"The embeddable host — what the gateway itself builds on — ships as [`@rckflr/mcpwasm`](https://www.npmjs.com/package/@rckflr/mcpwasm):\",\"okf_concept\":\"mcpwasm-readme-use-as-a-library-npm-8\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Use as a library (npm)\"}},{\"id\":\"mcpwasm-readme-try-the-deployed-gateway-curl-62#0\",\"metadata\":{\"content\":\"The gateway is live at `https://llmstxt-gateway.rckflr.workers.dev`. It is restricted to origins in its allowlist; the demo site `https://llmstxt-demo-site.rckflr.workers.dev`, the bookstore `https://llmstxt-bookstore.rckflr.workers.dev` (D1-backed, includes a write skill `create_order`), and the docs-site `https://llmstxt-docs.rckflr.workers.dev` (origin memory / BM25) are allowed. `origin` is URL-encoded as a query param. **The deployed gateway has auth enabled:** every request below needs `-H \\\"Authorization: Bearer <AUTH_TOKEN>\\\"` (the `AUTH_TOKEN` secret; 401 otherwise). The token is a secret — it is not in this repo.\",\"okf_concept\":\"mcpwasm-readme-try-the-deployed-gateway-curl-62\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Try the deployed gateway (curl)\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-13#0\",\"metadata\":{\"content\":\"#### Cross-checking against `index.json`\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-13\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-85#0\",\"metadata\":{\"content\":\"**Implications:** - Skill discovery is not triggered by implicit references in user prompts. - Even Mechanism D (the `/llms.txt` convention probe) only works if the agent is already navigating the domain, not when the domain is merely mentioned in text. - This suggests that either: - Agents need a pre-action step: \\\"before answering any task involving a domain, probe its `llms.txt`\\\", or - User instructions need an explicit signal (e.g., \\\"use the skill at...\\\") to trigger discovery.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-85\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.4 Discovery from User Instructions\"}},{\"id\":\"ext-skill-attestations-2-4-choosing-an-attester-type-normative--14#0\",\"metadata\":{\"content\":\"Both attester types are normative and fully supported. This section only sets the **default recommendation**, so adopters face one decision instead of two parallel models:\",\"okf_concept\":\"ext-skill-attestations-2-4-choosing-an-attester-type-normative--14\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.4 Choosing an attester type (normative guidance)\"}},{\"id\":\"ext-skill-attestations-5-security-considerations-26#1\",\"metadata\":{\"content\":\"— short validity windows are the honest stopgap. - **Expiry is a feature.** An expired attestation returning to `unattested` is the freshness layer doing its job: endorsements ro\",\"okf_concept\":\"ext-skill-attestations-5-security-considerations-26\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 5. Security considerations\"}},{\"id\":\"mcpwasm-readme-try-the-deployed-gateway-curl-66#0\",\"metadata\":{\"content\":\"The other deployed workers (their root path returns 404 by design — only specific routes like `/llms.txt` are served):\",\"okf_concept\":\"mcpwasm-readme-try-the-deployed-gateway-curl-66\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Try the deployed gateway (curl)\"}},{\"id\":\"ext-executable-skills-3-runtime-requirements-24#0\",\"metadata\":{\"content\":\"1. **Integrity.** MUST fetch the artifact, compute SHA-256 over the exact received bytes, and compare with the declared `tool_sha256`. On mismatch the skill MUST be excluded (not degraded to prose, not executed) and the rejection SHOULD be observable (log or diagnostic surface). This matches the mandatory-refusal language of core RFC §4. 2. **Isolation.** MUST execute artifacts in a sandbox where the host environment is not reachable: no ambient network, no filesystem, no host secrets. Host capabilities are injected explicitly; this extension defines only `host.fetchOrigin`, scoped to the publishing origin. A runtime MUST reject any `fetchOrigin` target that resolves outside that origin. 3.\",\"okf_concept\":\"ext-executable-skills-3-runtime-requirements-24\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 3. Runtime requirements\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-61#0\",\"metadata\":{\"content\":\"Every HTTP response from the domain MAY include a `Link` header pointing to `llms.txt`:\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-61\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"ext-executable-skills-5-reference-implementation-26#0\",\"metadata\":{\"content\":\"Working end-to-end chain (all deployed):\",\"okf_concept\":\"ext-executable-skills-5-reference-implementation-26\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 5. Reference implementation\"}},{\"id\":\"rfc-skills-in-llms-txt-1-1-two-ecosystems-that-do-not-talk-to-e-4#0\",\"metadata\":{\"content\":\"**Concrete example:** `img.automators.work` serves an SVG placeholder image API. It has a `llms.txt` that documents the API. It also has a `SKILL.md` that teaches agents how to build placeholder URLs. But an agent that reads `llms.txt` has **no signal** that the `SKILL.md` exists. The user must know the URL in advance.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-1-two-ecosystems-that-do-not-talk-to-e-4\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.1 Two ecosystems that do not talk to each other\"}},{\"id\":\"mcpwasm-readme-static-mcp-vs-a-traditional-mcp-server-28#0\",\"metadata\":{\"content\":\"| | Static MCP — local (`npx @rckflr/mcpwasm`) | Static MCP — gateway | Traditional MCP server | |---|---|---|---| | What the publisher ships | Static files: `llms.txt` + `tool.js` (+ `SKILL.md`) | Same static files | A running server process (any language) | | Infrastructure the publisher operates | None — GitHub Pages, R2, any static host | None — same | The whole server: uptime, scaling, patching, secrets | | Where the tool code runs | Your own machine, in a QuickJS-wasm sandbox | The gateway Worker, in the same sandbox | The publisher's own process, natively, no isolation | | Integrity guarantee | SHA-256 verified before any byte executes | Same | None built in — you trust the deployed binary/image as-is | | Third trust ring (human review) | Not enforced (v1 limit — trust i\",\"okf_concept\":\"mcpwasm-readme-static-mcp-vs-a-traditional-mcp-server-28\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Static MCP vs. a traditional MCP server\"}},{\"id\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-30#0\",\"metadata\":{\"content\":\"The core idea here — *a Markdown manifest hosted at your domain that agents read, with no server process* — is being arrived at independently by others, which is a useful signal that the pattern is sound.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-30\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.1 Relationship to adjacent protocols\"}},{\"id\":\"rfc-skills-in-llms-txt-rfc-publishing-agent-skills-through-llms-0#0\",\"metadata\":{\"content\":\"- **Status:** Draft (v0.10) - **Date:** 2026-06-02 - **Author:** automators.work - **Depends on:** [llmstxt.org](https://llmstxt.org/) spec, [Agent Skills](https://agentskills.io) (`SKILL.md`) - **Reference implementation:** [img.automators.work](https://img.automators.work)\",\"okf_concept\":\"rfc-skills-in-llms-txt-rfc-publishing-agent-skills-through-llms-0\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: RFC: Publishing Agent Skills through `llms.txt`\"}},{\"id\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-16#0\",\"metadata\":{\"content\":\"When a supported snapshot is declared, verified, and loaded, the runtime injects one additional capability into that origin's skills:\",\"okf_concept\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-16\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.4 Origin memory: search snapshots\"}},{\"id\":\"mcpwasm-readme-why-26#0\",\"metadata\":{\"content\":\"- The tool runs in a separate QuickJS-wasm context with no host globals beyond what the host predefines (`registerTool`, `host`). No `fetch`, no `process`, no disk, no secrets. - The platform secret (e.g. a Stripe key) stays on the host side. The tool can only ask the host to perform a *named* internal action via `host.callInternal` (sync host) or a scoped `host.fetchOrigin` (async host). The host decides what is allowed. - The tool's `tool.js` is content-addressed by SHA-256; the gateway refuses to load it if the hash declared in `llms.txt` does not match the bytes it fetched. - Resource limits bound what a malicious/buggy tool can do: memory cap, stack cap, a deterministic gas budget (interrupt-handler invocation count), and a wall-clock fetch deadline per call.\",\"okf_concept\":\"mcpwasm-readme-why-26\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Why\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-54#0\",\"metadata\":{\"content\":\"- Fetches `attestations.json` during discovery (only when attestations are not `off`). A 404 or malformed array means \\\"no attestations\\\" (every skill is `unattested`), not a discovery error. - Verifies each signature with WebCrypto (`Ed25519` via `crypto.subtle`, public key imported raw) against the runtime-side reviewer registry `REVIEWERS` (a `REVIEWERS` var in `wrangler-gateway.toml` mapping `attester → { public_key: <base64 raw 32 bytes>, registered_at }`). An attester not in the registry is ignored; a registered attester whose signature fails marks the skill `invalid`.\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-54\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"ext-skill-attestations-3-reviewer-registry-trust-anchor-17#0\",\"metadata\":{\"content\":\"Verification requires knowing reviewers' public keys. The registry is **runtime-side configuration**, not publisher data — the publisher hosting the keys that vouch for the publisher would be circular:\",\"okf_concept\":\"ext-skill-attestations-3-reviewer-registry-trust-anchor-17\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 3. Reviewer registry (trust anchor)\"}},{\"id\":\"rfc-skills-in-llms-txt-6-why-this-is-worth-doing-86#1\",\"metadata\":{\"content\":\"cument, in one fetch, with no additional probing. - **Multi-skill support.** A single domain can publish skills for different use cases (e.g., read-only queries, authenticated writes, admin operations) as separate entrie\",\"okf_concept\":\"rfc-skills-in-llms-txt-6-why-this-is-worth-doing-86\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 6. Why This Is Worth Doing\"}},{\"id\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-77#0\",\"metadata\":{\"content\":\"**Empirical evidence from DemoShop:** In a live test, an agent: 1. Discovered the `cart-add` skill via `llms.txt`. 2. Downloaded and read `SKILL.md`, which specified a Python `urllib` pattern with explicit headers and error handling. 3. Ignored the prescribed pattern and used its generic PowerShell `Invoke-WebRequest` tool instead.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-77\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.3 Observed Execution Gap\"}},{\"id\":\"rfc-skills-in-llms-txt-7-non-goals-87#0\",\"metadata\":{\"content\":\"- Replacing local skill filesystems or marketplaces — both remain valid distribution modes. - Defining a new skill format — this RFC reuses the Agent Skills `SKILL.md` spec as-is. - Mandating skill *execution* behavior — that is the agent runtime's responsibility. - Replacing MCP or A2A for complex, stateful integrations — this RFC targets the simpler, static-hosting case.\",\"okf_concept\":\"rfc-skills-in-llms-txt-7-non-goals-87\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 7. Non-Goals\"}},{\"id\":\"ext-executable-skills-2-1-skill-entry-7#0\",\"metadata\":{\"content\":\"An executable skill is declared with two additional keys in the skill entry's JSON comment:\",\"okf_concept\":\"ext-executable-skills-2-1-skill-entry-7\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.1 Skill entry\"}},{\"id\":\"mcpwasm-readme-mcpwasm-static-mcp-0#0\",\"metadata\":{\"content\":\"[![CI](https://github.com/MauricioPerera/mcpwasm/actions/workflows/ci.yml/badge.svg)](https://github.com/MauricioPerera/mcpwasm/actions/workflows/ci.yml) [![npm](https://img.shields.io/npm/v/%40rckflr%2Fmcpwasm)](https://www.npmjs.com/package/@rckflr/mcpwasm)\",\"okf_concept\":\"mcpwasm-readme-mcpwasm-static-mcp-0\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: mcpwasm — Static MCP\"}},{\"id\":\"mcpwasm-readme-static-mcp-vs-a-traditional-mcp-server-28#1\",\"metadata\":{\"content\":\"human review) | Not enforced (v1 limit — trust is your choic\",\"okf_concept\":\"mcpwasm-readme-static-mcp-vs-a-traditional-mcp-server-28\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Static MCP vs. a traditional MCP server\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-55#0\",\"metadata\":{\"content\":\"- `off` (default when `ATTESTATION_MODE` is unset) — attestations are not fetched; behavior is the pre-T25 gateway. - `advisory` — everything loads; verdicts are visible but do not exclude. - `enforcing` (deployed since T45) — only `attested` skills load; non-`attested` skills are excluded exactly like a `tool_sha256` mismatch (logged, not registered).\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-55\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"ext-skill-attestations-7-changelog-29#1\",\"metadata\":{\"content\":\"sting OIDC identities instead of pre-shared keys. Updated §3 (reviewer registry entry shape for `sigstore:` attesters) and\",\"okf_concept\":\"ext-skill-attestations-7-changelog-29\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 7. Changelog\"}},{\"id\":\"mcpwasm-readme-if-you-already-have-an-api-31#1\",\"metadata\":{\"content\":\"hema validation, no sandbox, and no hash pinning. That is the \\\"execution gap\\\" that executable skills (this repo's reference\",\"okf_concept\":\"mcpwasm-readme-if-you-already-have-an-api-31\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: If you already have an API\"}},{\"id\":\"rfc-skills-in-llms-txt-5-2-recommended-agent-behavior-non-norma-74#0\",\"metadata\":{\"content\":\"**Key principle:** The user opt-in (step 6) remains mandatory. Even with automatic discovery, agents MUST NOT auto-install skills without explicit approval.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-2-recommended-agent-behavior-non-norma-74\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.2 Recommended Agent Behavior (Non-Normative)\"}},{\"id\":\"mcpwasm-readme-architecture-33#1\",\"metadata\":{\"content\":\"uickJS asyncify suspends/resumes the wasm stack). An origin that declares a verified memory snapshot addition\",\"okf_concept\":\"mcpwasm-readme-architecture-33\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Architecture\"}},{\"id\":\"mcpwasm-readme-the-executable-skill-line-in-llms-txt-36#0\",\"metadata\":{\"content\":\"- The `<!-- skill: {...} -->` comment marks the line as an *executable* skill. List items without it are treated as descriptive-only and ignored by the gateway. - `tool` is resolved relative to the origin. - `tool_sha256` is verified against the fetched `tool.js` bytes before the tool is loaded. Mismatch → the skill is rejected (logged) and not registered. - If the JSON is invalid the line is silently skipped (no throw).\",\"okf_concept\":\"mcpwasm-readme-the-executable-skill-line-in-llms-txt-36\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: The executable-skill line in `llms.txt`\"}},{\"id\":\"mcpwasm-readme-try-the-deployed-gateway-curl-62#1\",\"metadata\":{\"content\":\"The token is a secret — it is not in this repo. The deployed gateway can also run in **per-client mode** (the `CLIENTS` secret), in which case each client sends its own `Authorization: Bearer <client_token>` with the sa\",\"okf_concept\":\"mcpwasm-readme-try-the-deployed-gateway-curl-62\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Try the deployed gateway (curl)\"}},{\"id\":\"mcpwasm-readme-skills-47#1\",\"metadata\":{\"content\":\"s the line non-executable (reported, not loaded). A public-name collision (two lines\",\"okf_concept\":\"mcpwasm-readme-skills-47\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skills\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-62#0\",\"metadata\":{\"content\":\"**Why it matters:** When the agent makes its *first* request to the domain (e.g., loading the homepage or calling an API endpoint), it receives this header for free. No extra round-trip is needed to probe for `llms.txt`. The agent parses the header, fetches `llms.txt`, and discovers skills before the user even asks.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-62\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"mcpwasm-readme-quick-start-61#0\",\"metadata\":{\"content\":\"`npm run gateway` is documented as-is from `package.json`; it builds the gateway worker and runs `mf-gateway.mjs` against the real deployed demo site. `npm run memspike` does the same for the memory capability: `build-memsnapshot.mjs` → `build-memspike.mjs` → `mf-memspike.mjs`.\",\"okf_concept\":\"mcpwasm-readme-quick-start-61\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Quick start\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-68#0\",\"metadata\":{\"content\":\"**Why it matters:** It is more discoverable by HTML parsers than `rel=\\\"alternate\\\"` (which some agents ignore). It also signals to developers inspecting the page source that the site exposes agent-facing resources.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-68\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"ext-executable-skills-7-changelog-30#0\",\"metadata\":{\"content\":\"- **v0.5 (2026-07-10):** Scopes for multi-project origins (SS2.5, resolves core RFC OQ6): optional `scope` key per skill line -> runtime-side tool prefixing `<scope>__<name>` (published bytes and hashes untouched, preserving the universal-template property); multiple `skills-memory` lines, one per scope, each binding its snapshot to its scope's skills; public-name collision -> skip with diagnostic (first wins); attestations unaffected. Fully backward compatible: no `scope` -> v0.4 behavior. - **v0.4 (2026-07-02):** Origin memory (§2.4): hash-pinned search snapshots (`skills-memory` line, `snapshot_sha256`, pluggable `format`) and the origin-scoped `host.memorySearch` capability; runtime requirement for snapshot integrity.\",\"okf_concept\":\"ext-executable-skills-7-changelog-30\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 7. Changelog\"}},{\"id\":\"ext-executable-skills-2-5-scopes-multi-project-origins-21#1\",\"metadata\":{\"content\":\"without `scope` get the unscoped memory (if any). A\",\"okf_concept\":\"ext-executable-skills-2-5-scopes-multi-project-origins-21\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.5 Scopes: multi-project origins\"}},{\"id\":\"ext-executable-skills-5-reference-implementation-27#0\",\"metadata\":{\"content\":\"| Piece | URL | |---|---| | Runtime + gateway source (QuickJS-wasm sandbox on Cloudflare Workers) | https://github.com/MauricioPerera/mcpwasm | | Demo publishing site (`llms.txt` with two executable skills) | https://llmstxt-demo-site.rckflr.workers.dev/llms.txt | | Realistic publisher (D1-backed bookstore: search/detail/stock/**order** skills, plus permanent robustness fixtures: a deliberately hash-mismatched skill and an infinite-loop skill) | https://llmstxt-bookstore.rckflr.workers.dev/llms.txt | | Docs publisher (the standard's own docs, searchable via a hash-pinned BM25 snapshot + `search_spec` skill — the spec searches itself) | https://llmstxt-docs.rckflr.workers.dev/llms.txt | | Gateway exposing them as an MCP server | `POST https://llmstxt-gateway.rckflr.workers.dev/mcp?origin=\",\"okf_concept\":\"ext-executable-skills-5-reference-implementation-27\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 5. Reference implementation\"}},{\"id\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-33#0\",\"metadata\":{\"content\":\"**General principle.** As more agent-facing Markdown conventions appear (`llms.txt`, `## Skills`, `AUTH.md`, `.well-known/*`), the risk is fragmentation. This RFC's position: `llms.txt` is the natural **co-located discovery layer** — an agent already reading it to understand a domain should find capability and pointer information there in one fetch, with `.well-known` and protocol-specific manifests (like `AUTH.md`) as the verification and specialized layers beneath it.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-33\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.1 Relationship to adjacent protocols\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-17#0\",\"metadata\":{\"content\":\"When set, discovery additionally fetches `/.well-known/agent-skills/attestations.json` and, for **every** skill, requires a matching entry (`origin` + `skill` + `tool_sha256`) whose `sigstore_bundle` verifies against exactly that `issuer|identity` pair, within its `[signed_on, valid_until]` window. A skill without one — absent attestations.json, no matching entry, expired, or an invalid/mismatched bundle — is excluded, same treatment as a `tool_sha256` mismatch. This flag is fail-closed by design: it is opt-in, but once set, absence of a valid attestation is *not* tolerated (unlike the gateway's `advisory` mode).\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-17\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"ext-executable-skills-1-motivation-2#0\",\"metadata\":{\"content\":\"This extension closes that gap for publishers who want it: a skill MAY additionally ship an **executable artifact** — a small JavaScript file with a declared input schema — that a conforming runtime executes *verbatim* inside a sandbox, instead of asking a model to improvise from prose.\",\"okf_concept\":\"ext-executable-skills-1-motivation-2\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 1. Motivation\"}},{\"id\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-78#0\",\"metadata\":{\"content\":\"The skill was treated as \\\"interesting documentation\\\" rather than as a \\\"behavioral contract\\\" that overrides the agent's default tools. The API call succeeded, but the agent did not follow the skill's intent: it used different tooling, different error handling, and different conventions than those the site designed.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-78\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.3 Observed Execution Gap\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-57#0\",\"metadata\":{\"content\":\"- `node scripts/attest.mjs keygen` — generates an Ed25519 pair, writes the private key to `.attester-key.json` and prints **only the public key** (base64 raw 32 bytes) for the `REVIEWERS` registry. - `node scripts/attest.mjs sign <origin> <skill> <valid_until>` — fetches the origin's live `llms.txt`, reads the real `tool_sha256` for the skill, signs, and prints the attestation object JSON.\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-57\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"ext-executable-skills-2-5-scopes-multi-project-origins-22#0\",\"metadata\":{\"content\":\"Aggregation recipe (informative): to republish another project's skills from a root origin, copy each skill line verbatim, re-root its `url`/`tool` paths (`/project/...`), add `scope`, and add the project's `skills-memory` line with the same `scope` and re-rooted `snapshot` path. All hashes remain valid because the served bytes are identical. Beware tools with baked root-relative fetch paths (e.g. a generated `get_concept`): they only work on the origin they were built for and SHOULD be omitted from aggregation.\",\"okf_concept\":\"ext-executable-skills-2-5-scopes-multi-project-origins-22\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.5 Scopes: multi-project origins\"}},{\"id\":\"ext-skill-attestations-8-reference-implementation-28#0\",\"metadata\":{\"content\":\"[mcpwasm](https://github.com/MauricioPerera/mcpwasm)'s local runtime (`bin/mcpwasm-local.mjs`, `sigstore-attest.mjs`) implements §2.1b: opt-in via `--require-attestation <issuer>|<identity>`, verified end-to-end against a real, live, publicly fetched Sigstore bundle (an npm package's own SLSA provenance attestation) proving the Fulcio/Rekor verification path genuinely works, plus all documented rejection paths (no attestations.json, no matching entry, expired, malformed dates, invalid/mismatched bundle). The gateway (`worker-gateway.mjs`) still implements only the Ed25519 model — confirmed during development that Sigstore's TUF-based trust-root cache needs filesystem access Cloudflare Workers does not have (§4).\",\"okf_concept\":\"ext-skill-attestations-8-reference-implementation-28\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 8. Reference implementation\"}},{\"id\":\"mcpwasm-readme-ci-72#0\",\"metadata\":{\"content\":\"The `hermetic` job is the gate. It runs five local suites — `npm test`, `npm run spike`, `npm run memspike`, `npm run gateway:offline`, `npm run local` — each preceded by its own build. None of these touch the network beyond `npm` itself: `test`, `spike`, `memspike`, and `local` are fully local (the last spawns the stdio runtime against an in-process fake publisher on `127.0.0.1`), and `gateway:offline` is the hermetic mode of the gateway suite (T35), where the production workers are replaced by in-process fakes served through the same URL-to-binding map the gateway uses. Hermeticity is enforced by an outbound fetch interceptor: if anything in the suite tries to leave the process for the network, the run fails. This job blocks the merge.\",\"okf_concept\":\"mcpwasm-readme-ci-72\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: CI\"}},{\"id\":\"rfc-skills-in-llms-txt-1-3-the-well-known-convention-is-limited-11#0\",\"metadata\":{\"content\":\"**Co-location matters.** An agent that reads `llms.txt` to understand a site already has the right document open. If skill discovery requires a separate probe, that is extra latency and a second source of truth.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-3-the-well-known-convention-is-limited-11\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.3 The well-known convention is limited\"}},{\"id\":\"mcpwasm-readme-development-notes-74#0\",\"metadata\":{\"content\":\"Each milestone is documented in its `reports/TAREA*-REPORT.md` (TAREA1 through TAREA45; `TAREA2` and `TAREA30` were skipped in numbering and `TAREA12B` is a continuation of TAREA12). The non-obvious bits live there:\",\"okf_concept\":\"mcpwasm-readme-development-notes-74\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Development notes\"}},{\"id\":\"ext-skill-attestations-2-1-the-attestation-object-5#0\",\"metadata\":{\"content\":\"| Field | Requirement | Meaning | |---|---|---| | `origin` | REQUIRED | The publishing origin the review applies to. | | `skill` | REQUIRED | Skill name as it appears in `llms.txt`. | | `tool_sha256` | REQUIRED | The exact artifact hash reviewed. A new artifact version voids the attestation by construction. | | `attester` | REQUIRED | Reviewer identity, `<type>:<name>` (e.g. `human:mauricio`). | | `signed_on` / `valid_until` | REQUIRED | Validity window, `YYYY-MM-DD` in UTC. An attestation is EXPIRED when the current UTC date is strictly greater than `valid_until` (the end date is inclusive). | | `signature` | REQUIRED | Ed25519 over the signing payload (§2.2), **base64** (aligned with the core RFC tooling, `scripts/verify_signatures.py`).\",\"okf_concept\":\"ext-skill-attestations-2-1-the-attestation-object-5\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.1 The attestation object\"}},{\"id\":\"ext-skill-attestations-6-open-questions-27#0\",\"metadata\":{\"content\":\"1. Discovery of third-party attestations not hosted on the publishing origin (reviewer-side feeds? aggregators?). 2. Revocation before expiry (a static revocation list at the same well-known path is the zero-infra candidate). 3. Should `enforcing` mode distinguish per-capability risk (e.g. require attestation only for skills using POST)? 4. Alignment with core RFC §4.6: a signed `llms.txt` could pin `attestations.json` transitively.\",\"okf_concept\":\"ext-skill-attestations-6-open-questions-27\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 6. Open questions\"}},{\"id\":\"mcpwasm-readme-why-24#0\",\"metadata\":{\"content\":\"MCP clients (Claude, Cursor, others) can call arbitrary tools. Running a third-party tool's code directly in your backend means that code can read your secrets, hit your DB, phone home, or loop forever. You either trust the author fully or you don't run the tool.\",\"okf_concept\":\"mcpwasm-readme-why-24\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Why\"}},{\"id\":\"rfc-skills-in-llms-txt-5-2-recommended-agent-behavior-non-norma-73#0\",\"metadata\":{\"content\":\"When an agent runtime decides to implement proactive discovery, this is the recommended flow:\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-2-recommended-agent-behavior-non-norma-73\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.2 Recommended Agent Behavior (Non-Normative)\"}},{\"id\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-79#0\",\"metadata\":{\"content\":\"**Why this happens:** - Agent runtimes do not have a \\\"skill execution mode\\\" that suspends generic tools and forces compliance with a downloaded `SKILL.md`. - Skills are not sandboxed or enforced; they are suggestions that the model may or may not follow. - The reward function of the agent (\\\"complete the user's task\\\") does not penalize tool substitution if the outcome is similar.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-79\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.3 Observed Execution Gap\"}},{\"id\":\"rfc-skills-in-llms-txt-2-3-discovery-flow-22#0\",\"metadata\":{\"content\":\"Step 5 is mandatory. Agents MUST NOT auto-install or auto-activate skills without explicit user approval (see §4).\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-3-discovery-flow-22\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2.3 Discovery flow\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-66#0\",\"metadata\":{\"content\":\"**Limitation:** DNS TXT records are limited to ~255 bytes per string and require DNS access, which sandboxed agents may not have.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-66\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"ext-skill-attestations-4-runtime-behavior-25#0\",\"metadata\":{\"content\":\"**Sigstore verification is the one exception to \\\"no callbacks\\\":** confirming a Fulcio certificate chain and a Rekor inclusion proof needs network access to Sigstore's public trust infrastructure (the core RFC §4.6 already flags this trade-off for identity-bound provenance). A platform without outbound network access at verification time (or without filesystem access to cache Sigstore's TUF-distributed trust root — **confirmed in the mcpwasm reference implementation: Cloudflare Workers cannot run Sigstore verification for exactly this reason**) simply does not support the `sigstore:` attester type; it MUST fall back to treating such an entry as UNKNOWN-ATTESTER, not silently accept or crash.\",\"okf_concept\":\"ext-skill-attestations-4-runtime-behavior-25\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 4. Runtime behavior\"}},{\"id\":\"mcpwasm-readme-ci-73#0\",\"metadata\":{\"content\":\"The `prod-integration` job runs `npm run gateway`, the online gateway suite against the deployed production workers (`*.rckflr.workers.dev`) over the public internet. This is the only command in CI that reaches production, and its purpose is to detect drift between the fakes and the real workers. It is non-blocking (`continue-on-error`): an outage on their side surfaces as a warning, not a red gate, so a foreign incident cannot block work in this repo.\",\"okf_concept\":\"mcpwasm-readme-ci-73\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: CI\"}},{\"id\":\"mcpwasm-readme-development-notes-76#0\",\"metadata\":{\"content\":\"Benchmark headline numbers (full matrix and methodology in [`BENCHMARK.md`](./BENCHMARK.md), single-client from México to the Workers edge, not a load test; latest figures from the post-pool+preheat run): the sandbox itself costs **~2 ms warm** (gateway pure-sandbox `sum_numbers` p50 ≈ 55 ms vs. the same worker's raw ping p50 ≈ 53 ms), and the full gateway adds **~6 ms** over calling the publisher's API directly for the same read (`stock_report` through the gateway p50 = 96 ms vs. direct API p50 = 90 ms). A cold discovery miss costs ~210–400 ms (compile + sha256 + fetch); the scheduled preheat (see \\\"Security model\\\" above) keeps the cron's own isolate/colo mostly out of this cold path.\",\"okf_concept\":\"mcpwasm-readme-development-notes-76\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Development notes\"}},{\"id\":\"mcpwasm-readme-try-the-deployed-gateway-curl-65#0\",\"metadata\":{\"content\":\"Call `server_time` (async tool that calls `host.fetchOrigin(\\\"/api/time\\\")` on the allowed origin):\",\"okf_concept\":\"mcpwasm-readme-try-the-deployed-gateway-curl-65\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Try the deployed gateway (curl)\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-22#0\",\"metadata\":{\"content\":\"```bash npx -y @rckflr/mcpwasm --serve ./my-skills-repo\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-22\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-63#0\",\"metadata\":{\"content\":\"**Reference implementation:** [DemoShop](https://demoshop-88e.pages.dev) sends this header on every API response.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-63\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"rfc-skills-in-llms-txt-3-ecosystem-comparison-28#0\",\"metadata\":{\"content\":\"| Dimension | MCP | A2A | `/.well-known/skills/` | **`## Skills` in `llms.txt`** | |---|---|---|---|---| | Requires server process | Yes | Yes | **No** | **No** | | Works on static hosts | No | No | **Yes** | **Yes** | | Multi-skill per domain | Yes | Yes | **No** (fixed path) | **Yes** | | Co-located with `llms.txt` | No | No | No | **Yes** | | Zero infrastructure beyond static files | No | No | **Yes** | **Yes** | | Complex / stateful integrations | **Yes** | **Yes** | No | No | | Simple API wrapping | Overkill | Overkill | Limited | **Designed for this** | | Version metadata inline | N/A | N/A | No | **Yes** | | User opt-in required | Runtime-dependent | Runtime-dependent | Runtime-dependent | **Mandatory** |\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-ecosystem-comparison-28\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3. Ecosystem Comparison\"}},{\"id\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-56#0\",\"metadata\":{\"content\":\"Today, most agents read `llms.txt` only when the user explicitly directs them to a URL or instructs them to research a site. Proactive, background skill discovery does not yet occur in mainstream runtimes.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-56\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5. On Agent-Side Discovery Triggers\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-43#0\",\"metadata\":{\"content\":\"**Evidence (reference POC, [`evals/poc_orchestration/`](../evals/poc_orchestration/README.md)).** Against a live n8n MCP server (which exposes **25 tools** to build a workflow), a small local model was driven through a real agentic loop in five arms spanning *how much capability sits in context as tool schemas vs. as skill prose*:\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-43\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-51#0\",\"metadata\":{\"content\":\"**What the signature actually buys you.** Publishing the public key on the same origin does not, by itself, defeat a fully compromised origin: an attacker who controls the server can swap key, signatures, and content together. The value is:\",\"okf_concept\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-51\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 4.6 Signing and authenticity (resolves Open Question 4)\"}},{\"id\":\"ext-skill-attestations-4-runtime-behavior-19#0\",\"metadata\":{\"content\":\"A runtime that supports this extension evaluates, per skill, at discovery time:\",\"okf_concept\":\"ext-skill-attestations-4-runtime-behavior-19\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 4. Runtime behavior\"}},{\"id\":\"ext-skill-attestations-4-runtime-behavior-20#0\",\"metadata\":{\"content\":\"| Verdict | Condition | |---|---| | `attested` | ≥1 attestation with valid signature from a registered key, matching `origin`+`skill`+`tool_sha256`, within its validity window | | `expired` | matching attestations exist but all are past `valid_until` | | `invalid` | a signature fails verification (SHOULD be surfaced loudly — a forged attestation is worse than none) | | `unattested` | no matching attestation (including hash mismatch after a skill update) |\",\"okf_concept\":\"ext-skill-attestations-4-runtime-behavior-20\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 4. Runtime behavior\"}},{\"id\":\"mcpwasm-readme-architecture-33#0\",\"metadata\":{\"content\":\"1. A publisher site ships `/llms.txt` plus per-skill `tool.js` and `SKILL.md` files. `llms.txt` lists each executable skill with a SHA-256 of its `tool.js`, and may declare an *origin memory* snapshot (see below). 2. On each request the gateway downloads `llms.txt`, parses the executable skills, downloads each `tool.js`, verifies SHA-256, and loads the verified ones into a fresh `AsyncToolHost` scoped to that origin. 3. Tool code runs inside QuickJS-wasm. It can only call `host.fetchOrigin(path, opts?)` (`opts`: `{method: \\\"GET\\\"|\\\"POST\\\", body?: string ≤16 KB, contentType?}` — write skills go through POST), which is async from the host side but synchronous-looking inside the sandbox (QuickJS asyncify suspends/resumes the wasm stack).\",\"okf_concept\":\"mcpwasm-readme-architecture-33\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Architecture\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-40#0\",\"metadata\":{\"content\":\"**This RFC is complementary, and sits at a third place: publisher-side, and one level up.** Two differences matter:\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-40\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"mcpwasm-readme-skills-47#0\",\"metadata\":{\"content\":\"- `scope` (optional, pattern `^[a-z][a-z0-9_-]*$`) declares the project namespace of a skill line. The runtime exposes the tool under the public name **`<scope>__<toolName>`** (`kdd__search_knowledge`); the rename happens at the **host boundary only** — the published `tool.js` bytes, its `tool_sha256`, and any attestations are untouched. This preserves the universal-template property: the same `tool.js` can be served by every publisher under different scopes with one ecosystem-wide hash. - One `skills-memory` line **per scope** (at most one without `scope`). Each skill gets `host.memorySearch` bound to **its own scope's** verified snapshot — memories are isolated per project. - An invalid `scope` value makes the line non-executable (reported, not loaded).\",\"okf_concept\":\"mcpwasm-readme-skills-47\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skills\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-69#0\",\"metadata\":{\"content\":\"#### Mechanism D — The `/llms.txt` convention probe\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-69\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-52#0\",\"metadata\":{\"content\":\"1. **Offline key.** Because the private key is not on the server, a server compromise *alone* (without the key) cannot forge valid signatures. The attacker can only serve unsigned or invalid-signature content, which a verifying agent rejects. 2. **Agent-side key pinning (TOFU).** The agent pins the origin's public key on first use and warns / requires re-confirmation if it later changes. This detects silent key or skill swaps across sessions — the gap that plain `sha256` cannot close.\",\"okf_concept\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-52\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 4.6 Signing and authenticity (resolves Open Question 4)\"}},{\"id\":\"mcpwasm-readme-use-as-a-library-npm-9#0\",\"metadata\":{\"content\":\"- In Cloudflare Workers, pass a pre-built asyncify module via the `quickjs` option (see `worker-gateway.mjs` for the `CompiledWasm` import pattern and import `@rckflr/mcpwasm/shim` first). - Subpath exports: `/host` (sync `ToolHost`), `/host-async`, `/mcp-core`, `/mcp-core-async`, `/llmstxt-parse`, `/shim`. - The sync `ToolHost` lazy-imports the optional peer `quickjs-emscripten` unless you pass a pre-built module; the async host's dependencies install with the package. - The package contains only the host/core/parser files plus the local runtime binary; the workers, publisher sites and test suites stay in this repo (they are the deployed reference, not the library).\",\"okf_concept\":\"mcpwasm-readme-use-as-a-library-npm-9\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Use as a library (npm)\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-11#0\",\"metadata\":{\"content\":\"MCP client configuration (Claude Code, Cursor, …):\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-11\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"rfc-skills-in-llms-txt-1-4-why-structured-over-prose-14#0\",\"metadata\":{\"content\":\"- **LLM-free parsing.** A regex parser can extract `## Skills` entries without invoking a model. Prose requires inference to normalize (\\\"here's a skill:\\\", \\\"you can use:\\\", \\\"for API access:\\\") — every site phrases it differently. - **Non-LLM tooling.** Validators, CI linters, skill crawlers, and IDE integrations need a fixed schema to function. Prose is unparseable without a model in the loop. - **Runtime auto-discovery.** An agent runtime can implement native `## Skills` support with a deterministic code path. With prose, the runtime needs per-site prompt engineering to extract the same signal. - **Cross-domain indexing.** A skills directory can index published skills across all domains by crawling `## Skills` sections. This is not possible with ad-hoc prose.\",\"okf_concept\":\"rfc-skills-in-llms-txt-1-4-why-structured-over-prose-14\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 1.4 Why structured over prose\"}},{\"id\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-57#0\",\"metadata\":{\"content\":\"This is not a defect of the proposal — it is the current state of the ecosystem. MCP, A2A, and `/.well-known/skills/` face the same trigger problem: all require the user or agent to know the site exists before discovery begins.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-on-agent-side-discovery-triggers-57\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5. On Agent-Side Discovery Triggers\"}},{\"id\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-84#0\",\"metadata\":{\"content\":\"**Why this happens:** - The agent interpreted the URL as a decorative reference, not as a service endpoint. - The runtime has no rule that says: \\\"when a user mentions a domain, check `/llms.txt` before acting.\\\" - The default behavior is \\\"do what the user asked with local tools\\\" rather than \\\"delegate to the domain's published skills.\\\"\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-84\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.4 Discovery from User Instructions\"}},{\"id\":\"ext-executable-skills-2-1-skill-entry-10#0\",\"metadata\":{\"content\":\"Rationale for staying inline (rather than only in `/.well-known/agent-skills/index.json`): the core standard's value proposition is *one static file, no infrastructure*. Two inline keys preserve that. Publishers who already maintain the well-known index MAY mirror the same `tool`/`tool_sha256` fields there; if both are present and disagree, runtimes MUST refuse the skill.\",\"okf_concept\":\"ext-executable-skills-2-1-skill-entry-10\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.1 Skill entry\"}},{\"id\":\"mcpwasm-readme-development-notes-75#0\",\"metadata\":{\"content\":\"- `reports/TAREA4-REPORT.md` — deploying to Cloudflare Workers: the `CompiledWasm` rule and why importing the `.wasm` as a static module avoids \\\"Wasm code generation disallowed by embedder\\\". - `reports/TAREA5-REPORT.md` — the asyncify spike: why asyncify is needed for an `await`-shaped capability, and the promise-pumping loop in `AsyncToolHost.callTool`. - `reports/TAREA7-REPORT.md` — the gateway: sha256 verification, the Cache API use, and the Cloudflare error 1042 (same-account worker-to-worker fetch via `workers.dev`) workaround via a service binding. - `reports/TAREA12-REPORT.md` / `reports/TAREA12B-REPORT.md` — `Date.now()` is frozen in Cloudflare Workers during synchronous execution, so a wall-clock deadline never cuts a `while(true){}`.\",\"okf_concept\":\"mcpwasm-readme-development-notes-75\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Development notes\"}},{\"id\":\"ext-skill-attestations-1-motivation-4#0\",\"metadata\":{\"content\":\"This layer is **OPTIONAL**. Publishers who skip it lose nothing they have today; runtimes decide what unattested skills are worth.\",\"okf_concept\":\"ext-skill-attestations-1-motivation-4\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 1. Motivation\"}},{\"id\":\"mcpwasm-readme-development-notes-77#0\",\"metadata\":{\"content\":\"Run the e2e tests with `npm test` (sync) / `npm run spike` (async) / `npm run gateway` (gateway against the live demo site) / `npm run memspike` (memory capability against the docs-site origin).\",\"okf_concept\":\"mcpwasm-readme-development-notes-77\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Development notes\"}},{\"id\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-14#0\",\"metadata\":{\"content\":\"Both the local runtime and the gateway now also fetch `/.well-known/agent-skills/index.json` — the canonical metadata layer the core llms-txt-skills RFC defines (§8 Open Question 5: `llms.txt` is the zero-fetch discovery pointer, `index.json` is the metadata/verification source of truth). When a skill's name appears in both `llms.txt` and `index.json`, and the latter declares a `tool_sha256`, it **must** match the one declared in `llms.txt`; a mismatch rejects the skill (drift/tampering signal) exactly like a `tool_sha256` mismatch against the fetched `tool.js` itself. Absence of `index.json` (most origins today) changes nothing.\",\"okf_concept\":\"mcpwasm-readme-local-mcp-runtime-no-gateway-at-all-14\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Local MCP runtime — no gateway at all\"}},{\"id\":\"ext-skill-attestations-2-2-signing-payload-7#0\",\"metadata\":{\"content\":\"**`origin` is deliberately inside the payload.** Identical bytes on a different origin are a *different skill in effect*: `fetchOrigin` is scoped to the publishing origin, so the same code talks to a different backend. An attestation MUST NOT be replayable across origins. (This is the one place this extension diverges from the donor PoC, which had no cross-site dimension.)\",\"okf_concept\":\"ext-skill-attestations-2-2-signing-payload-7\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.2 Signing payload\"}},{\"id\":\"mcpwasm-readme-development-notes-75#1\",\"metadata\":{\"content\":\"wall-clock deadline never cuts a `while(true){}`. Fix: a deterministic gas budget — the interrup\",\"okf_concept\":\"mcpwasm-readme-development-notes-75\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Development notes\"}},{\"id\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-47#0\",\"metadata\":{\"content\":\"**Reproducing the server, not just shrinking it.** A companion experiment ([`n8n-skills-sdk`](https://github.com/MauricioPerera/n8n-skills-sdk)) takes the last rung — *no MCP at all*. The n8n MCP's build path turns out to be a thin wrapper over the [`@n8n/workflow-sdk`](https://www.npmjs.com/package/@n8n/workflow-sdk) package (local code→JSON parse + `validateWorkflow`) plus one REST call (`POST /workflows`): `get_sdk_reference`, `validate_workflow`, and `create_workflow_from_code` map onto the package's own exports and the public API. Delivered as a published `SKILL.md` + three local tools (`reference`/`validate`/`create`), a local model built, validated, and created the same workflows the MCP does — in **~3 tool calls vs.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-3-skills-as-the-recipe-layer-over-tool-47\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.3 Skills as the *recipe* layer over tool discovery\"}},{\"id\":\"ext-skill-attestations-extension-skill-attestations-0#0\",\"metadata\":{\"content\":\"**Status:** Draft (v0.4) **Date:** 2026-07-09 **Extends:** [Extension: Executable Skills](./ext-executable-skills.md) (v0.4) **Design donor:** the three-layer knowledge-governance PoC in [ccdd/examples/okf-integration](https://github.com/MauricioPerera/ccdd/tree/main/examples/okf-integration) (working Ed25519 attestation + freshness tooling), adapted here to executable skills.\",\"okf_concept\":\"ext-skill-attestations-extension-skill-attestations-0\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: Extension: Skill Attestations\"}},{\"id\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-53#0\",\"metadata\":{\"content\":\"**Higher assurance.** For identity-bound provenance, publishers SHOULD consider **keyless signing via a transparency log** (e.g., Sigstore): the artifact is signed by an OIDC identity — such as the CI workflow that built it — and recorded in a public log, so verification asserts *who* produced the artifact, not merely that *some* key did. Trade-off: verification needs network access to the transparency log, which sandboxed agents may lack; same-origin ed25519 + pinning works fully offline.\",\"okf_concept\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-53\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 4.6 Signing and authenticity (resolves Open Question 4)\"}},{\"id\":\"ext-skill-attestations-1-motivation-1#0\",\"metadata\":{\"content\":\"The executable-skills extension gives two rings of trust: `tool_sha256` proves **integrity** (these bytes are these bytes) and core RFC §4.6 signatures prove **authenticity** (the publisher signed them). Neither answers the question an agent operator actually cares about before running third-party code: **has a human I trust reviewed this code, and do they still stand behind it?** A correctly hashed, correctly signed malicious artifact passes both rings — the residual risk §4 of the executable-skills extension explicitly admits.\",\"okf_concept\":\"ext-skill-attestations-1-motivation-1\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 1. Motivation\"}},{\"id\":\"rfc-skills-in-llms-txt-10-changelog-95#0\",\"metadata\":{\"content\":\"- **v0.10 (2026-07-10):** Open Question 6 RESOLVED by Executable Skills extension v0.5 SS2.5 (scopes: declarative per-skill `scope` -> runtime-side `<scope>__<name>` prefixing + scoped `skills-memory` lines). The OQ entry records the resolution and keeps the original problem statement for provenance. No core-RFC normative change. - **v0.9 (2026-07-10):** Added Open Question 6 (multi-project origins / namespacing), from field experience aggregating two real publishers (llms-txt-skills, KDD) on one GitHub Pages root origin: skill-name uniqueness per origin and the single `skills-memory` line make same-origin multi-project aggregation collide today; candidate directions listed, unresolved. No normative change.\",\"okf_concept\":\"rfc-skills-in-llms-txt-10-changelog-95\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 10. Changelog\"}},{\"id\":\"ext-executable-skills-6-open-questions-29#0\",\"metadata\":{\"content\":\"1. Should `tool`/`tool_sha256` live only in `/.well-known/agent-skills/index.json` instead of inline, keeping the inline comment single-key? (This draft chooses inline for zero-infrastructure parity; feedback welcome.) 2. Artifact size limit (the reference implementation truncates fetched bodies at 4 KB for `fetchOrigin` but does not yet cap artifact size). 3. A declared capability list per skill (e.g. `\\\"capabilities\\\":[\\\"fetchOrigin\\\"]`) so runtimes can surface least-privilege prompts before loading. 4. WASM artifacts as a second artifact type (`tool.wasm` + WIT-style interface) for non-JS publishers.\",\"okf_concept\":\"ext-executable-skills-6-open-questions-29\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 6. Open questions\"}},{\"id\":\"mcpwasm-readme-skills-40#0\",\"metadata\":{\"content\":\"The reference publisher is the docs-site (see \\\"Repository layout\\\"): it serves the spec snapshot and a `search_spec` skill that runs `host.memorySearch(args.q, k)` to do BM25 search over the four llms-txt-skills documents, plus `get_doc` and `list_docs`.\",\"okf_concept\":\"mcpwasm-readme-skills-40\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skills\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-56#0\",\"metadata\":{\"content\":\"`scripts/attest.mjs` is the signing tool (Node `node:crypto` Ed25519, no deps):\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-56\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"mcpwasm-readme-origin-memory-search-over-static-content-38#0\",\"metadata\":{\"content\":\"``` <!-- skills-memory: {\\\"snapshot\\\":\\\"/skills-index.snapshot\\\",\\\"snapshot_sha256\\\":\\\"a0235f071aa7e28f2096312f22f1ad035901595f3fa91d2cc92b5879bbb7f6d5\\\",\\\"format\\\":\\\"minimemory-okf-v1\\\"} -->\",\"okf_concept\":\"mcpwasm-readme-origin-memory-search-over-static-content-38\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Origin memory (search over static content)\"}},{\"id\":\"rfc-skills-in-llms-txt-skills-18#0\",\"metadata\":{\"content\":\"1. The section heading MUST be exactly `## Skills` (case-insensitive). 2. Each list item MUST follow the standard `llms.txt` link convention: `- [title](URL): description`. 3. The URL MUST resolve to either: - A raw `SKILL.md` document (`Content-Type: text/markdown`), OR - An archive ending in `.zip` or `.tar.gz` containing `SKILL.md` at the archive root. 4. The remote `SKILL.md` MUST be a valid Agent Skill (YAML frontmatter + body per the Agent Skills spec). 5. The URL SHOULD be same-origin as the `llms.txt`. Cross-origin URLs are permitted but carry additional security implications (see §4). 6. The description SHOULD match or summarize the `description` field in the skill's frontmatter.\",\"okf_concept\":\"rfc-skills-in-llms-txt-skills-18\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: Skills\"}},{\"id\":\"ext-executable-skills-5-reference-implementation-28#0\",\"metadata\":{\"content\":\"The gateway demonstrates: discovery from `llms.txt`, per-skill SHA-256 verification with exclusion on mismatch, sandboxed execution (QuickJS-wasm) with async handlers in per-skill contexts, origin-scoped `fetchOrigin` including POST write skills (a real order decrementing D1 stock), deterministic gas interruption of infinite loops, and end-to-end consumption by an unmodified MCP client (headless Claude configured with only the gateway URL).\",\"okf_concept\":\"ext-executable-skills-5-reference-implementation-28\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 5. Reference implementation\"}},{\"id\":\"ext-executable-skills-3-runtime-requirements-23#0\",\"metadata\":{\"content\":\"A runtime that executes skills published under this extension (a *gateway*, an agent-embedded engine, etc.):\",\"okf_concept\":\"ext-executable-skills-3-runtime-requirements-23\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 3. Runtime requirements\"}},{\"id\":\"mcpwasm-readme-skill-attestations-advisory-58#0\",\"metadata\":{\"content\":\"The private key lives in `.attester-key.json` and is **local and gitignored** — never commit it, and it is never printed by the tool. No key material belongs in this repo or in `REVIEWERS` (only public keys).\",\"okf_concept\":\"mcpwasm-readme-skill-attestations-advisory-58\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skill attestations (advisory)\"}},{\"id\":\"ext-skill-attestations-2-4-choosing-an-attester-type-normative--15#0\",\"metadata\":{\"content\":\"- **Sigstore (`sigstore:`) is RECOMMENDED for new deployments.** It scales without per-reviewer key registration (the bottleneck §5 names), binds attestations to auditable OIDC *identities* rather than bearer keys, and removes the private-key management burden entirely (Fulcio issues short-lived certificates per signing operation). This is the core RFC §4.6 recommendation applied to this extension. - **Pre-registered Ed25519 (`human:`) is the profile for constrained environments.** Choose it when verification must run on a platform without outbound network or filesystem access at verification time (confirmed: Cloudflare Workers, §4), in air-gapped or offline review workflows, or when a deployment deliberately avoids depending on external trust infrastructure (Fulcio/Rekor availability).\",\"okf_concept\":\"ext-skill-attestations-2-4-choosing-an-attester-type-normative--15\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.4 Choosing an attester type (normative guidance)\"}},{\"id\":\"mcpwasm-readme-skills-46#0\",\"metadata\":{\"content\":\"- [search_knowledge](/KDD/skills/search_knowledge/SKILL.md): … <!-- skill: {\\\"version\\\":\\\"1.0.0\\\",\\\"tool\\\":\\\"/KDD/skills/search_knowledge/tool.js\\\",\\\"tool_sha256\\\":\\\"…\\\",\\\"scope\\\":\\\"kdd\\\"} --> ```\",\"okf_concept\":\"mcpwasm-readme-skills-46\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skills\"}},{\"id\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-23#0\",\"metadata\":{\"content\":\"**Pattern A — API wrapping.** The site has a public HTTP API. The skill teaches the agent how to authenticate and which endpoints to call. The agent executes calls directly against the API.\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-23\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2.4 Two primary use cases\"}},{\"id\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-32#0\",\"metadata\":{\"content\":\"- **Different layer.** `auth.md` answers *\\\"how does an agent authenticate to this service?\\\"*; `## Skills` answers *\\\"what can an agent do here, and how?\\\"*. This RFC's example skills are deliberately auth-free; `auth.md` is exactly the missing authentication layer. A fully agent-ready domain can serve **both**: `## Skills` for capability discovery and `AUTH.md` for credential acquisition. - **Discovery alignment.** `auth.md` is discovered through a `.well-known` pointer that carries a `skill` field referencing the manifest — the same shape as this RFC's `/.well-known/agent-skills/index.json` metadata layer (§8, Open Question 5). The two `.well-known` documents can coexist.\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-1-relationship-to-adjacent-protocols-32\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.1 Relationship to adjacent protocols\"}},{\"id\":\"mcpwasm-readme-skills-39#0\",\"metadata\":{\"content\":\"- `snapshot` is a path (relative to the origin) to a BM25 snapshot in the `minimemory-okf-v1` format (built by the [`@rckflr/minimemory`](https://www.npmjs.com/package/@rckflr/minimemory) engine; the wasm binary ships as `minimemory_bg.wasm`). - `snapshot_sha256` is the hex SHA-256 of the snapshot bytes. Both runtimes download the snapshot and **verify it against this hash before injecting the capability** — same content-addressing rule as `tool.js`. On mismatch, fetch failure, non-200, or an unsupported `format`, the snapshot is discarded and **the capability is not injected**.\",\"okf_concept\":\"mcpwasm-readme-skills-39\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Skills\"}},{\"id\":\"ext-executable-skills-2-1-skill-entry-8#0\",\"metadata\":{\"content\":\"| Key | Type | Requirement | Meaning | |----------|--------|-------------|---------| | `version`| string | inherited from core RFC | Human-readable hint. | | `tool` | string | REQUIRED for executable skills | Path to the executable artifact. MUST be a same-origin path (relative or absolute path, never a full URL to another origin). | | `tool_sha256` | string | REQUIRED when `tool` is present | Lowercase hex SHA-256 of the exact bytes served at `tool`. | | `scope` | string | OPTIONAL | Namespace for multi-project origins (§2.5). Pattern `^[a-z][a-z0-9_-]*$`. When present, runtimes MUST expose this skill's tools under `<scope>__<tool-name>`. |\",\"okf_concept\":\"ext-executable-skills-2-1-skill-entry-8\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.1 Skill entry\"}},{\"id\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-49#0\",\"metadata\":{\"content\":\"**Threat model.** A `sha256` declared in `## Skills` or in `index.json` provides **integrity** — the content was not corrupted in transit or by a CDN. It does **not** provide **authenticity**, because the same document that points to the skill also asserts its hash. A party that can modify the `llms.txt` or the index can rewrite the pointer and the hash together. Defending against a *compromised or malicious publisher* requires a signature whose trust is not rooted solely in the live document.\",\"okf_concept\":\"rfc-skills-in-llms-txt-4-6-signing-and-authenticity-resolves-op-49\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 4.6 Signing and authenticity (resolves Open Question 4)\"}},{\"id\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-9#1\",\"metadata\":{\"content\":\"chain + Rekor transparency-log inclusion proof). The signed payload is an [in-toto Statement v1](https://in-toto.io/Statement/v1) whose `predicate` carries the *same five fields* as §2.2's canonical string (`origin`, `skill`, `tool_sha256`, `signed_on`, `valid_until`) — a verifier MUST confirm the DSSE-embedded predicate matches the attestation's own top-level fields; without that cross-check, a validly-signed bundle for skill A could be relabeled as skill B's attesta\",\"okf_concept\":\"ext-skill-attestations-2-1b-alternative-sigstore-keyless-attest-9\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 2.1b Alternative: Sigstore (keyless) attestation\"}},{\"id\":\"mcpwasm-readme-use-it-now-any-static-site-an-mcp-server-5#0\",\"metadata\":{\"content\":\"Point the local runtime at any origin that publishes `llms.txt` with executable skills. It fetches `/llms.txt`, verifies every `tool_sha256`, sandboxes each skill in QuickJS-wasm, and speaks MCP over stdio — **no account, no deploy, no infrastructure on either side**:\",\"okf_concept\":\"mcpwasm-readme-use-it-now-any-static-site-an-mcp-server-5\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Use it now — any static site → an MCP server\"}},{\"id\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-18#0\",\"metadata\":{\"content\":\"Updating content means re-exporting the snapshot and updating `snapshot_sha256` — same lifecycle as `tool_sha256` (§2.3).\",\"okf_concept\":\"ext-executable-skills-2-4-origin-memory-search-snapshots-18\",\"okf_type\":\"docs\",\"title\":\"ext-executable-skills: 2.4 Origin memory: search snapshots\"}},{\"id\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-26#0\",\"metadata\":{\"content\":\"**Pattern B — Interaction instructions.** The site has no dedicated API but wants to describe how an agent should interact with it. The skill contains heuristics, preferred phrasing, or task decomposition patterns.\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-26\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2.4 Two primary use cases\"}},{\"id\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-81#0\",\"metadata\":{\"content\":\"A related but distinct gap occurs when the user includes a domain in their prompt, but the agent does not associate that URL with the need to discover skills.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-4-discovery-from-user-instructions-81\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.4 Discovery from User Instructions\"}},{\"id\":\"mcpwasm-readme-scopes-multiple-projects-on-one-origin-44#0\",\"metadata\":{\"content\":\"One origin (e.g. a GitHub Pages root site) can aggregate skills from several projects. Without namespacing, two projects that both publish a `search_knowledge` tool collide, and only one `skills-memory` line can exist per origin. Scopes fix both:\",\"okf_concept\":\"mcpwasm-readme-scopes-multiple-projects-on-one-origin-44\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Scopes — multiple projects on one origin\"}},{\"id\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-67#0\",\"metadata\":{\"content\":\"In addition to the existing `link rel=\\\"alternate\\\"`, the site MAY include a visible `<meta>` tag in the HTML `<head>`:\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-1-proactive-discovery-mechanisms-67\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.1 Proactive Discovery Mechanisms\"}},{\"id\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-25#0\",\"metadata\":{\"content\":\"*Example:* [DemoShop](https://demoshop-88e.pages.dev) publishes three skills (product-search, cart-add, checkout-complete) that teach agents to browse a catalog, add items to a cart, and complete a purchase via its public HTTP API — all without authentication or server-side sessions.\",\"okf_concept\":\"rfc-skills-in-llms-txt-2-4-two-primary-use-cases-25\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 2.4 Two primary use cases\"}},{\"id\":\"mcpwasm-readme-quick-start-60#0\",\"metadata\":{\"content\":\"Requirements: Node 18+ and `npm install` (already done in this checkout).\",\"okf_concept\":\"mcpwasm-readme-quick-start-60\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Quick start\"}},{\"id\":\"mcpwasm-readme-static-mcp-vs-a-traditional-mcp-server-27#0\",\"metadata\":{\"content\":\"In mcpwasm, *publishing* (static files + a hash) and *execution* (a runtime that discovers, verifies, and sandboxes on demand) are two separate things. In a traditional MCP server they are the same thing — the server you deploy *is* the execution, with no isolation layer in between.\",\"okf_concept\":\"mcpwasm-readme-static-mcp-vs-a-traditional-mcp-server-27\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Static MCP vs. a traditional MCP server\"}},{\"id\":\"rfc-skills-in-llms-txt-3-2-relationship-to-agents-txt-the-actio-35#0\",\"metadata\":{\"content\":\"This RFC and `agents.txt` are **complementary along two axes**:\",\"okf_concept\":\"rfc-skills-in-llms-txt-3-2-relationship-to-agents-txt-the-actio-35\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 3.2 Relationship to `agents.txt` (the action layer)\"}},{\"id\":\"rfc-skills-in-llms-txt-6-why-this-is-worth-doing-86#0\",\"metadata\":{\"content\":\"- **Deployable on any static host.** A Cloudflare Pages site, a GitHub Pages repo, a Netlify deploy — any host that can serve a text file can publish skills through this mechanism. No server process required. - **Self-describing at the source.** A site ships its API *and* the skill for consuming it in the same deploy. No third-party marketplace required. - **Version-locked to the API.** When the API changes, the skill changes in the same commit. No drift between capability and documentation. - **Co-located discovery.** An agent reading `llms.txt` finds skills in the same document, in one fetch, with no additional probing.\",\"okf_concept\":\"rfc-skills-in-llms-txt-6-why-this-is-worth-doing-86\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 6. Why This Is Worth Doing\"}},{\"id\":\"mcpwasm-readme-why-25#0\",\"metadata\":{\"content\":\"mcpwasm removes the trust requirement for the *code*:\",\"okf_concept\":\"mcpwasm-readme-why-25\",\"okf_type\":\"docs\",\"title\":\"mcpwasm-readme: Why\"}},{\"id\":\"rfc-skills-in-llms-txt-9-reference-implementation-93#0\",\"metadata\":{\"content\":\"[`img.automators.work`](https://img.automators.work) is a live Cloudflare Pages static site — no server process, no MCP server, no A2A endpoint.\",\"okf_concept\":\"rfc-skills-in-llms-txt-9-reference-implementation-93\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 9. Reference Implementation\"}},{\"id\":\"ext-skill-attestations-5-security-considerations-26#0\",\"metadata\":{\"content\":\"- **What this buys:** an agent operator can require \\\"code reviewed by someone I chose, endorsement not expired\\\" before executing third-party artifacts. Compromising the publisher's site is no longer enough to ship malicious code to enforcing runtimes — the attacker also needs a registered reviewer's private key. - **What this does not buy:** a review is a point-in-time human judgment; it cannot guarantee the absence of vulnerabilities. Key compromise of a reviewer defeats the ring for that reviewer's attestations (mitigation: multiple reviewers, short windows). Revocation before `valid_until` is unsolved here (§6) — short validity windows are the honest stopgap.\",\"okf_concept\":\"ext-skill-attestations-5-security-considerations-26\",\"okf_type\":\"docs\",\"title\":\"ext-skill-attestations: 5. Security considerations\"}},{\"id\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-76#0\",\"metadata\":{\"content\":\"Discovery (Section 5) is only half the problem. Even when an agent successfully finds and reads a `SKILL.md`, there is no guarantee it will *execute* the skill according to its instructions.\",\"okf_concept\":\"rfc-skills-in-llms-txt-5-3-observed-execution-gap-76\",\"okf_type\":\"docs\",\"title\":\"rfc-skills-in-llms-txt: 5.3 Observed Execution Gap\"}}]";
const LLMS_TXT = "# llmstxt-docs\n\n> Publisher of the llms-txt-skills standard documents. Serves the RFC, the executable-skills and skill-attestations extensions, and the mcpwasm reference README, plus a hash-pinned BM25 search snapshot and 3 executable skills to query them.\n\n<!-- skills-memory: {\"snapshot\":\"/skills-index.snapshot\",\"snapshot_sha256\":\"9463e09b4127047cf98bbd446eeed4bf4b44cdddfc95a22cc548779d79a75039\",\"format\":\"minimemory-okf-v1\"} -->\n\n## Skills\n\n- [search_spec](/skills/search_spec/SKILL.md): BM25 search over the llms-txt-skills spec snapshot (4 docs). Returns hits {text,score,title,concept_id}. <!-- skill: {\"version\":\"1.0.0\",\"tool\":\"/skills/search_spec/tool.js\",\"tool_sha256\":\"bab53b2efd3064de693923919a0e63bb0aa2c4071cd58836932465692a1b55f6\"} -->\n- [get_doc](/skills/get_doc/SKILL.md): Fetch one of the 4 published documents by name. Returns {name,length,content} (content truncated to 4000 chars). <!-- skill: {\"version\":\"1.0.0\",\"tool\":\"/skills/get_doc/tool.js\",\"tool_sha256\":\"dff5130ed8ace0591e16ab1ef7276a53b6b0596141693b43a532737a43e42d31\"} -->\n- [list_docs](/skills/list_docs/SKILL.md): List the 4 published documents with title and path. Static, no fetch. <!-- skill: {\"version\":\"1.0.0\",\"tool\":\"/skills/list_docs/tool.js\",\"tool_sha256\":\"371bd294e89ba37e12f1665b12fba8eb2bed44d74eabfc672fb5daabd249f68d\"} -->\n";
const ATTESTATIONS = [{"origin":"https://llmstxt-docs.rckflr.workers.dev","skill":"search_spec","tool_sha256":"95301993d9e1b8881e489734914ca7e7ceea3f4220c162f14206238c3ecdbbee","attester":"human:mauricio","signed_on":"2026-07-02","valid_until":"2027-07-02","signature":"ohpC1s+7keIF/BYUmGTwIwWSxLrH0OVc5zIvfwYl2tyrYh8r/o/dfvmAld/tZmBgJyXPMbKMfy4qoZfPO7ueDA=="},{"origin":"https://llmstxt-docs.rckflr.workers.dev","skill":"get_doc","tool_sha256":"7cff29b54d5fdecb3c203c749475e9bae1955d3f5c397df4fb2ee9ac5a4eecd0","attester":"human:mauricio","signed_on":"2026-07-02","valid_until":"2027-07-02","signature":"WvlhjJAfQGBYZte4XQDD+C3OpGcyJIoiBPNsNs+vjbDoi0/7jwUhw4NrPfvXRJkYu4mTzWA6dMWTzPzdxvI2CA=="},{"origin":"https://llmstxt-docs.rckflr.workers.dev","skill":"list_docs","tool_sha256":"17d6175805386a0829012ab088c72ca98058255564a47230903c697432666735","attester":"human:mauricio","signed_on":"2026-07-03","valid_until":"2027-07-02","signature":"hdY0j0SmYg+DKE69rvgdK22FVdzuB1ULbU+PFo0zOALzXvT36aQrIYC49sFlpNLaajqEkyczQDemhIoIek9GCQ=="}];

const JSON_HEADERS = { "content-type": "application/json; charset=utf-8" };
const json = (obj, status = 200) => new Response(JSON.stringify(obj), { status, headers: JSON_HEADERS });

export default {
  async fetch(request) {
    const url = new URL(request.url);
    const path = url.pathname;

    if (path === "/llms.txt") {
      return new Response(LLMS_TXT, { headers: { "content-type": "text/plain; charset=utf-8", "cache-control": "no-store" } });
    }

    if (path === "/docs/rfc-skills-in-llms-txt.md") { return new Response(DOC_RFC_SKILLS_IN_LLMS_TXT_MD, { headers: { "content-type": "text/markdown; charset=utf-8", "cache-control": "no-store" } }); }
    if (path === "/docs/ext-executable-skills.md") { return new Response(DOC_EXT_EXECUTABLE_SKILLS_MD, { headers: { "content-type": "text/markdown; charset=utf-8", "cache-control": "no-store" } }); }
    if (path === "/docs/ext-skill-attestations.md") { return new Response(DOC_EXT_SKILL_ATTESTATIONS_MD, { headers: { "content-type": "text/markdown; charset=utf-8", "cache-control": "no-store" } }); }
    if (path === "/docs/mcpwasm-readme.md") { return new Response(DOC_MCPWASM_README_MD, { headers: { "content-type": "text/markdown; charset=utf-8", "cache-control": "no-store" } }); }

    if (path === "/skills/search_spec/tool.js") { return new Response(SEARCH_SPEC_TOOL_JS, { headers: { "content-type": "application/javascript; charset=utf-8", "cache-control": "no-store" } }); }
    if (path === "/skills/search_spec/SKILL.md") { return new Response(SEARCH_SPEC_SKILL_MD, { headers: { "content-type": "text/markdown; charset=utf-8", "cache-control": "no-store" } }); }
    if (path === "/skills/get_doc/tool.js") { return new Response(GET_DOC_TOOL_JS, { headers: { "content-type": "application/javascript; charset=utf-8", "cache-control": "no-store" } }); }
    if (path === "/skills/get_doc/SKILL.md") { return new Response(GET_DOC_SKILL_MD, { headers: { "content-type": "text/markdown; charset=utf-8", "cache-control": "no-store" } }); }
    if (path === "/skills/list_docs/tool.js") { return new Response(LIST_DOCS_TOOL_JS, { headers: { "content-type": "application/javascript; charset=utf-8", "cache-control": "no-store" } }); }
    if (path === "/skills/list_docs/SKILL.md") { return new Response(LIST_DOCS_SKILL_MD, { headers: { "content-type": "text/markdown; charset=utf-8", "cache-control": "no-store" } }); }

    if (path === "/skills-index.snapshot") {
      return new Response(SNAPSHOT, { headers: { "content-type": "application/octet-stream", "cache-control": "no-store" } });
    }

    if (path === "/.well-known/agent-skills/attestations.json") {
      return new Response(JSON.stringify(ATTESTATIONS), { headers: { "content-type": "application/json; charset=utf-8", "cache-control": "no-store" } });
    }

    return json({ error: "Not Found", path }, 404);
  }
};
